/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * Input and response types of the `{{ group.name }}` group. They are exported from the package root:
 *   import type { {{ (group.operations | first).params.name }} } from "xyp-sdk";
 */

{% for operation in group.operations %}
export type { {{ operation.exported_types | join(", ") }} } from "../operations/{{ group.name }}/{{ operation.name }}.js";
{% endfor %}
