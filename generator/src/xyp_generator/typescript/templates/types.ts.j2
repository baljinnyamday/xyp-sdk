/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * Input and response types of the `{{ group.name }}` group, for annotations:
 *   import type { types } from "xyp-sdk";
 *   let params: types.{{ group.name }}.{{ (group.operations | first).params.name }};
 */

{% for operation in group.operations %}
export type { {{ operation.exported_types | join(", ") }} } from "../operations/{{ group.name }}/{{ operation.name }}.js";
{% endfor %}
