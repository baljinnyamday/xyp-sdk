/** GENERATED by xyp-generator from spec/services.json. Do not edit. */

{% for group in groups %}
import { type {{ group.class_name }}Service, create{{ group.class_name }}Service } from "./services/{{ group.name }}.js";
{% endfor %}
import type { Transport } from "./transport.js";

/** Every service group. `Xyp` extends this, so they are available as `xyp.<group>`. */
export abstract class ServiceGroups {
{% for group in groups %}
  /** {{ group.operations | length }} services served from {{ group.endpoints | join(", ") }}. */
  readonly {{ group.name }}: {{ group.class_name }}Service;
{% endfor %}

  protected constructor(transport: Transport) {
{% for group in groups %}
    this.{{ group.name }} = create{{ group.class_name }}Service(transport);
{% endfor %}
  }
}
