/** GENERATED by xyp-generator from spec/services.json. Do not edit. */

/** Operation name -> endpoint it is served from. */
export const OPERATIONS: Readonly<Record<string, string>> = {
{% for service in api.services %}
  {{ service.operation }}: {{ service.endpoint | json }},
{% endfor %}
};

/**
 * Endpoint -> XML namespace, for endpoints whose WSDL is checked into spec/wsdl/.
 * Other endpoints have their namespace read from the live WSDL on first use.
 */
export const NAMESPACES: Readonly<Record<string, string>> = {
{% for endpoint, namespace in api.namespaces | dictsort %}
  {{ endpoint | json }}: {{ namespace | json }},
{% endfor %}
};
