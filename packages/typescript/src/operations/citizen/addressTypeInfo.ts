/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100116_addressTypeInfo (citizen, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100116_addressTypeInfo";

/** Input of WS100116_addressTypeInfo. */
export type AddressTypeInfoParams = Readonly<Record<string, never>>;

export interface AddressTypeInfoListData {
  readonly addressTypeCode: string | null;
  readonly addressTypeName: string | null;
}

/** WS100116_addressTypeInfo: Хаягийн төрөл хайх */
export interface AddressTypeInfoResponse {
  readonly listData: readonly AddressTypeInfoListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { addressTypeCode: "string", addressTypeName: "string" } } } };

/**
 * WS100116_addressTypeInfo: Хаягийн төрөл хайх
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function addressTypeInfo(transport: Transport, _params: AddressTypeInfoParams = {}, options: CallOptions = {}): Promise<XypResult<AddressTypeInfoResponse>> {
  const data = await transport.call(OPERATION, {}, options);
  return decodeResponse<AddressTypeInfoResponse>("AddressTypeInfoResponse", SCHEMA, data);
}
