/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS101161_GetTopicScope (citizen, ҮНДЭСНИЙ АУДИТЫН ГАЗАР)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS101161_GetTopicScope";

/** Input of WS101161_GetTopicScope. */
export interface GetTopicScopeParams {
  /** төлөв */
  readonly status?: boolean;
}

/** жагсаалт */
export interface GetTopicScopeList {
  /** Сэдвийн хамрах хүрээний ID */
  readonly id: number | null;
  /** Сэдвийн хамрах хүрээний нэр */
  readonly name: string | null;
}

/** WS101161_GetTopicScope: Сэдвийн хамрах хүрээний ID авах сервис */
export interface GetTopicScopeResponse {
  /** жагсаалт */
  readonly list: readonly GetTopicScopeList[] | null;
}

const SCHEMA: Schema = { list: { list: { object: { id: "int", name: "string" } } } };

/**
 * WS101161_GetTopicScope: Сэдвийн хамрах хүрээний ID авах сервис
 *
 * Provider: ҮНДЭСНИЙ АУДИТЫН ГАЗАР
 */
export async function getTopicScope(transport: Transport, params: GetTopicScopeParams = {}, options: CallOptions = {}): Promise<XypResult<GetTopicScopeResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      status: params.status,
    },
    options,
  );
  return decodeResponse<GetTopicScopeResponse>("GetTopicScopeResponse", SCHEMA, data);
}
