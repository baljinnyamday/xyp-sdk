/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100106_authorizeCitizen (citizen, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100106_authorizeCitizen";

/** Input of WS100106_authorizeCitizen. */
export interface AuthorizeCitizenParams {
  readonly civilId?: string;
  readonly regnum?: string;
}

/** WS100106_authorizeCitizen: Иргэнийг хурууны хээгээр баталгаажуулах сервис */
export interface AuthorizeCitizenResponse {
  /** Мөн эсэх */
  readonly result: boolean | null;
}

const SCHEMA: Schema = { result: "bool" };

/**
 * WS100106_authorizeCitizen: Иргэнийг хурууны хээгээр баталгаажуулах сервис
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function authorizeCitizen(transport: Transport, params: AuthorizeCitizenParams = {}, options: CallOptions = {}): Promise<XypResult<AuthorizeCitizenResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      civilId: params.civilId,
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<AuthorizeCitizenResponse>("AuthorizeCitizenResponse", SCHEMA, data);
}
