/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100139_getCitizenChangeRegnumInfo (citizen, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100139_getCitizenChangeRegnumInfo";

/** Input of WS100139_getCitizenChangeRegnumInfo. */
export interface GetCitizenChangeRegnumInfoParams {
  /** Иргэний хувийн дугаар */
  readonly civilId?: string;
  /** регистрийн дугаар */
  readonly regnum?: string;
}

/** WS100139_getCitizenChangeRegnumInfo: Регистрийн дугаар өөрчилсний бүртгэлийн лавлагаа */
export interface GetCitizenChangeRegnumInfoResponse {
  /** Ургийн овог */
  readonly forename: string | null;
  /** Эцэг эхийн нэр */
  readonly surname: string | null;
  /** Нэр */
  readonly givenname: string | null;
  /** Хуучин регистрийн дугаар */
  readonly regnumOld: string | null;
  /** Шалтгаан код */
  readonly causeCode: string | null;
  /** Шалтгаан */
  readonly causeName: string | null;
  /** Шинэ регистрийн дугаар */
  readonly regnumNew: string | null;
}

const SCHEMA: Schema = { forename: "string", surname: "string", givenname: "string", regnumOld: "string", causeCode: "string", causeName: "string", regnumNew: "string" };

/**
 * WS100139_getCitizenChangeRegnumInfo: Регистрийн дугаар өөрчилсний бүртгэлийн лавлагаа
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function getCitizenChangeRegnumInfo(transport: Transport, params: GetCitizenChangeRegnumInfoParams = {}, options: CallOptions = {}): Promise<XypResult<GetCitizenChangeRegnumInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      civilId: params.civilId,
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<GetCitizenChangeRegnumInfoResponse>("GetCitizenChangeRegnumInfoResponse", SCHEMA, data);
}
