/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100148_getCitizenHealthInsuranceInfo (citizen, Эрүүл мэндийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100148_getCitizenHealthInsuranceInfo";

/** Input of WS100148_getCitizenHealthInsuranceInfo. */
export interface GetCitizenHealthInsuranceInfoParams {
  /** регистрийн дугаар */
  readonly regnum?: string;
  /** Иргэний хувийн дугаар */
  readonly civilId?: string;
}

/** Мэдээллийн жагсаалт */
export interface GetCitizenHealthInsuranceInfoListData {
  /** Он */
  readonly year: string | null;
  /** Сар */
  readonly month: string | null;
  /** Хаана төлсөн */
  readonly officeName: string | null;
  /** Регистрийн дугаар */
  readonly regnum: string | null;
}

/** WS100148_getCitizenHealthInsuranceInfo: Эрүүл мэндийн даатгалын нэгдсэн лавлагаа олгох */
export interface GetCitizenHealthInsuranceInfoResponse {
  /** Мэдээллийн жагсаалт */
  readonly listData: readonly GetCitizenHealthInsuranceInfoListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { year: "string", month: "string", officeName: "string", regnum: "string" } } } };

/**
 * WS100148_getCitizenHealthInsuranceInfo: Эрүүл мэндийн даатгалын нэгдсэн лавлагаа олгох
 *
 * Provider: Эрүүл мэндийн даатгалын ерөнхий газар
 */
export async function getCitizenHealthInsuranceInfo(transport: Transport, params: GetCitizenHealthInsuranceInfoParams = {}, options: CallOptions = {}): Promise<XypResult<GetCitizenHealthInsuranceInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      civilId: params.civilId,
    },
    options,
  );
  return decodeResponse<GetCitizenHealthInsuranceInfoResponse>("GetCitizenHealthInsuranceInfoResponse", SCHEMA, data);
}
