/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100136_checkCitizenNonPassport (citizen, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100136_checkCitizenNonPassport";

/** Input of WS100136_checkCitizenNonPassport. */
export interface CheckCitizenNonPassportParams {
  /** Иргэний хувийн дугаар */
  readonly civilId?: string;
  /** регистрийн дугаар */
  readonly regnum?: string;
}

/** WS100136_checkCitizenNonPassport: Үндэсний энгийн гадаад паспортын бүртгэлгүй тухай лавлагаа */
export interface CheckCitizenNonPassportResponse {
  readonly result: boolean | null;
}

const SCHEMA: Schema = { result: "bool" };

/**
 * WS100136_checkCitizenNonPassport: Үндэсний энгийн гадаад паспортын бүртгэлгүй тухай лавлагаа
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function checkCitizenNonPassport(transport: Transport, params: CheckCitizenNonPassportParams = {}, options: CallOptions = {}): Promise<XypResult<CheckCitizenNonPassportResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      civilId: params.civilId,
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<CheckCitizenNonPassportResponse>("CheckCitizenNonPassportResponse", SCHEMA, data);
}
