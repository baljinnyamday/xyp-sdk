/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100129_getCitizenFoulInfo (citizen, Цагдаагийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100129_getCitizenFoulInfo";

/** Input of WS100129_getCitizenFoulInfo. */
export interface GetCitizenFoulInfoParams {
  readonly regnum?: string;
}

/** WS100129_getCitizenFoulInfo: Иргэний зөрчлийн мэдээлэл дамжуулах сервис */
export interface GetCitizenFoulInfoResponse {
  readonly listData: readonly unknown[] | null;
}

const SCHEMA: Schema = { listData: { list: "any" } };

/**
 * WS100129_getCitizenFoulInfo: Иргэний зөрчлийн мэдээлэл дамжуулах сервис
 *
 * Provider: Цагдаагийн ерөнхий газар
 */
export async function getCitizenFoulInfo(transport: Transport, params: GetCitizenFoulInfoParams = {}, options: CallOptions = {}): Promise<XypResult<GetCitizenFoulInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<GetCitizenFoulInfoResponse>("GetCitizenFoulInfoResponse", SCHEMA, data);
}
