/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100125_checkCitizenRegnum (citizen, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100125_checkCitizenRegnum";

/** Input of WS100125_checkCitizenRegnum. */
export interface CheckCitizenRegnumParams {
  readonly regnum?: string;
}

/** WS100125_checkCitizenRegnum: Иргэний регистрийн дугаар үнэн эсэхийг шалгах */
export interface CheckCitizenRegnumResponse {
  readonly false: boolean | null;
}

const SCHEMA: Schema = { false: "bool" };

/**
 * WS100125_checkCitizenRegnum: Иргэний регистрийн дугаар үнэн эсэхийг шалгах
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function checkCitizenRegnum(transport: Transport, params: CheckCitizenRegnumParams = {}, options: CallOptions = {}): Promise<XypResult<CheckCitizenRegnumResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<CheckCitizenRegnumResponse>("CheckCitizenRegnumResponse", SCHEMA, data);
}
