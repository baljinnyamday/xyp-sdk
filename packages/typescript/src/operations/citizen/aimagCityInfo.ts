/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100111_aimagCityInfo (citizen, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100111_aimagCityInfo";

/** Input of WS100111_aimagCityInfo. */
export type AimagCityInfoParams = Readonly<Record<string, never>>;

export interface AimagCityInfoListData {
  readonly aimagCityCode: string | null;
  readonly aimagCityName: string | null;
}

/** WS100111_aimagCityInfo: Аймаг,хот хайх */
export interface AimagCityInfoResponse {
  readonly listData: readonly AimagCityInfoListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { aimagCityCode: "string", aimagCityName: "string" } } } };

/**
 * WS100111_aimagCityInfo: Аймаг,хот хайх
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function aimagCityInfo(transport: Transport, _params: AimagCityInfoParams = {}, options: CallOptions = {}): Promise<XypResult<AimagCityInfoResponse>> {
  const data = await transport.call(OPERATION, {}, options);
  return decodeResponse<AimagCityInfoResponse>("AimagCityInfoResponse", SCHEMA, data);
}
