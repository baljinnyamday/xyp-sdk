/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS102102_RegnumChangeInfoEnglish (citizen, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS102102_RegnumChangeInfoEnglish";

/** Input of WS102102_RegnumChangeInfoEnglish. */
export interface RegnumChangeInfoEnglishParams {
  /** Регистр */
  readonly regnum?: string;
}

/** Жагсаалт */
export interface RegnumChangeInfoEnglishListData {
  /** Овог (англи) */
  readonly forename: string | null;
  /** Эцгийн нэр (англи) */
  readonly surname: string | null;
  /** Өөрийн нэр (англи) */
  readonly givenName: string | null;
  /** Хуучин регистрийн дугаар */
  readonly regnumOld: string | null;
  /** Регистр өөрчилсөн шалтгаан (нэр) */
  readonly changeRegnumCauseName: string | null;
  /** Регистр өөрчилсөн шалтгаан (код) */
  readonly changeRegnumCauseCode: string | null;
  /** Шинэ регистрийн дугаар */
  readonly regnumNew: string | null;
  /** Иргэншил (нэр) */
  readonly nationalityName: string | null;
  /** Бүртгэлийн дугаар */
  readonly civilId: number | null;
  /** Хүйс */
  readonly sexName: string | null;
}

/** WS102102_RegnumChangeInfoEnglish: Регистрийн дугаар өөрчилсний лавлагаа- Англи */
export interface RegnumChangeInfoEnglishResponse {
  /** Жагсаалт */
  readonly listData: readonly RegnumChangeInfoEnglishListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { forename: "string", surname: "string", givenName: "string", regnumOld: "string", changeRegnumCauseName: "string", changeRegnumCauseCode: "string", regnumNew: "string", nationalityName: "string", civilId: "int", sexName: "string" } } } };

/**
 * WS102102_RegnumChangeInfoEnglish: Регистрийн дугаар өөрчилсний лавлагаа- Англи
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function regnumChangeInfoEnglish(transport: Transport, params: RegnumChangeInfoEnglishParams = {}, options: CallOptions = {}): Promise<XypResult<RegnumChangeInfoEnglishResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<RegnumChangeInfoEnglishResponse>("RegnumChangeInfoEnglishResponse", SCHEMA, data);
}
