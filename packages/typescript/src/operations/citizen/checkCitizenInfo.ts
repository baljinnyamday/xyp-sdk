/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100107_checkCitizenInfo (citizen, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100107_checkCitizenInfo";

/** Input of WS100107_checkCitizenInfo. */
export interface CheckCitizenInfoParams {
  /** нэр */
  readonly firstName?: string;
  /** овог */
  readonly lastName?: string;
  /** регистр */
  readonly regnum?: string;
}

/** WS100107_checkCitizenInfo: Иргэний мэдээлэл шалгах */
export interface CheckCitizenInfoResponse {
  readonly isMatched: boolean | null;
}

const SCHEMA: Schema = { isMatched: "bool" };

/**
 * WS100107_checkCitizenInfo: Иргэний мэдээлэл шалгах
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function checkCitizenInfo(transport: Transport, params: CheckCitizenInfoParams = {}, options: CallOptions = {}): Promise<XypResult<CheckCitizenInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      firstName: params.firstName,
      lastName: params.lastName,
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<CheckCitizenInfoResponse>("CheckCitizenInfoResponse", SCHEMA, data);
}
