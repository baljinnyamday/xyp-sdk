/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100138_getCitizenRegnumInfo (citizen, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100138_getCitizenRegnumInfo";

/** Input of WS100138_getCitizenRegnumInfo. */
export interface GetCitizenRegnumInfoParams {
  /** Иргэний хувийн дугаар */
  readonly civilId?: string;
  /** регистрийн дугаар */
  readonly regnum?: string;
}

/** WS100138_getCitizenRegnumInfo: Регистрийн дугаарын тухай лавлагаа */
export interface GetCitizenRegnumInfoResponse {
  /** Иргэний регистрийн дугаар */
  readonly forename: string | null;
  /** Ургийн овог */
  readonly regnum: string | null;
  /** Эцэг эхийн нэр */
  readonly surname: string | null;
  /** Нэр */
  readonly givenname: string | null;
  /** Иргэншил */
  readonly citizenship: string | null;
  /** Бүртгэлийн дугаар */
  readonly registeredNum: string | null;
  /** Төрсөн огноо */
  readonly birthDate: Date | string | null;
  /** Хүйс */
  readonly sex: string | null;
}

const SCHEMA: Schema = { forename: "string", regnum: "string", surname: "string", givenname: "string", citizenship: "string", registeredNum: "string", birthDate: "date", sex: "string" };

/**
 * WS100138_getCitizenRegnumInfo: Регистрийн дугаарын тухай лавлагаа
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function getCitizenRegnumInfo(transport: Transport, params: GetCitizenRegnumInfoParams = {}, options: CallOptions = {}): Promise<XypResult<GetCitizenRegnumInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      civilId: params.civilId,
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<GetCitizenRegnumInfoResponse>("GetCitizenRegnumInfoResponse", SCHEMA, data);
}
