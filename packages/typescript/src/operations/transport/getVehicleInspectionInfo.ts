/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100409_getVehicleInspectionInfo (transport, Авто тээврийн үндэсний төв)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100409_getVehicleInspectionInfo";

/** Input of WS100409_getVehicleInspectionInfo. */
export interface GetVehicleInspectionInfoParams {
  /** Тээврийн хэрэгслийн арлын дугаар */
  readonly cabinNumber?: string;
}

/** WS100409_getVehicleInspectionInfo: Тээврийн хэрэгслийн оншилгооний мэдээлэл шалгах сервис */
export interface GetVehicleInspectionInfoResponse {
  /** Тээврийн хэрэгслийн дугаар */
  readonly vehicleNumber: string | null;
  /** Арлын дугаар */
  readonly cabinNumber: string | null;
  /** Үзлэгт орсон огноо */
  readonly checkDate: Date | string | null;
  /** Үзлэгт дахин орох огноо */
  readonly expireDate: Date | string | null;
  /** Тэнцсэн эсэх */
  readonly isPassed: boolean | null;
}

const SCHEMA: Schema = { vehicleNumber: "string", cabinNumber: "string", checkDate: "date", expireDate: "date", isPassed: "bool" };

/**
 * WS100409_getVehicleInspectionInfo: Тээврийн хэрэгслийн оншилгооний мэдээлэл шалгах сервис
 *
 * Provider: Авто тээврийн үндэсний төв
 */
export async function getVehicleInspectionInfo(transport: Transport, params: GetVehicleInspectionInfoParams = {}, options: CallOptions = {}): Promise<XypResult<GetVehicleInspectionInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      cabinNumber: params.cabinNumber,
    },
    options,
  );
  return decodeResponse<GetVehicleInspectionInfoResponse>("GetVehicleInspectionInfoResponse", SCHEMA, data);
}
