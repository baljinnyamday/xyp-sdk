/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100437_vehicleLegalEntityTotal (transport, Зам, тээврийн хөгжлийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100437_vehicleLegalEntityTotal";

/** Input of WS100437_vehicleLegalEntityTotal. */
export interface VehicleLegalEntityTotalParams {
  /** регистрийн дугаар */
  readonly orgRegnum?: string;
}

/** WS100437_vehicleLegalEntityTotal: хуулийн этгээдийн нийт тээврийн хэрэгслийн мэдээлэл дамжуулах сервис */
export interface VehicleLegalEntityTotalResponse {
  /** Нийт машин */
  readonly totalVehicles: number | null;
}

const SCHEMA: Schema = { totalVehicles: "int" };

/**
 * WS100437_vehicleLegalEntityTotal: хуулийн этгээдийн нийт тээврийн хэрэгслийн мэдээлэл дамжуулах сервис
 *
 * Provider: Зам, тээврийн хөгжлийн яам
 */
export async function vehicleLegalEntityTotal(transport: Transport, params: VehicleLegalEntityTotalParams = {}, options: CallOptions = {}): Promise<XypResult<VehicleLegalEntityTotalResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      orgRegnum: params.orgRegnum,
    },
    options,
  );
  return decodeResponse<VehicleLegalEntityTotalResponse>("VehicleLegalEntityTotalResponse", SCHEMA, data);
}
