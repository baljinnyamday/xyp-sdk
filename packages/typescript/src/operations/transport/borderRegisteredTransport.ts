/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100477_BorderRegisteredTransport (transport, Гаалийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100477_BorderRegisteredTransport";

/** Input of WS100477_BorderRegisteredTransport. */
export interface BorderRegisteredTransportParams {
  /** Тээврийн хэрэгслийн улсын дугаар */
  readonly plateNumber?: string;
  /** Харьяалах улс */
  readonly countryCode?: string;
  /** Хил нэвтэрсэн боомт */
  readonly createdOrgName?: string;
  /** Арлын дугаар */
  readonly cabinNumber?: string;
  /** Эхлэх огноо */
  readonly startDate?: string;
  /** Дуусах огноо */
  readonly endDate?: string;
  /** Сервисийг ашиглах байгууллагад өгөгдөх */
  readonly orgCode?: string;
}

/** Мэдээллийн жагсаалт */
export interface BorderRegisteredTransportListData {
  /** Харьялах улс */
  readonly countryName: string | null;
  /** Хил нэвтрэсэн чиглэл */
  readonly passDirection: string | null;
  /** Аялалын дугаар */
  readonly tripNumber: string | null;
  /** Аялалын чиглэл */
  readonly tripRoute: string | null;
  /** Тээврийн төрөл */
  readonly transport: string | null;
  /** Тээврийн хэрэгслийн төрөл */
  readonly transportType: string | null;
  /** Хил нэвтрэсэн огноо */
  readonly passDate: string | null;
  /** Бүртгэсэн цаг */
  readonly passTime: string | null;
  /** Хил нэвтэрсэн боомт */
  readonly passPort: string | null;
  /** Тээврийн хэрэгслийн марк */
  readonly transportMark: string | null;
  /** Тээврийн хэрэгслийн модель */
  readonly transportModel: string | null;
  /** Тээврийн хэрэгслийн арлын дугаар */
  readonly transportCabinNumber: string | null;
  /** Тээврийн хэрэгслийн өнгө */
  readonly transportColor: string | null;
  /** чиргүүлтэй эсэх */
  readonly hasTrailer: string | null;
  /** чиргүүлийн дугаар */
  readonly trailerNumber: string | null;
  /** чиргүүлийн марк */
  readonly trailerMark: string | null;
  /** чиргүүлийн өнгө */
  readonly trailerColor: string | null;
}

/** WS100477_BorderRegisteredTransport: Бүртгэгдсэн тээврийн хэрэгслийн мэдээлэл */
export interface BorderRegisteredTransportResponse {
  /** Мэдээллийн жагсаалт */
  readonly listData: readonly BorderRegisteredTransportListData[] | null;
}

const SCHEMA: Schema = {
  listData: {
    list: {
      object: {
        countryName: "string",
        passDirection: "string",
        tripNumber: "string",
        tripRoute: "string",
        transport: "string",
        transportType: "string",
        passDate: "string",
        passTime: "string",
        passPort: "string",
        transportMark: "string",
        transportModel: "string",
        transportCabinNumber: "string",
        transportColor: "string",
        hasTrailer: "string",
        trailerNumber: "string",
        trailerMark: "string",
        trailerColor: "string",
      },
    },
  },
};

/**
 * WS100477_BorderRegisteredTransport: Бүртгэгдсэн тээврийн хэрэгслийн мэдээлэл
 *
 * Provider: Гаалийн ерөнхий газар
 */
export async function borderRegisteredTransport(transport: Transport, params: BorderRegisteredTransportParams = {}, options: CallOptions = {}): Promise<XypResult<BorderRegisteredTransportResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      plateNumber: params.plateNumber,
      countryCode: params.countryCode,
      createdOrgName: params.createdOrgName,
      cabinNumber: params.cabinNumber,
      startDate: params.startDate,
      endDate: params.endDate,
      orgCode: params.orgCode,
    },
    options,
  );
  return decodeResponse<BorderRegisteredTransportResponse>("BorderRegisteredTransportResponse", SCHEMA, data);
}
