/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100444_orderDriverLicense (transport, Зам, тээврийн хөгжлийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100444_orderDriverLicense";

/** Input of WS100444_orderDriverLicense. */
export interface OrderDriverLicenseParams {
  /** Хүсэлтийн дугаар */
  readonly requestId?: string;
  /** Хүсэлтийн төрөл */
  readonly requestType?: string;
  /** регистрийн дугаар */
  readonly regnum?: string;
  /** иргэний дугаар */
  readonly civilId?: string;
  /** Жолоочийн үнэмлэхний дугаар */
  readonly licenseNumber?: string;
  /** Нөхөн олголтын үндэслэл (3800 1-Гээгдүүлсэн, 2-Хүчинтэй байдал алдагдуулсан, 3-Хугацаа сунгах, 4-Мэдээлэл зөрүүтэй) */
  readonly reasonId?: number;
  /** Нөхөн олголтын шалтгаан */
  readonly reason?: string;
  /** Холбоо барих - Нэр */
  readonly contactFName?: string;
  /** Холбоо барих - Овог */
  readonly contactLName?: string;
  /** Холбоо барих - Утас 1 */
  readonly phoneNum1?: string;
  /** Холбоо барих - Утас 2 */
  readonly phoneNum2?: string;
  /** Хүргэлтийн төрөл */
  readonly deliveryType?: number;
  /** И-Мэйл */
  readonly email?: string;
  /** Хүсэлтийн огноо */
  readonly invoiceDate?: string;
  /** Гарын үсэгний зураг base64 */
  readonly signImage?: string;
  /** ОУЖҮ: Нүдний өнгө */
  readonly eyeColor?: string;
  /** ОУЖҮ: Үсний өнгө */
  readonly hairColor?: string;
  /** ОУЖҮ: Өндөр */
  readonly height?: number;
  /** ОУЖҮ: Жин */
  readonly weight?: number;
  /** ОУЖҮ: хэдэн жилээр захиалж байгаа */
  readonly licensePeriod?: number;
  /** hurgej ogoh medeelel */
  readonly deliveryAddress?: string;
}

/** WS100444_orderDriverLicense: Жолооны үнэмлэх солих, нөхөн олгох хүсэлт явуулах */
export interface OrderDriverLicenseResponse {
  readonly isSuccess: boolean | null;
}

const SCHEMA: Schema = { isSuccess: "bool" };

/**
 * WS100444_orderDriverLicense: Жолооны үнэмлэх солих, нөхөн олгох хүсэлт явуулах
 *
 * Provider: Зам, тээврийн хөгжлийн яам
 */
export async function orderDriverLicense(transport: Transport, params: OrderDriverLicenseParams = {}, options: CallOptions = {}): Promise<XypResult<OrderDriverLicenseResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      requestId: params.requestId,
      requestType: params.requestType,
      regnum: params.regnum,
      civilId: params.civilId,
      licenseNumber: params.licenseNumber,
      reasonId: params.reasonId,
      reason: params.reason,
      contactFName: params.contactFName,
      contactLName: params.contactLName,
      phoneNum1: params.phoneNum1,
      phoneNum2: params.phoneNum2,
      deliveryType: params.deliveryType,
      email: params.email,
      invoiceDate: params.invoiceDate,
      signImage: params.signImage,
      eyeColor: params.eyeColor,
      hairColor: params.hairColor,
      height: params.height,
      weight: params.weight,
      licensePeriod: params.licensePeriod,
      deliveryAddress: params.deliveryAddress,
    },
    options,
  );
  return decodeResponse<OrderDriverLicenseResponse>("OrderDriverLicenseResponse", SCHEMA, data);
}
