/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100480_VehicleAndDriverBorderReport (transport, Авто тээврийн үндэсний төв)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100480_VehicleAndDriverBorderReport";

/** Input of WS100480_VehicleAndDriverBorderReport. */
export interface VehicleAndDriverBorderReportParams {
  readonly key?: string;
  readonly document?: unknown;
}

/** WS100480_VehicleAndDriverBorderReport: Хилээр орж гарсан машин болон жолоочийн мэдээллийг шалгах сервис */
export interface VehicleAndDriverBorderReportResponse {
  /** Хариу */
  readonly result: string | null;
}

const SCHEMA: Schema = { result: "string" };

/**
 * WS100480_VehicleAndDriverBorderReport: Хилээр орж гарсан машин болон жолоочийн мэдээллийг шалгах сервис
 *
 * Provider: Авто тээврийн үндэсний төв
 */
export async function vehicleAndDriverBorderReport(transport: Transport, params: VehicleAndDriverBorderReportParams = {}, options: CallOptions = {}): Promise<XypResult<VehicleAndDriverBorderReportResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      key: params.key,
      document: params.document,
    },
    options,
  );
  return decodeResponse<VehicleAndDriverBorderReportResponse>("VehicleAndDriverBorderReportResponse", SCHEMA, data);
}
