/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100468_FreightTransportTrainDocumentDetail (transport, Улаанбаатар төмөр зам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100468_FreightTransportTrainDocumentDetail";

/** Input of WS100468_FreightTransportTrainDocumentDetail. */
export interface FreightTransportTrainDocumentDetailParams {
  /** Баримт бичгийн дугаар */
  readonly billNumber?: string;
}

/** Ачааны мэдээлэл */
export interface FreightTransportTrainDocumentDetailFreights {
  /** Ачааны ГНГ */
  readonly gng: string | null;
  /** Ачааны мэдээлэл */
  readonly name: string | null;
  /** Ачааны жин (кг) */
  readonly weight: string | null;
}

/** Вагоны мэдээлэл */
export interface FreightTransportTrainDocumentDetailWagons {
  /** Вагон дугаар */
  readonly number: string | null;
  /** Вагон төрөл */
  readonly type: string | null;
  /** Вагон эзэмшигч */
  readonly owner: string | null;
}

/** WS100468_FreightTransportTrainDocumentDetail: Галт тэрэгний ачаа тээврийн бичиг баримтын дэлгэрэнгүй мэдээлэл авах сервис */
export interface FreightTransportTrainDocumentDetailResponse {
  /** Тээврийн бичиг баримтын ерөнхий мэдээлэл */
  readonly bill: unknown | null;
  /** Зуучийн мэдээлэл */
  readonly freightForwarder: unknown | null;
  /** Илгээгчийн мэдээлэл */
  readonly sender: unknown | null;
  /** Хүлээн авагчийн мэдээлэл */
  readonly receiver: unknown | null;
  /** Өртөөний мэдээлэл */
  readonly stations: unknown | null;
  /** Төлбөрийн мэдээлэл */
  readonly payment: unknown | null;
  /** Ачааны мэдээлэл */
  readonly freights: readonly FreightTransportTrainDocumentDetailFreights[] | null;
  /** Вагоны мэдээлэл */
  readonly wagons: readonly FreightTransportTrainDocumentDetailWagons[] | null;
  /** Гэрээний файлууд */
  readonly files: readonly string[] | null;
}

const SCHEMA: Schema = {
  bill: "any",
  freightForwarder: "any",
  sender: "any",
  receiver: "any",
  stations: "any",
  payment: "any",
  freights: { list: { object: { gng: "string", name: "string", weight: "string" } } },
  wagons: { list: { object: { number: "string", type: "string", owner: "string" } } },
  files: { list: "string" },
};

/**
 * WS100468_FreightTransportTrainDocumentDetail: Галт тэрэгний ачаа тээврийн бичиг баримтын дэлгэрэнгүй мэдээлэл авах сервис
 *
 * Provider: Улаанбаатар төмөр зам
 */
export async function freightTransportTrainDocumentDetail(transport: Transport, params: FreightTransportTrainDocumentDetailParams = {}, options: CallOptions = {}): Promise<XypResult<FreightTransportTrainDocumentDetailResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      billNumber: params.billNumber,
    },
    options,
  );
  return decodeResponse<FreightTransportTrainDocumentDetailResponse>("FreightTransportTrainDocumentDetailResponse", SCHEMA, data);
}
