/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100438_getPreOwnedOrgVehicleTotal (transport, Зам, тээврийн хөгжлийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100438_getPreOwnedOrgVehicleTotal";

/** Input of WS100438_getPreOwnedOrgVehicleTotal. */
export interface GetPreOwnedOrgVehicleTotalParams {
  /** регистрийн дугаар */
  readonly orgRegnum?: string;
}

/** WS100438_getPreOwnedOrgVehicleTotal: хуулийн этгээдийн өмнө нь өмчлөж байсан нийт тээврийн хэрэгслийн мэдээлэл */
export interface GetPreOwnedOrgVehicleTotalResponse {
  /** Нийт машин */
  readonly totalVehicles: number | null;
}

const SCHEMA: Schema = { totalVehicles: "int" };

/**
 * WS100438_getPreOwnedOrgVehicleTotal: хуулийн этгээдийн өмнө нь өмчлөж байсан нийт тээврийн хэрэгслийн мэдээлэл
 *
 * Provider: Зам, тээврийн хөгжлийн яам
 */
export async function getPreOwnedOrgVehicleTotal(transport: Transport, params: GetPreOwnedOrgVehicleTotalParams = {}, options: CallOptions = {}): Promise<XypResult<GetPreOwnedOrgVehicleTotalResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      orgRegnum: params.orgRegnum,
    },
    options,
  );
  return decodeResponse<GetPreOwnedOrgVehicleTotalResponse>("GetPreOwnedOrgVehicleTotalResponse", SCHEMA, data);
}
