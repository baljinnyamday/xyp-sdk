/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS101401_VehicleTaxInfoService (transport, Татварын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS101401_VehicleTaxInfoService";

/** Input of WS101401_VehicleTaxInfoService. */
export interface VehicleTaxInfoServiceParams {
  /** Тээврийн хэрэглийн улсын дугаарыг оруулах */
  readonly plateNo?: string;
}

/** Жагсаалт */
export interface VehicleTaxInfoServiceListData {
  /** Татварын тайлант он */
  readonly year: number | null;
  /** Тээврийн хэрэгслийн дугаар */
  readonly plateNo: string | null;
  /** Тээврийн хэрэгслийн арлын дугаарыг оруулах */
  readonly cabinNo: string | null;
  /** Татварын нэхэмжлэхийн дугаар */
  readonly invoiceNo: string | null;
  /** Татварын төрөл нэр (Автын агаарын бохирдлын төлбөр гэх мэт) */
  readonly taxTypeName: string | null;
  /** Ногдуулсан дүн буюу татварын нэхэмжлэхийн дагуу төлөх дүн */
  readonly payAmount: number | null;
  /** Татварын нэхэмлэхийн дагуу Төлсөн дүн */
  readonly paidAmount: number | null;
  /** Татвар төлөгдсөн эсэх төлөв (0-төлөөгүй, 1-төлөгдсөн) */
  readonly isPay: number | null;
}

/** WS101401_VehicleTaxInfoService: Тээврийн хэрэгслийн улсын дугаараар татвар төлсөн эсэх мэдээллийг авах сервис */
export interface VehicleTaxInfoServiceResponse {
  /** Жагсаалт */
  readonly listData: readonly VehicleTaxInfoServiceListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { year: "int", plateNo: "string", cabinNo: "string", invoiceNo: "string", taxTypeName: "string", payAmount: "int", paidAmount: "int", isPay: "int" } } } };

/**
 * WS101401_VehicleTaxInfoService: Тээврийн хэрэгслийн улсын дугаараар татвар төлсөн эсэх мэдээллийг авах сервис
 *
 * Provider: Татварын ерөнхий газар
 */
export async function vehicleTaxInfoService(transport: Transport, params: VehicleTaxInfoServiceParams = {}, options: CallOptions = {}): Promise<XypResult<VehicleTaxInfoServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      plateNo: params.plateNo,
    },
    options,
  );
  return decodeResponse<VehicleTaxInfoServiceResponse>("VehicleTaxInfoServiceResponse", SCHEMA, data);
}
