/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100495_ECLicenseCreator (transport, Нийтийн тээврийн бодлогын газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100495_ECLicenseCreator";

/** Input of WS100495_ECLicenseCreator. */
export interface ECLicenseCreatorParams {
  readonly maxLoad?: number;
  readonly regnum?: string;
  readonly plateNo?: string;
  readonly lastName?: string;
  /** Системийн баталгаажуулалтын токен */
  readonly appToken?: string;
  readonly userType?: string;
  readonly firstName?: string;
}

/** WS100495_ECLicenseCreator: EC лиценз үүсгэх сервис */
export interface ECLicenseCreatorResponse {
  /** pdf */
  readonly pdf: string | null;
  /** licenceId */
  readonly licenceId: number | null;
  /** Хариу мессеж */
  readonly message: string | null;
  /** Улирлын нэр */
  readonly season: string | null;
}

const SCHEMA: Schema = { pdf: "string", licenceId: "int", message: "string", season: "string" };

/**
 * WS100495_ECLicenseCreator: EC лиценз үүсгэх сервис
 *
 * Provider: Нийтийн тээврийн бодлогын газар
 */
export async function eCLicenseCreator(transport: Transport, params: ECLicenseCreatorParams = {}, options: CallOptions = {}): Promise<XypResult<ECLicenseCreatorResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      maxLoad: params.maxLoad,
      regnum: params.regnum,
      plateNo: params.plateNo,
      lastName: params.lastName,
      appToken: params.appToken,
      userType: params.userType,
      firstName: params.firstName,
    },
    options,
  );
  return decodeResponse<ECLicenseCreatorResponse>("ECLicenseCreatorResponse", SCHEMA, data);
}
