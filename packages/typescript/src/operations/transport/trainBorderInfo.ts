/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100471_TrainBorderInfo (transport, Хил хамгаалах ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100471_TrainBorderInfo";

/** Input of WS100471_TrainBorderInfo. */
export interface TrainBorderInfoParams {
  /** Явсан улс */
  readonly toCountryCode?: string;
  /** 1: Галт тэрэгний хил нэвтрэлт, 2: Галт тэрэгний орсон хил нэвтрэлт, 3: Галт тэрэгний гарсан хил нэвтрэлт, 4: Галт тэрэгний сүүлийн хил нэвтрэлт */
  readonly type?: number;
  readonly orgToken?: string;
  /** Аяллын дугаар */
  readonly travelNumber?: string;
  /** Ирсэн улс */
  readonly fromCountryCode?: string;
}

/** жагсаалт */
export interface TrainBorderInfoListData {
  /** Аяллын дугаар */
  readonly travel_no: string | null;
  /** Галт тэрэгний төрөл */
  readonly vehicle_type_name: string | null;
  /** Харьяалах улс */
  readonly owner_country_name: string | null;
  readonly owner_country_code: string | null;
  /** Хөдөлгөөний чиглэл */
  readonly pass_type: string | null;
  /** Ирсэн улс */
  readonly from_country_name: string | null;
  readonly from_country_code: string | null;
  /** Явсан улс */
  readonly to_country_name: string | null;
  readonly to_country_code: string | null;
  /** Хил нэвтэрсэн боомт */
  readonly created_org_name: string | null;
  /** Хил нэвтэрсэн огноо */
  readonly created_date: string | null;
}

/** WS100471_TrainBorderInfo: Галт тэрэгний хил нэвтрэлт */
export interface TrainBorderInfoResponse {
  /** жагсаалт */
  readonly listData: readonly TrainBorderInfoListData[] | null;
}

const SCHEMA: Schema = {
  listData: {
    list: { object: { travel_no: "string", vehicle_type_name: "string", owner_country_name: "string", owner_country_code: "string", pass_type: "string", from_country_name: "string", from_country_code: "string", to_country_name: "string", to_country_code: "string", created_org_name: "string", created_date: "string" } },
  },
};

/**
 * WS100471_TrainBorderInfo: Галт тэрэгний хил нэвтрэлт
 *
 * Provider: Хил хамгаалах ерөнхий газар
 */
export async function trainBorderInfo(transport: Transport, params: TrainBorderInfoParams = {}, options: CallOptions = {}): Promise<XypResult<TrainBorderInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      toCountryCode: params.toCountryCode,
      type: params.type,
      orgToken: params.orgToken,
      travelNumber: params.travelNumber,
      fromCountryCode: params.fromCountryCode,
    },
    options,
  );
  return decodeResponse<TrainBorderInfoResponse>("TrainBorderInfoResponse", SCHEMA, data);
}
