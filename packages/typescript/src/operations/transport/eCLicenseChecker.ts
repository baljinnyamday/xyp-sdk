/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100496_ECLicenseChecker (transport, Нийтийн тээврийн бодлогын газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100496_ECLicenseChecker";

/** Input of WS100496_ECLicenseChecker. */
export interface ECLicenseCheckerParams {
  /** Тээврийн хэрэгслийн улсын дугаар */
  readonly plateNo?: string;
  /** Системийн баталгаажуулалтын токен */
  readonly appToken?: string;
}

/** WS100496_ECLicenseChecker: EC лиценз шалгах сервис */
export interface ECLicenseCheckerResponse {
  /** EC лиценз дугаар */
  readonly licenceId: number | null;
  /** EC лицензтэй эсэх */
  readonly isEcLicence: boolean | null;
  /** Алдааны мессеж */
  readonly message: string | null;
}

const SCHEMA: Schema = { licenceId: "int", isEcLicence: "bool", message: "string" };

/**
 * WS100496_ECLicenseChecker: EC лиценз шалгах сервис
 *
 * Provider: Нийтийн тээврийн бодлогын газар
 */
export async function eCLicenseChecker(transport: Transport, params: ECLicenseCheckerParams = {}, options: CallOptions = {}): Promise<XypResult<ECLicenseCheckerResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      plateNo: params.plateNo,
      appToken: params.appToken,
    },
    options,
  );
  return decodeResponse<ECLicenseCheckerResponse>("ECLicenseCheckerResponse", SCHEMA, data);
}
