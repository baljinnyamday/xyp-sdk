/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100421_getPreOwnedVehicle (transport, Зам, тээврийн хөгжлийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100421_getPreOwnedVehicle";

/** Input of WS100421_getPreOwnedVehicle. */
export interface GetPreOwnedVehicleParams {
  /** улсын дугаар */
  readonly plateNumber?: string;
  /** Арлын дугаар */
  readonly cabinNumber?: string;
  /** регистрийн дугаар */
  readonly regnum?: string;
  /** гэрчилгээний дугаар */
  readonly certificatNumber?: string;
}

export interface GetPreOwnedVehicleListData {
  /** улсын дугаар */
  readonly plateNumber: string | null;
  /** марк */
  readonly markName: string | null;
  /** модел */
  readonly modelName: string | null;
  /** арлын дугаар */
  readonly cabinNumber: string | null;
  /** өнгө */
  readonly colorName: string | null;
  /** Эхэлсэн огноо */
  readonly startDate: string | null;
  /** Дууссан огноо */
  readonly endDate: string | null;
  /** эзэмшигчийн id */
  readonly ownerId: string | null;
  /** нэр */
  readonly firstName: string | null;
  /** овог */
  readonly lastName: string | null;
  /** регистрын дугаар */
  readonly regnum: string | null;
  /** үйлдвэрлэсэн огноо */
  readonly buildDate: string | null;
}

/** WS100421_getPreOwnedVehicle: Иргэний өмнө нь өмчлөж байсан тээврийн хэрэгслийн мэдээлэл */
export interface GetPreOwnedVehicleResponse {
  readonly listData: readonly GetPreOwnedVehicleListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { plateNumber: "string", markName: "string", modelName: "string", cabinNumber: "string", colorName: "string", startDate: "string", endDate: "string", ownerId: "string", firstName: "string", lastName: "string", regnum: "string", buildDate: "string" } } } };

/**
 * WS100421_getPreOwnedVehicle: Иргэний өмнө нь өмчлөж байсан тээврийн хэрэгслийн мэдээлэл
 *
 * Provider: Зам, тээврийн хөгжлийн яам
 */
export async function getPreOwnedVehicle(transport: Transport, params: GetPreOwnedVehicleParams = {}, options: CallOptions = {}): Promise<XypResult<GetPreOwnedVehicleResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      plateNumber: params.plateNumber,
      cabinNumber: params.cabinNumber,
      regnum: params.regnum,
      certificatNumber: params.certificatNumber,
    },
    options,
  );
  return decodeResponse<GetPreOwnedVehicleResponse>("GetPreOwnedVehicleResponse", SCHEMA, data);
}
