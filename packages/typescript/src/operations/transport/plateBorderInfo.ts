/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100472_PlateBorderInfo (transport, Хил хамгаалах ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100472_PlateBorderInfo";

/** Input of WS100472_PlateBorderInfo. */
export interface PlateBorderInfoParams {
  /** Аяллын дугаар */
  readonly travelNumber?: string;
  /** Ирсэн улс */
  readonly fromCountryCode?: string;
  /** Явсан улс */
  readonly toCountryCode?: string;
  /** Сервисийг ашиглах байгууллагад өгөгдөх токен */
  readonly orgToken?: string;
  /** 1.Онгоцны хил нэвтрэлт 2.Онгоцны орсон хил нэвтрэлт 3.Онгоцны гарсан хил нэвтрэлт 4.Онгоцны сүүлийн хил нэвтрэлт */
  readonly type?: number;
}

/** жагсаалт */
export interface PlateBorderInfoList {
  /** Аяллын дугаар */
  readonly travelNumber: string | null;
  /** Онгоцны төрөл /суудлын, ачааны.../ */
  readonly vehicleTypeName: string | null;
  /** Харьяалах улс */
  readonly ownerCountryName: string | null;
  /** Ирсэн/Явсан улс */
  readonly fromCountryName: string | null;
  /** Хөдөлгөөний чиглэл /Сүүлийн/ */
  readonly passType: string | null;
  /** Хил нэвтэрсэн боомт */
  readonly createdOrgName: string | null;
  /** Хил нэвтэрсэн огноо */
  readonly createdDate: string | null;
}

/** WS100472_PlateBorderInfo: Онгоцны хил нэвтрэлт */
export interface PlateBorderInfoResponse {
  /** жагсаалт */
  readonly list: readonly PlateBorderInfoList[] | null;
}

const SCHEMA: Schema = { list: { list: { object: { travelNumber: "string", vehicleTypeName: "string", ownerCountryName: "string", fromCountryName: "string", passType: "string", createdOrgName: "string", createdDate: "string" } } } };

/**
 * WS100472_PlateBorderInfo: Онгоцны хил нэвтрэлт
 *
 * Provider: Хил хамгаалах ерөнхий газар
 */
export async function plateBorderInfo(transport: Transport, params: PlateBorderInfoParams = {}, options: CallOptions = {}): Promise<XypResult<PlateBorderInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      travelNumber: params.travelNumber,
      fromCountryCode: params.fromCountryCode,
      toCountryCode: params.toCountryCode,
      orgToken: params.orgToken,
      type: params.type,
    },
    options,
  );
  return decodeResponse<PlateBorderInfoResponse>("PlateBorderInfoResponse", SCHEMA, data);
}
