/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100445_driverLicenseOrderedLog (transport, Зам, тээврийн хөгжлийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100445_driverLicenseOrderedLog";

/** Input of WS100445_driverLicenseOrderedLog. */
export type DriverLicenseOrderedLogParams = Readonly<Record<string, never>>;

export interface DriverLicenseOrderedLogListData {
  /** Хүсэлтийн дугаар */
  readonly requestId: string | null;
  /** Хүсэлтийн төрөл */
  readonly requestType: string | null;
  /** Төлөв */
  readonly status: number | null;
  readonly statusName: string | null;
  /** Тайлбар */
  readonly description: string | null;
  /** Огноо */
  readonly createdDate: string | null;
}

/** WS100445_driverLicenseOrderedLog: Жолооны үнэмлэх солих, нөхөн олгох хүсэлтын өөрчлөлт */
export interface DriverLicenseOrderedLogResponse {
  readonly listData: readonly DriverLicenseOrderedLogListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { requestId: "string", requestType: "string", status: "int", statusName: "string", description: "string", createdDate: "string" } } } };

/**
 * WS100445_driverLicenseOrderedLog: Жолооны үнэмлэх солих, нөхөн олгох хүсэлтын өөрчлөлт
 *
 * Provider: Зам, тээврийн хөгжлийн яам
 */
export async function driverLicenseOrderedLog(transport: Transport, _params: DriverLicenseOrderedLogParams = {}, options: CallOptions = {}): Promise<XypResult<DriverLicenseOrderedLogResponse>> {
  const data = await transport.call(OPERATION, {}, options);
  return decodeResponse<DriverLicenseOrderedLogResponse>("DriverLicenseOrderedLogResponse", SCHEMA, data);
}
