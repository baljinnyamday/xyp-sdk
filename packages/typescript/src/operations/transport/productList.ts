/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100499_ProductList (transport, Нийтийн тээврийн бодлогын газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100499_ProductList";

/** Input of WS100499_ProductList. */
export interface ProductListParams {
  /** нэр */
  readonly name?: string;
}

/** жагсаалт */
export interface ProductListListData {
  /** дугаар */
  readonly id: number | null;
  /** нэр */
  readonly name: string | null;
}

/** WS100499_ProductList: Бүтээгдэхүүний жагсаалт авах сервис */
export interface ProductListResponse {
  /** жагсаалт */
  readonly listData: readonly ProductListListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { id: "int", name: "string" } } } };

/**
 * WS100499_ProductList: Бүтээгдэхүүний жагсаалт авах сервис
 *
 * Provider: Нийтийн тээврийн бодлогын газар
 */
export async function productList(transport: Transport, params: ProductListParams = {}, options: CallOptions = {}): Promise<XypResult<ProductListResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      name: params.name,
    },
    options,
  );
  return decodeResponse<ProductListResponse>("ProductListResponse", SCHEMA, data);
}
