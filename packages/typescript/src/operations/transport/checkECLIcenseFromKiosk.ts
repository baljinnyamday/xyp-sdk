/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100434_checkECLIcenseFromKiosk (transport, Үндэсний дата төв)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100434_checkECLIcenseFromKiosk";

/** Input of WS100434_checkECLIcenseFromKiosk. */
export interface CheckECLIcenseFromKioskParams {
  /** Тухайн үйлчилгээг авсан огноо (Утга оруулаагү */
  readonly serviceDate?: string;
  /** Шалгах машины дугаар */
  readonly carNumber?: string;
}

/** WS100434_checkECLIcenseFromKiosk: 6 сар тутамд олгодог нийслэлийн нутаг дэвсгэрт ачаа тээвэрлэх лавлагааг ТҮЦ машинаас авсан эсэхийг шалгах сервис */
export interface CheckECLIcenseFromKioskResponse {
  /** Дэс дугаар */
  readonly id: number | null;
  /** Лавлагаа авсан ТҮЦ машины К дугаар */
  readonly kioskName: string | null;
  /** Үйлчилгээний төлбөр */
  readonly serviceFee: number | null;
  /** Үйлчилгээ авсан огноо */
  readonly serviceDate: Date | string | null;
}

const SCHEMA: Schema = { id: "int", kioskName: "string", serviceFee: "int", serviceDate: "date" };

/**
 * WS100434_checkECLIcenseFromKiosk: 6 сар тутамд олгодог нийслэлийн нутаг дэвсгэрт ачаа тээвэрлэх лавлагааг ТҮЦ машинаас авсан эсэхийг шалгах сервис
 *
 * Provider: Үндэсний дата төв
 */
export async function checkECLIcenseFromKiosk(transport: Transport, params: CheckECLIcenseFromKioskParams = {}, options: CallOptions = {}): Promise<XypResult<CheckECLIcenseFromKioskResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      serviceDate: params.serviceDate,
      carNumber: params.carNumber,
    },
    options,
  );
  return decodeResponse<CheckECLIcenseFromKioskResponse>("CheckECLIcenseFromKioskResponse", SCHEMA, data);
}
