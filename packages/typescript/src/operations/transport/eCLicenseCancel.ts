/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100498_ECLicenseCancel (transport, Нийтийн тээврийн бодлогын газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100498_ECLicenseCancel";

/** Input of WS100498_ECLicenseCancel. */
export interface ECLicenseCancelParams {
  /** Системийн баталгаажуулалтын токен */
  readonly appToken?: string;
  /** Цуцлах лицензийн ID */
  readonly licenceId?: string;
}

/** WS100498_ECLicenseCancel: EC лиценз цуцлах сервис */
export interface ECLicenseCancelResponse {
  /** Хариу мессеж */
  readonly message: string | null;
}

const SCHEMA: Schema = { message: "string" };

/**
 * WS100498_ECLicenseCancel: EC лиценз цуцлах сервис
 *
 * Provider: Нийтийн тээврийн бодлогын газар
 */
export async function eCLicenseCancel(transport: Transport, params: ECLicenseCancelParams = {}, options: CallOptions = {}): Promise<XypResult<ECLicenseCancelResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      appToken: params.appToken,
      licenceId: params.licenceId,
    },
    options,
  );
  return decodeResponse<ECLicenseCancelResponse>("ECLicenseCancelResponse", SCHEMA, data);
}
