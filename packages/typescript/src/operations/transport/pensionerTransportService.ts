/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS101402_PensionerTransportService (transport, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS101402_PensionerTransportService";

/** Input of WS101402_PensionerTransportService. */
export interface PensionerTransportServiceParams {
  /** Хуудасны индекс */
  readonly index?: number;
}

/** Жагсаалт */
export interface PensionerTransportServiceListData {
  /** Дотоод дугаар (ID) */
  readonly id: number | null;
  /** Регистрийн дугаар (иргэний бүртгэлийн дугаар) */
  readonly regnum: string | null;
  /** Овог */
  readonly surname: string | null;
  /** Өөрийн нэр */
  readonly givenName: string | null;
  /** Бүртгэлийн дугаар (байхгүй байж болно) */
  readonly civilId: number | null;
  /** Тэтгэврийн төрөл (ж: өндөр насны тэтгэвэр авагч) */
  readonly type: string | null;
  /** Дуусах огноо (хүчинтэй хугацаа) */
  readonly expiryDate: string | null;
  /** Нэг хуудсанд харуулах тоо */
  readonly perPage: number | null;
  /** Хуудасны индекс */
  readonly index: number | null;
  /** Нийт мөрийн тоо */
  readonly rowCount: number | null;
  /** Нийт хуудасны тоо */
  readonly pageCount: number | null;
}

/** WS101402_PensionerTransportService: Тэтгэвэр авагч-Автобусаар зорчих сервис */
export interface PensionerTransportServiceResponse {
  /** Жагсаалт */
  readonly listData: readonly PensionerTransportServiceListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { id: "int", regnum: "string", surname: "string", givenName: "string", civilId: "int", type: "string", expiryDate: "string", perPage: "int", index: "int", rowCount: "int", pageCount: "int" } } } };

/**
 * WS101402_PensionerTransportService: Тэтгэвэр авагч-Автобусаар зорчих сервис
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function pensionerTransportService(transport: Transport, params: PensionerTransportServiceParams = {}, options: CallOptions = {}): Promise<XypResult<PensionerTransportServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      index: params.index,
    },
    options,
  );
  return decodeResponse<PensionerTransportServiceResponse>("PensionerTransportServiceResponse", SCHEMA, data);
}
