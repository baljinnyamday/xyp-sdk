/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100441_umnsVehicleInfo (transport, Авто тээврийн үндэсний төв)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100441_umnsVehicleInfo";

/** Input of WS100441_umnsVehicleInfo. */
export interface UmnsVehicleInfoParams {
  /** регистийн дугаар */
  readonly regnum?: string;
}

/** мэдээллийн жагсаалт */
export interface UmnsVehicleInfoListData {
  /** үйлдвэрлэсэн он */
  readonly buildYear: number | null;
  /** төрлийн дугаар */
  readonly vehicleId: number | null;
  /** эзэмшигч эсэх */
  readonly isOwner: number | null;
  /** зориулалтын дугаар */
  readonly purposeId: number | null;
  /** зориулалт */
  readonly purposeName: string | null;
  /** хөдөлгүүрийн багтаамж */
  readonly engineCapacity: number | null;
  /** эзэмшигчийн регистр */
  readonly ownerRegnum: string | null;
}

/** WS100441_umnsVehicleInfo: ӨМНС тээврийн хэрэгслийн лавлагаа */
export interface UmnsVehicleInfoResponse {
  /** мэдээллийн жагсаалт */
  readonly listData: readonly UmnsVehicleInfoListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { buildYear: "int", vehicleId: "int", isOwner: "int", purposeId: "int", purposeName: "string", engineCapacity: "int", ownerRegnum: "string" } } } };

/**
 * WS100441_umnsVehicleInfo: ӨМНС тээврийн хэрэгслийн лавлагаа
 *
 * Provider: Авто тээврийн үндэсний төв
 */
export async function umnsVehicleInfo(transport: Transport, params: UmnsVehicleInfoParams = {}, options: CallOptions = {}): Promise<XypResult<UmnsVehicleInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<UmnsVehicleInfoResponse>("UmnsVehicleInfoResponse", SCHEMA, data);
}
