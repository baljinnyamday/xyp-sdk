/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100497_PoliceECLicenseChecker (transport, Нийтийн тээврийн бодлогын газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100497_PoliceECLicenseChecker";

/** Input of WS100497_PoliceECLicenseChecker. */
export interface PoliceECLicenseCheckerParams {
  /** Системийн баталгаажуулалтын токен */
  readonly appToken?: string;
  /** Тээврийн хэрэгслийн улсын дугаар */
  readonly plateNo?: string;
}

/** WS100497_PoliceECLicenseChecker: EC лиценз шалгах (Цагдаа) сервис */
export interface PoliceECLicenseCheckerResponse {
  /** дуусах огноо */
  readonly endDate: string | null;
  /** EC лицензтэй эсэх */
  readonly isLicence: boolean | null;
  /** эхэлсэн огноо */
  readonly startDate: string | null;
}

const SCHEMA: Schema = { endDate: "string", isLicence: "bool", startDate: "string" };

/**
 * WS100497_PoliceECLicenseChecker: EC лиценз шалгах (Цагдаа) сервис
 *
 * Provider: Нийтийн тээврийн бодлогын газар
 */
export async function policeECLicenseChecker(transport: Transport, params: PoliceECLicenseCheckerParams = {}, options: CallOptions = {}): Promise<XypResult<PoliceECLicenseCheckerResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      appToken: params.appToken,
      plateNo: params.plateNo,
    },
    options,
  );
  return decodeResponse<PoliceECLicenseCheckerResponse>("PoliceECLicenseCheckerResponse", SCHEMA, data);
}
