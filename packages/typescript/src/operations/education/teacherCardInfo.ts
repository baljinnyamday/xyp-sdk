/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS400108_teacherCardInfo (education, Боловсролын мэдээллийн технологийн Төв)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS400108_teacherCardInfo";

/** Input of WS400108_teacherCardInfo. */
export interface TeacherCardInfoParams {
  /** Регистрийн дугаар */
  readonly regnum?: string;
}

/** жагсаалт */
export interface TeacherCardInfoListData {
  /** Байгууллагын нэр */
  readonly orgName: string | null;
  /** Өмчийн хэлбэр */
  readonly orgProperty: string | null;
  /** Ажлын байрны нэр */
  readonly positionName: string | null;
}

/** WS400108_teacherCardInfo: Багш мөн эсэх шалгах */
export interface TeacherCardInfoResponse {
  /** жагсаалт */
  readonly listData: readonly TeacherCardInfoListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { orgName: "string", orgProperty: "string", positionName: "string" } } } };

/**
 * WS400108_teacherCardInfo: Багш мөн эсэх шалгах
 *
 * Provider: Боловсролын мэдээллийн технологийн Төв
 */
export async function teacherCardInfo(transport: Transport, params: TeacherCardInfoParams = {}, options: CallOptions = {}): Promise<XypResult<TeacherCardInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<TeacherCardInfoResponse>("TeacherCardInfoResponse", SCHEMA, data);
}
