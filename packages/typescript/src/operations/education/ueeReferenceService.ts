/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS400101_ueeReferenceService (education, Боловсрол шинжлэх ухааны яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS400101_ueeReferenceService";

/** Input of WS400101_ueeReferenceService. */
export interface UeeReferenceServiceParams {
  /** регистрийн дугаар */
  readonly regnum?: string;
}

/** WS400101_ueeReferenceService: БОЛОВСРОЛЫН ҮНЭЛГЭЭНИЙ ТӨВ ЭЛСЭЛТИЙН ЕРӨНХИЙ ШАЛГАЛТЫН ЛАВЛАГАА */
export interface UeeReferenceServiceResponse {
  /** лавлагааны дугаар */
  readonly referenceId: string | null;
  /** регистр */
  readonly regnum: string | null;
  /** овог */
  readonly lastname: string | null;
  /** нэр */
  readonly firstname: string | null;
}

const SCHEMA: Schema = { referenceId: "string", regnum: "string", lastname: "string", firstname: "string" };

/**
 * WS400101_ueeReferenceService: БОЛОВСРОЛЫН ҮНЭЛГЭЭНИЙ ТӨВ ЭЛСЭЛТИЙН ЕРӨНХИЙ ШАЛГАЛТЫН ЛАВЛАГАА
 *
 * Provider: Боловсрол шинжлэх ухааны яам
 */
export async function ueeReferenceService(transport: Transport, params: UeeReferenceServiceParams = {}, options: CallOptions = {}): Promise<XypResult<UeeReferenceServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<UeeReferenceServiceResponse>("UeeReferenceServiceResponse", SCHEMA, data);
}
