/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS400106_umnsEducationInfo (education, Боловсролын мэдээллийн технологийн Төв)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS400106_umnsEducationInfo";

/** Input of WS400106_umnsEducationInfo. */
export interface UmnsEducationInfoParams {
  /** Регистрийн дугаар */
  readonly regnum?: string;
}

/** WS400106_umnsEducationInfo: ӨМНС боловсролын мэдээлэл */
export interface UmnsEducationInfoResponse {
  /** ЕБС-д суралцдаг бол 1 үгүй бол 0 */
  readonly schoolStudent: number | null;
  /** ЕБС-ийн өмчийн хэлбэр Төрийн бол-1, Хувийн бол-2 */
  readonly schoolProperty: number | null;
  /** СӨБ-д суралцдаг бол 1 үгүй бол 0 */
  readonly kindergartenStudent: number | null;
  /** СӨБ-ийн өмчийн хэлбэр Төрийн бол-1, Хувийн бол-2 */
  readonly kindergartenProperty: number | null;
}

const SCHEMA: Schema = { schoolStudent: "int", schoolProperty: "int", kindergartenStudent: "int", kindergartenProperty: "int" };

/**
 * WS400106_umnsEducationInfo: ӨМНС боловсролын мэдээлэл
 *
 * Provider: Боловсролын мэдээллийн технологийн Төв
 */
export async function umnsEducationInfo(transport: Transport, params: UmnsEducationInfoParams = {}, options: CallOptions = {}): Promise<XypResult<UmnsEducationInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<UmnsEducationInfoResponse>("UmnsEducationInfoResponse", SCHEMA, data);
}
