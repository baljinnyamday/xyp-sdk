/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100932_getTotalTransactionMonthly (laborWelfare, Хөдөлмөр халамж, үйлчилгээний ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100932_getTotalTransactionMonthly";

/** Input of WS100932_getTotalTransactionMonthly. */
export interface GetTotalTransactionMonthlyParams {
  /** Хөтөлбөрийн код */
  readonly fundId?: string;
  /** Олголт авсан жил */
  readonly year?: string;
  /** Олголт авсан сар */
  readonly month?: string;
}

/** жагсаалт */
export interface GetTotalTransactionMonthlyList {
  /** Аймаг/дүүргийн нэр */
  readonly officeName: string | null;
  /** Олголт авсан дүн */
  readonly totalAmount: number | null;
  /** Олголт авсан хүний тоо */
  readonly count: number | null;
}

/** WS100932_getTotalTransactionMonthly: Аймаг дүүргээр олголтын мэдээлэл лавлах сервис */
export interface GetTotalTransactionMonthlyResponse {
  /** Олголт авсан жил */
  readonly year: number | null;
  /** Олголт авсан сар */
  readonly month: number | null;
  /** Хөтөлбөрийн нэр */
  readonly fundName: string | null;
  /** жагсаалт */
  readonly list: readonly GetTotalTransactionMonthlyList[] | null;
}

const SCHEMA: Schema = { year: "int", month: "int", fundName: "string", list: { list: { object: { officeName: "string", totalAmount: "int", count: "int" } } } };

/**
 * WS100932_getTotalTransactionMonthly: Аймаг дүүргээр олголтын мэдээлэл лавлах сервис
 *
 * Provider: Хөдөлмөр халамж, үйлчилгээний ерөнхий газар
 */
export async function getTotalTransactionMonthly(transport: Transport, params: GetTotalTransactionMonthlyParams = {}, options: CallOptions = {}): Promise<XypResult<GetTotalTransactionMonthlyResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      fundId: params.fundId,
      year: params.year,
      month: params.month,
    },
    options,
  );
  return decodeResponse<GetTotalTransactionMonthlyResponse>("GetTotalTransactionMonthlyResponse", SCHEMA, data);
}
