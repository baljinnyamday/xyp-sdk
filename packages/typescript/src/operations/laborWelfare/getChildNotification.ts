/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100919_getChildNotification (laborWelfare, Хөдөлмөр халамж, үйлчилгээний ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100919_getChildNotification";

/** Input of WS100919_getChildNotification. */
export interface GetChildNotificationParams {
  /** регистрийн дугаар */
  readonly regnum?: string;
}

/** WS100919_getChildNotification: Мэдэгдэл авах сервис */
export interface GetChildNotificationResponse {
  /** true - Мэдэгдэл харуулна. false - Мэдэгдэл харуулахгүй */
  readonly result: boolean | null;
}

const SCHEMA: Schema = { result: "bool" };

/**
 * WS100919_getChildNotification: Мэдэгдэл авах сервис
 *
 * Provider: Хөдөлмөр халамж, үйлчилгээний ерөнхий газар
 */
export async function getChildNotification(transport: Transport, params: GetChildNotificationParams = {}, options: CallOptions = {}): Promise<XypResult<GetChildNotificationResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<GetChildNotificationResponse>("GetChildNotificationResponse", SCHEMA, data);
}
