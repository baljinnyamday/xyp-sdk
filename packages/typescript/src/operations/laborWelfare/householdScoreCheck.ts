/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100927_householdScoreCheck (laborWelfare, Хөдөлмөр халамж, үйлчилгээний ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100927_householdScoreCheck";

/** Input of WS100927_householdScoreCheck. */
export interface HouseholdScoreCheckParams {
  /** малын тоогоо шалгуулах эсэх тийм-1, үгүй-0 */
  readonly isLivestockNum?: number;
  /** цалингийн мэдээллээ шалгуулах эсэх тийм-1, үгүй-0 */
  readonly isSalary?: number;
  /** татварын мэдээллээ шалгуулах эсэх тийм-1, үгүй-0 */
  readonly isPreTax?: number;
  /** Ямар системээс илгээгдсэн хүсэлт */
  readonly isFrom?: number;
  /** Иргэний регистрийн дугаар */
  readonly regnum?: string;
  /** Ам бүлийн мэдээллээ шалгуулах эсэх тийм-1, үгүй-0 */
  readonly isMemberSize?: number;
  /** Хүүхдийн мэдээллээ шалгуулах эсэх тийм-1, үгүй-0 */
  readonly isChildren?: number;
  /** Үл хөдлөх хөрөнгийн мэдээллээ шалгуулах эсэх тийм-1, үгүй-0 */
  readonly isPropertyNum?: number;
  /** тээврийн хэрэгслийн мэдээллээ шалгуулах эсэх тийм-1, үгүй-0 */
  readonly isCarNum?: number;
  /** Өрхийн амьжиргаанд нөлөөлхүйц өөрчлөлт гарсан */
  readonly isRequestSignificance?: number;
  /** Төрийн байгууллагын мэдээлэл зөрүүтэй эсэх ти */
  readonly isRequestAdmindata?: number;
}

/** WS100927_householdScoreCheck: Өрхийн оноотой холбоотой мэдээлэл шалгуулах хүсэлт илгээх */
export interface HouseholdScoreCheckResponse {
  /** хариу */
  readonly message: string | null;
}

const SCHEMA: Schema = { message: "string" };

/**
 * WS100927_householdScoreCheck: Өрхийн оноотой холбоотой мэдээлэл шалгуулах хүсэлт илгээх
 *
 * Provider: Хөдөлмөр халамж, үйлчилгээний ерөнхий газар
 */
export async function householdScoreCheck(transport: Transport, params: HouseholdScoreCheckParams = {}, options: CallOptions = {}): Promise<XypResult<HouseholdScoreCheckResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      isLivestockNum: params.isLivestockNum,
      isSalary: params.isSalary,
      isPreTax: params.isPreTax,
      isFrom: params.isFrom,
      regnum: params.regnum,
      isMemberSize: params.isMemberSize,
      isChildren: params.isChildren,
      isPropertyNum: params.isPropertyNum,
      isCarNum: params.isCarNum,
      isRequestSignificance: params.isRequestSignificance,
      isRequestAdmindata: params.isRequestAdmindata,
    },
    options,
  );
  return decodeResponse<HouseholdScoreCheckResponse>("HouseholdScoreCheckResponse", SCHEMA, data);
}
