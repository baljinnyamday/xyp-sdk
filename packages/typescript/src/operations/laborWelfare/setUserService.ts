/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100936_setUserService (laborWelfare, Хөдөлмөр халамж, үйлчилгээний ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100936_setUserService";

/** Input of WS100936_setUserService. */
export interface SetUserServiceParams {
  /** getChildListWithService ийн parentUserId дугаар байна */
  readonly parentUserId?: number;
  /** getChildListWithService ийн UserId дугаар байна */
  readonly userId?: number;
  /** default-р 2501 /STRING/ илгээнэ */
  readonly serviceCode?: string;
}

/** WS100936_setUserService: Хүүхдийн мөнгөн тэтгэмж үйлчилгээ бүртгэх сервис */
export interface SetUserServiceResponse {
  /** 1-амжилттай 0 амжилтгүй */
  readonly code: number | null;
  /** тайлбар */
  readonly result: string | null;
}

const SCHEMA: Schema = { code: "int", result: "string" };

/**
 * WS100936_setUserService: Хүүхдийн мөнгөн тэтгэмж үйлчилгээ бүртгэх сервис
 *
 * Provider: Хөдөлмөр халамж, үйлчилгээний ерөнхий газар
 */
export async function setUserService(transport: Transport, params: SetUserServiceParams = {}, options: CallOptions = {}): Promise<XypResult<SetUserServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      parentUserId: params.parentUserId,
      userId: params.userId,
      serviceCode: params.serviceCode,
    },
    options,
  );
  return decodeResponse<SetUserServiceResponse>("SetUserServiceResponse", SCHEMA, data);
}
