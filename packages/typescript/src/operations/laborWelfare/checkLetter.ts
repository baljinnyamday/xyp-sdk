/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100909_checkLetter (laborWelfare, Хөдөлмөр халамж, үйлчилгээний ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100909_checkLetter";

/** Input of WS100909_checkLetter. */
export interface CheckLetterParams {
  /** Байгууллагын регистерийн дугаар */
  readonly orgCode?: string;
  /** Он */
  readonly yearCheck?: number;
}

/** Мэдээллийн жагсаалт */
export interface CheckLetterListData {
  /** Дугаар */
  readonly actionNum: string | null;
  /** ХХҮЕГ-ын албан бичгийн дугаар */
  readonly checkNum: string | null;
}

/** WS100909_checkLetter: Байгууллагын регистрын дугаараар ХХҮЕГ-ын албан бичгийн дугаар, төрөл, оны мэдээлэл харах */
export interface CheckLetterResponse {
  /** Мэдээллийн жагсаалт */
  readonly listData: readonly CheckLetterListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { actionNum: "string", checkNum: "string" } } } };

/**
 * WS100909_checkLetter: Байгууллагын регистрын дугаараар ХХҮЕГ-ын албан бичгийн дугаар, төрөл, оны мэдээлэл харах
 *
 * Provider: Хөдөлмөр халамж, үйлчилгээний ерөнхий газар
 */
export async function checkLetter(transport: Transport, params: CheckLetterParams = {}, options: CallOptions = {}): Promise<XypResult<CheckLetterResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      orgCode: params.orgCode,
      yearCheck: params.yearCheck,
    },
    options,
  );
  return decodeResponse<CheckLetterResponse>("CheckLetterResponse", SCHEMA, data);
}
