/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100911_pensionInfo (laborWelfare, Хөдөлмөр халамж, үйлчилгээний ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100911_pensionInfo";

/** Input of WS100911_pensionInfo. */
export interface PensionInfoParams {
  /** регистрийн дугаар */
  readonly regnum?: string;
}

/** WS100911_pensionInfo: Халамжийн тэтгэвэр авагчийн мэдээлэл */
export interface PensionInfoResponse {
  /** нэр */
  readonly firstName: string | null;
  /** овог */
  readonly lastName: string | null;
  /** регистрийн дугаар */
  readonly regnum: string | null;
  /** тэтгэврийн төрөл */
  readonly pensionType: string | null;
  /** Тэтгэврийн үйлчилгээний нэр */
  readonly pensionCode: string | null;
  /** тэтгэвэр эхэлсэн огноо */
  readonly pensionStartDate: string | null;
  /** тэтгэвэр дуусан огноо */
  readonly pensionEndDate: string | null;
}

const SCHEMA: Schema = { firstName: "string", lastName: "string", regnum: "string", pensionType: "string", pensionCode: "string", pensionStartDate: "string", pensionEndDate: "string" };

/**
 * WS100911_pensionInfo: Халамжийн тэтгэвэр авагчийн мэдээлэл
 *
 * Provider: Хөдөлмөр халамж, үйлчилгээний ерөнхий газар
 */
export async function pensionInfo(transport: Transport, params: PensionInfoParams = {}, options: CallOptions = {}): Promise<XypResult<PensionInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<PensionInfoResponse>("PensionInfoResponse", SCHEMA, data);
}
