/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100929_householdScoreCalculateRequest (laborWelfare, Хөдөлмөр халамж, үйлчилгээний ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100929_householdScoreCalculateRequest";

/** Input of WS100929_householdScoreCalculateRequest. */
export interface HouseholdScoreCalculateRequestParams {
  /** регистрийн дугаар */
  readonly regnum?: string;
  /** хүсэлт илгээсэн систем E-mongolia-1,Ehalamj-2 */
  readonly isFrom?: number;
}

/** WS100929_householdScoreCalculateRequest: Өрхийн оноотой холбоотой мэдээлэл шалгуулах хүсэлт илгээх */
export interface HouseholdScoreCalculateRequestResponse {
  /** хариу */
  readonly message: string | null;
}

const SCHEMA: Schema = { message: "string" };

/**
 * WS100929_householdScoreCalculateRequest: Өрхийн оноотой холбоотой мэдээлэл шалгуулах хүсэлт илгээх
 *
 * Provider: Хөдөлмөр халамж, үйлчилгээний ерөнхий газар
 */
export async function householdScoreCalculateRequest(transport: Transport, params: HouseholdScoreCalculateRequestParams = {}, options: CallOptions = {}): Promise<XypResult<HouseholdScoreCalculateRequestResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      isFrom: params.isFrom,
    },
    options,
  );
  return decodeResponse<HouseholdScoreCalculateRequestResponse>("HouseholdScoreCalculateRequestResponse", SCHEMA, data);
}
