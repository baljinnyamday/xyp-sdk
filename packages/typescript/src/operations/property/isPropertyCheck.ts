/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100203_isPropertyCheck (property, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100203_isPropertyCheck";

/** Input of WS100203_isPropertyCheck. */
export interface IsPropertyCheckParams {
  readonly regnum?: string;
}

/** WS100203_isPropertyCheck: Эд хөрөнгөтэй эсэх сервис */
export interface IsPropertyCheckResponse {
  readonly propertyCount: number | null;
  readonly status: string | null;
}

const SCHEMA: Schema = { propertyCount: "int", status: "string" };

/**
 * WS100203_isPropertyCheck: Эд хөрөнгөтэй эсэх сервис
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function isPropertyCheck(transport: Transport, params: IsPropertyCheckParams = {}, options: CallOptions = {}): Promise<XypResult<IsPropertyCheckResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<IsPropertyCheckResponse>("IsPropertyCheckResponse", SCHEMA, data);
}
