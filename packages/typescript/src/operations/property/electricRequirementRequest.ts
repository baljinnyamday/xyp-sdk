/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100243_electricRequirementRequest (property, Эрчим хүчний яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100243_electricRequirementRequest";

/** Input of WS100243_electricRequirementRequest. */
export interface ElectricRequirementRequestParams {
  /** Хүсэлт гаргагч иргэн эсвэл байгууллагын регистрийн дугаар */
  readonly regnum?: string;
  /** Хүсэлт гаргагчийн төрөл (citizen / legalEntity) */
  readonly requesterType?: string;
  /** Хүсэлтийн төрөл (RE01 / RE02 / RE03) */
  readonly requestType?: string;
  /** Техникийн нөхцөлийн хүсэлт гаргаж буй шалтгаан */
  readonly purpose?: string;
  /** Эрхлэх үйл ажиллагааны дэлгэрэнгүй */
  readonly activityDetail?: string;
  /** Газрын нэгж талбарын дугаар */
  readonly parcelId?: string;
  /** Үл хөдлөхийн дугаар */
  readonly propertyNumber?: string;
  /** Цахилгаан хэрэглээний тооцооны хамгийн их чадал, S /кВа/ */
  readonly valueOfPeakDemand?: number;
  /** Ачааллын тооцоо base64 pdf */
  readonly loadCalculation?: string;
  /** Хүчдэлийн түвшин */
  readonly voltageLevel?: string;
  /** Чадлын коэффициент (Cos𝜑) */
  readonly powerFactor?: number;
  /** Хэрэглэгчийн зэрэглэл */
  readonly consumerLevel?: string;
  /** Сүлжээнд холбогдох хугацаа */
  readonly connectionDuration?: string;
  /** Тооцооны хамгийн их чадал авах цаг */
  readonly scheduleOfPeakDemand?: string;
  readonly spring?: boolean;
  readonly summer?: boolean;
  readonly autumn?: boolean;
  readonly winter?: boolean;
  /** Архитектур төлөвлөлтийн даалгавар */
  readonly architecturePlanningAssignment?: string;
  /** Барилгын эскиз зураг base64 */
  readonly constructionSketchDrawing?: string;
  /** Нэмэлт материал base64 */
  readonly additionalMaterial?: string;
}

/** WS100243_electricRequirementRequest: Цахилгааны техникийн нөхцөлийн хүсэлт илгээх */
export interface ElectricRequirementRequestResponse {
  readonly requestId: number | null;
  /** Хүсэлтийн шийдвэрлэлт ямар түвшинд байгааг харуулна */
  readonly status: string | null;
  /** Хүсэлтийн шийдвэрлэлт хэдэн хувийн гүйцэтгэлтэй байгааг харуулна */
  readonly finishedProcess: string | null;
  /** Уг хүсэлтийг шийдвэрлэхээр хувиарлагдсан байгууллагын нэр */
  readonly companyInCharge: string | null;
  /** Уг хүсэлтийг үүсгэсэн огноо */
  readonly createdDate: string | null;
  /** Хүсэлт гаргагч уг хүсэлтийг өөрчилсөн сүүлийн огноо */
  readonly lastUpdatedDate: string | null;
}

const SCHEMA: Schema = { requestId: "int", status: "string", finishedProcess: "string", companyInCharge: "string", createdDate: "string", lastUpdatedDate: "string" };

/**
 * WS100243_electricRequirementRequest: Цахилгааны техникийн нөхцөлийн хүсэлт илгээх
 *
 * Provider: Эрчим хүчний яам
 */
export async function electricRequirementRequest(transport: Transport, params: ElectricRequirementRequestParams = {}, options: CallOptions = {}): Promise<XypResult<ElectricRequirementRequestResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      requesterType: params.requesterType,
      requestType: params.requestType,
      purpose: params.purpose,
      activityDetail: params.activityDetail,
      parcelId: params.parcelId,
      propertyNumber: params.propertyNumber,
      valueOfPeakDemand: params.valueOfPeakDemand,
      loadCalculation: params.loadCalculation,
      voltageLevel: params.voltageLevel,
      powerFactor: params.powerFactor,
      consumerLevel: params.consumerLevel,
      connectionDuration: params.connectionDuration,
      scheduleOfPeakDemand: params.scheduleOfPeakDemand,
      spring: params.spring,
      summer: params.summer,
      autumn: params.autumn,
      winter: params.winter,
      architecturePlanningAssignment: params.architecturePlanningAssignment,
      constructionSketchDrawing: params.constructionSketchDrawing,
      additionalMaterial: params.additionalMaterial,
    },
    options,
  );
  return decodeResponse<ElectricRequirementRequestResponse>("ElectricRequirementRequestResponse", SCHEMA, data);
}
