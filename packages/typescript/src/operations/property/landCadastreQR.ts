/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100236_landCadastreQR (property, Газар зохион байгуулалт, геодези, зураг зүйн газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100236_landCadastreQR";

/** Input of WS100236_landCadastreQR. */
export interface LandCadastreQRParams {
  /** Нэгж талбарын дугаар */
  readonly id?: string;
}

/** WS100236_landCadastreQR: Газрын кадастрын нэгж талбарын QR код */
export interface LandCadastreQRResponse {
  /** qr string */
  readonly StringQr: string | null;
}

const SCHEMA: Schema = { StringQr: "string" };

/**
 * WS100236_landCadastreQR: Газрын кадастрын нэгж талбарын QR код
 *
 * Provider: Газар зохион байгуулалт, геодези, зураг зүйн газар
 */
export async function landCadastreQR(transport: Transport, params: LandCadastreQRParams = {}, options: CallOptions = {}): Promise<XypResult<LandCadastreQRResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      id: params.id,
    },
    options,
  );
  return decodeResponse<LandCadastreQRResponse>("LandCadastreQRResponse", SCHEMA, data);
}
