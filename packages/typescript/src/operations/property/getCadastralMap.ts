/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100222_getCadastralMap (property, Газар зохион байгуулалт, геодези, зураг зүйн газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100222_getCadastralMap";

/** Input of WS100222_getCadastralMap. */
export interface GetCadastralMapParams {
  /** Нэгж талбарын дугаар */
  readonly parcelId?: string;
}

/** WS100222_getCadastralMap: Кадастрын зураг авах */
export interface GetCadastralMapResponse {
  /** Кадастрын зурагны мэдээлэл */
  readonly data: string | null;
}

const SCHEMA: Schema = { data: "string" };

/**
 * WS100222_getCadastralMap: Кадастрын зураг авах
 *
 * Provider: Газар зохион байгуулалт, геодези, зураг зүйн газар
 */
export async function getCadastralMap(transport: Transport, params: GetCadastralMapParams = {}, options: CallOptions = {}): Promise<XypResult<GetCadastralMapResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      parcelId: params.parcelId,
    },
    options,
  );
  return decodeResponse<GetCadastralMapResponse>("GetCadastralMapResponse", SCHEMA, data);
}
