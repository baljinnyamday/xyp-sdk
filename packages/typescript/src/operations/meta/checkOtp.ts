/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100009_checkOtp (meta, E-Mongolia)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100009_checkOtp";

/** Input of WS100009_checkOtp. */
export interface CheckOtpParams {
  readonly otp?: string;
  readonly regnum?: string;
}

/** WS100009_checkOtp: нэг удаагийн нууц үг шалгах сервис */
export interface CheckOtpResponse {
  /** таарсан */
  readonly isMatch: boolean | null;
}

const SCHEMA: Schema = { isMatch: "bool" };

/**
 * WS100009_checkOtp: нэг удаагийн нууц үг шалгах сервис
 *
 * Provider: E-Mongolia
 */
export async function checkOtp(transport: Transport, params: CheckOtpParams = {}, options: CallOptions = {}): Promise<XypResult<CheckOtpResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      otp: params.otp,
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<CheckOtpResponse>("CheckOtpResponse", SCHEMA, data);
}
