/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100001_listAccess (meta, Үндэсний дата төв)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100001_listAccess";

/** Input of WS100001_listAccess. */
export type ListAccessParams = Readonly<Record<string, never>>;

/** WS100001_listAccess: Зөвшөөрөгдсөн веб сервис болон түүний гаралтын жагсаалтыг авах сервис */
export interface ListAccessResponse {
  /** Системийн ID */
  readonly id: number | null;
  /** Зөвшөөрөгдсөн веб сервисийн жагсаалт */
  readonly approvedServices: readonly unknown[] | null;
  readonly expireDate: Date | string | null;
  /** Сертификатын байрлах зам */
  readonly certPath: string | null;
  /** Сертификатын хурууны хээний утга */
  readonly certFingerprint: string | null;
  /** Хандах token утга */
  readonly accessToken: string | null;
  /** Бүртгэлтэй эсэх */
  readonly registered: boolean | null;
  /** Байгууллагын ID */
  readonly orgId: number | null;
  /** Байгууллагын нэр */
  readonly orgTitle: string | null;
  readonly certificate: string | null;
  readonly useNotification: number | null;
}

const SCHEMA: Schema = { id: "int", approvedServices: { list: "any" }, expireDate: "date", certPath: "string", certFingerprint: "string", accessToken: "string", registered: "bool", orgId: "int", orgTitle: "string", certificate: "string", useNotification: "int" };

/**
 * WS100001_listAccess: Зөвшөөрөгдсөн веб сервис болон түүний гаралтын жагсаалтыг авах сервис
 *
 * Provider: Үндэсний дата төв
 */
export async function listAccess(transport: Transport, _params: ListAccessParams = {}, options: CallOptions = {}): Promise<XypResult<ListAccessResponse>> {
  const data = await transport.call(OPERATION, {}, options);
  return decodeResponse<ListAccessResponse>("ListAccessResponse", SCHEMA, data);
}
