/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100638_TaxPaymentVerificationService (tax, Гааль, татвар, санхүүгийн мэдээллийн технологийн төв)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100638_TaxPaymentVerificationService";

/** Input of WS100638_TaxPaymentVerificationService. */
export interface TaxPaymentVerificationServiceParams {
  /** Финтекийн гүйлгээний дугаар */
  readonly transactionNo?: string;
  /** Гүйлгээний дугаар */
  readonly statementNo?: string;
}

/** WS100638_TaxPaymentVerificationService: Татварын төлөлт шалгах сервис */
export interface TaxPaymentVerificationServiceResponse {
  /** Сервисийн хариу */
  readonly msg: string | null;
  /** Сервисийн хариу */
  readonly status: string | null;
  /** Сервисийн хариу */
  readonly data: string | null;
}

const SCHEMA: Schema = { msg: "string", status: "string", data: "string" };

/**
 * WS100638_TaxPaymentVerificationService: Татварын төлөлт шалгах сервис
 *
 * Provider: Гааль, татвар, санхүүгийн мэдээллийн технологийн төв
 */
export async function taxPaymentVerificationService(transport: Transport, params: TaxPaymentVerificationServiceParams = {}, options: CallOptions = {}): Promise<XypResult<TaxPaymentVerificationServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      transactionNo: params.transactionNo,
      statementNo: params.statementNo,
    },
    options,
  );
  return decodeResponse<TaxPaymentVerificationServiceResponse>("TaxPaymentVerificationServiceResponse", SCHEMA, data);
}
