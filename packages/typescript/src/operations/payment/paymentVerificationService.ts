/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100855_PaymentVerificationService (payment, Татварын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100855_PaymentVerificationService";

/** Input of WS100855_PaymentVerificationService. */
export interface PaymentVerificationServiceParams {
  /** QR generate хйих сервисийн хариу дээр ирсэн qrUuid талбар */
  readonly uuid?: string;
}

/** WS100855_PaymentVerificationService: Төлөлт шалгах сервис */
export interface PaymentVerificationServiceResponse {
  /** төлөлтийн мэдээлэл */
  readonly data: string | null;
  /** сервисийн хариу код */
  readonly status: number | null;
  /** сервисийн хариу утга */
  readonly msg: string | null;
}

const SCHEMA: Schema = { data: "string", status: "int", msg: "string" };

/**
 * WS100855_PaymentVerificationService: Төлөлт шалгах сервис
 *
 * Provider: Татварын ерөнхий газар
 */
export async function paymentVerificationService(transport: Transport, params: PaymentVerificationServiceParams = {}, options: CallOptions = {}): Promise<XypResult<PaymentVerificationServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      uuid: params.uuid,
    },
    options,
  );
  return decodeResponse<PaymentVerificationServiceResponse>("PaymentVerificationServiceResponse", SCHEMA, data);
}
