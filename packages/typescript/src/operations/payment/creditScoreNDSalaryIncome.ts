/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100858_CreditScoreNDSalaryIncome (payment, Бүрэн Скор ЗМС ХХК)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100858_CreditScoreNDSalaryIncome";

/** Input of WS100858_CreditScoreNDSalaryIncome. */
export interface CreditScoreNDSalaryIncomeParams {
  readonly regnum?: string;
}

/** WS100858_CreditScoreNDSalaryIncome: Зээлжих зэрэглэлийн оноо /Бүрэн оноо/-г зөвхөн “Цалингийн орлого /НДШ/” орлогоор тооцох */
export interface CreditScoreNDSalaryIncomeResponse {
  readonly data: unknown | null;
  readonly restInquirySummaryResponse: unknown | null;
  readonly score: number | null;
  readonly vendor: string | null;
  readonly stateHistoriesData: readonly unknown[] | null;
}

const SCHEMA: Schema = { data: "any", restInquirySummaryResponse: "any", score: "int", vendor: "string", stateHistoriesData: { list: "any" } };

/**
 * WS100858_CreditScoreNDSalaryIncome: Зээлжих зэрэглэлийн оноо /Бүрэн оноо/-г зөвхөн “Цалингийн орлого /НДШ/” орлогоор тооцох
 *
 * Provider: Бүрэн Скор ЗМС ХХК
 */
export async function creditScoreNDSalaryIncome(transport: Transport, params: CreditScoreNDSalaryIncomeParams = {}, options: CallOptions = {}): Promise<XypResult<CreditScoreNDSalaryIncomeResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<CreditScoreNDSalaryIncomeResponse>("CreditScoreNDSalaryIncomeResponse", SCHEMA, data);
}
