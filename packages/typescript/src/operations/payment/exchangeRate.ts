/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100847_ExchangeRate (payment, Монгол банк)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100847_ExchangeRate";

/** Input of WS100847_ExchangeRate. */
export interface ExchangeRateParams {
  /** Ханш татах өдөр */
  readonly date?: string;
}

/** жагсаалт */
export interface ExchangeRateDataList {
  /** Нэр */
  readonly name: string | null;
  /** код */
  readonly code: string | null;
  /** Ханш */
  readonly price: string | null;
  /** огноо */
  readonly date: string | null;
}

/** WS100847_ExchangeRate: Монголбанкны албан ханш авах сервис */
export interface ExchangeRateResponse {
  /** жагсаалт */
  readonly dataList: readonly ExchangeRateDataList[] | null;
}

const SCHEMA: Schema = { dataList: { list: { object: { name: "string", code: "string", price: "string", date: "string" } } } };

/**
 * WS100847_ExchangeRate: Монголбанкны албан ханш авах сервис
 *
 * Provider: Монгол банк
 */
export async function exchangeRate(transport: Transport, params: ExchangeRateParams = {}, options: CallOptions = {}): Promise<XypResult<ExchangeRateResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      date: params.date,
    },
    options,
  );
  return decodeResponse<ExchangeRateResponse>("ExchangeRateResponse", SCHEMA, data);
}
