/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100862_PaymentOrderInvoiceGenerator (payment, Татварын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100862_PaymentOrderInvoiceGenerator";

/** Input of WS100862_PaymentOrderInvoiceGenerator. */
export interface PaymentOrderInvoiceGeneratorParams {
  /** УТХ хүлээн авагч байгууллагын татвар төлөгчийн дугаар */
  readonly authorityTin?: string;
  /** Харилцагчийн татвар төлөгчийн дугаар */
  readonly taxpayerTin?: string;
  readonly taxpayerEntId?: number;
  /** Татварын төрлийн код */
  readonly taxTypeCode?: string;
  /** Татварын албаны код */
  readonly branchCode?: string;
  /** Татварын дэд албаны код */
  readonly subBranchCode?: string;
  /** Төлбөрийн дүн */
  readonly amount?: number;
  /** Тайлбар */
  readonly description?: string;
  /** Харилцагчийн утас */
  readonly phoneNumber?: string;
  /** 2-taxTypeCode заавал, 1-stateAccount заавал */
  readonly type?: number;
  /** Тохиргооны дугаар */
  readonly configId?: number;
  readonly calledFrom?: string;
  /** жагсаалт */
  readonly requiredFeilds?: readonly unknown[];
}

/** WS100862_PaymentOrderInvoiceGenerator: Төлбөрийн даалгаварын нэхэмжлэх үүсгэх сервис */
export interface PaymentOrderInvoiceGeneratorResponse {
  /** Нэхэмжлэх /Цахим төлбөрийн даалгавар/-ын дугаар */
  readonly invoiceNo: string | null;
  /** Нэхэмжлэх үүссэн огноо */
  readonly invoiceDate: string | null;
}

const SCHEMA: Schema = { invoiceNo: "string", invoiceDate: "string" };

/**
 * WS100862_PaymentOrderInvoiceGenerator: Төлбөрийн даалгаварын нэхэмжлэх үүсгэх сервис
 *
 * Provider: Татварын ерөнхий газар
 */
export async function paymentOrderInvoiceGenerator(transport: Transport, params: PaymentOrderInvoiceGeneratorParams = {}, options: CallOptions = {}): Promise<XypResult<PaymentOrderInvoiceGeneratorResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      authorityTin: params.authorityTin,
      taxpayerTin: params.taxpayerTin,
      taxpayerEntId: params.taxpayerEntId,
      taxTypeCode: params.taxTypeCode,
      branchCode: params.branchCode,
      subBranchCode: params.subBranchCode,
      amount: params.amount,
      description: params.description,
      phoneNumber: params.phoneNumber,
      type: params.type,
      configId: params.configId,
      calledFrom: params.calledFrom,
      requiredFeilds: params.requiredFeilds,
    },
    options,
  );
  return decodeResponse<PaymentOrderInvoiceGeneratorResponse>("PaymentOrderInvoiceGeneratorResponse", SCHEMA, data);
}
