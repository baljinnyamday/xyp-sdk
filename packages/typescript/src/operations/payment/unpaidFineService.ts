/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100872_UnpaidFineService (payment, Монгол улсын ерөнхий прокурорын газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100872_UnpaidFineService";

/** Input of WS100872_UnpaidFineService. */
export interface UnpaidFineServiceParams {
  /** Тээврийн хэрэгслийн улсын дугаар */
  readonly plateNumber?: string;
  readonly tokenUsername?: string;
  readonly tokenPassword?: string;
}

/** Жагсаалт */
export interface UnpaidFineServiceListData {
  /** Шийтгэлийн дугаар */
  readonly shiitgelDugaar: string | null;
  /** Нийт төлөх дүн (торгууль + нэмэлт) */
  readonly totalAmount: number | null;
  /** Үндсэн торгуулийн дүн */
  readonly amount: number | null;
  /** Нэхэмжлэхийн дугаар */
  readonly invoiceNumber: string | null;
  /** Төлбөрийн төлөв */
  readonly statusName: string | null;
  /** Торгууль ногдуулсан огноо */
  readonly penaltyDate: string | null;
  /** Төлсөн огноо */
  readonly paidDate: string | null;
  /** Тээврийн хэрэгслийн улсын дугаар */
  readonly plateNumber: string | null;
  /** Хуулийн этгээдийн регистр */
  readonly legalEntityNumber: string | null;
  /** Хувь хүний овог */
  readonly lastName: string | null;
  /** Хувь хүний нэр */
  readonly firstName: string | null;
  /** Хуулийн этгээдийн нэр */
  readonly legalEntityName: string | null;
  /** Хаяг, байрлалын дэлгэрэнгүй */
  readonly addressDelgerengui: string | null;
  /** Хууль, зөрчлийн зүйл заалтын код */
  readonly zuilZaaltName: string | null;
  /** Хууль, зөрчлийн зүйл заалтын дэлгэрэнгүй тайлбар */
  readonly zuilZaalt: string | null;
  /** Эрх бүхий байгууллагын нэгжийн нэр */
  readonly ebbNegjName: string | null;
  /** Эрх бүхий албан тушаалтны нэр */
  readonly ebatName: string | null;
  /** Хувь хүний регистрийн дугаар */
  readonly regnum: string | null;
}

/** WS100872_UnpaidFineService: Тээврийн хэрэгслийн улсын дугаараар торгуулийн мэдээлэл авах - Төлбөр төлөгдөөгүй */
export interface UnpaidFineServiceResponse {
  /** Жагсаалт */
  readonly listData: readonly UnpaidFineServiceListData[] | null;
}

const SCHEMA: Schema = {
  listData: {
    list: {
      object: {
        shiitgelDugaar: "string",
        totalAmount: "int",
        amount: "int",
        invoiceNumber: "string",
        statusName: "string",
        penaltyDate: "string",
        paidDate: "string",
        plateNumber: "string",
        legalEntityNumber: "string",
        lastName: "string",
        firstName: "string",
        legalEntityName: "string",
        addressDelgerengui: "string",
        zuilZaaltName: "string",
        zuilZaalt: "string",
        ebbNegjName: "string",
        ebatName: "string",
        regnum: "string",
      },
    },
  },
};

/**
 * WS100872_UnpaidFineService: Тээврийн хэрэгслийн улсын дугаараар торгуулийн мэдээлэл авах - Төлбөр төлөгдөөгүй
 *
 * Provider: Монгол улсын ерөнхий прокурорын газар
 */
export async function unpaidFineService(transport: Transport, params: UnpaidFineServiceParams = {}, options: CallOptions = {}): Promise<XypResult<UnpaidFineServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      plateNumber: params.plateNumber,
      tokenUsername: params.tokenUsername,
      tokenPassword: params.tokenPassword,
    },
    options,
  );
  return decodeResponse<UnpaidFineServiceResponse>("UnpaidFineServiceResponse", SCHEMA, data);
}
