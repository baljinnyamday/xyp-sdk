/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100843_personLoanInfoTitan (payment, Титан си ар эй зээлийн мэдээллийн сан)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100843_personLoanInfoTitan";

/** Input of WS100843_personLoanInfoTitan. */
export interface PersonLoanInfoTitanParams {
  /** Иргэн, ААН регистрийн дугаар */
  readonly regnum?: string;
}

/** Жагсаалт */
export interface PersonLoanInfoTitanRell {
  /** Үндсэн зээлдэгч Нийт зээлийн тоо */
  readonly total: number | null;
  /** Үндсэн зээлдэгч Хэвийн зээлийн тоо */
  readonly normal: number | null;
  /** Үндсэн зээлдэгч Анхаарал хандуулах зээлийн тоо */
  readonly attention: number | null;
  /** Үндсэн зээлдэгч Чанаргүй зээлийн тоо */
  readonly notQuality: number | null;
}

/** Жагсаалт */
export interface PersonLoanInfoTitanMain {
  /** Үндсэн зээлдэгч Нийт зээлийн тоо */
  readonly total: number | null;
  /** Үндсэн зээлдэгч Хэвийн зээлийн тоо */
  readonly normal: number | null;
  /** Үндсэн зээлдэгч Анхаарал хандуулах зээлийн тоо */
  readonly attention: number | null;
  /** Үндсэн зээлдэгч Чанаргүй зээлийн тоо */
  readonly notQuality: number | null;
}

/** WS100843_personLoanInfoTitan: Монгол улсын иргэний зээлийн мэдээллийг авах сервис */
export interface PersonLoanInfoTitanResponse {
  /** Жагсаалт */
  readonly rell: readonly PersonLoanInfoTitanRell[] | null;
  /** Жагсаалт */
  readonly main: readonly PersonLoanInfoTitanMain[] | null;
  /** Өр төлбөртэй эсэх, 0-төлбөргүй, 1-төлбөртэй */
  readonly is_liabilities: number | null;
}

const SCHEMA: Schema = { rell: { list: { object: { total: "int", normal: "int", attention: "int", notQuality: "int" } } }, main: { list: { object: { total: "int", normal: "int", attention: "int", notQuality: "int" } } }, is_liabilities: "int" };

/**
 * WS100843_personLoanInfoTitan: Монгол улсын иргэний зээлийн мэдээллийг авах сервис
 *
 * Provider: Титан си ар эй зээлийн мэдээллийн сан
 */
export async function personLoanInfoTitan(transport: Transport, params: PersonLoanInfoTitanParams = {}, options: CallOptions = {}): Promise<XypResult<PersonLoanInfoTitanResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<PersonLoanInfoTitanResponse>("PersonLoanInfoTitanResponse", SCHEMA, data);
}
