/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100818_taxPayerInfo (payment, Татварын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100818_taxPayerInfo";

/** Input of WS100818_taxPayerInfo. */
export interface TaxPayerInfoParams {
  /** Татвар төлөгчийн регистрийн дугаар */
  readonly pin?: string;
  /** Татвар төлөгчийн дугаар */
  readonly tin?: string;
}

/** Татвар төлөгчийн жагсаалт */
export interface TaxPayerInfoListTins {
  /** Татвар төлөгчийн дугаар */
  readonly tin: string | null;
  /** Татвар төлөгчийн нэр */
  readonly name: string | null;
}

/** WS100818_taxPayerInfo: Татвар төлөгчийн Регистрийн дугаараар ТТД-ын лавлагаа авах */
export interface TaxPayerInfoResponse {
  /** Татвар төлөгчийн жагсаалт */
  readonly listTins: readonly TaxPayerInfoListTins[] | null;
}

const SCHEMA: Schema = { listTins: { list: { object: { tin: "string", name: "string" } } } };

/**
 * WS100818_taxPayerInfo: Татвар төлөгчийн Регистрийн дугаараар ТТД-ын лавлагаа авах
 *
 * Provider: Татварын ерөнхий газар
 */
export async function taxPayerInfo(transport: Transport, params: TaxPayerInfoParams = {}, options: CallOptions = {}): Promise<XypResult<TaxPayerInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      pin: params.pin,
      tin: params.tin,
    },
    options,
  );
  return decodeResponse<TaxPayerInfoResponse>("TaxPayerInfoResponse", SCHEMA, data);
}
