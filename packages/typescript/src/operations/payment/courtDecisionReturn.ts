/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100863_CourtDecisionReturn (payment, Татварын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100863_CourtDecisionReturn";

/** Input of WS100863_CourtDecisionReturn. */
export interface CourtDecisionReturnParams {
  /** Төрийн сангийн дансны дугаар */
  readonly stateAccount?: number;
  /** Буцаан олголт авах татвар төлөгчийн iban дансны дугаар */
  readonly refundIban?: string;
  /** Буцаалтын дүн */
  readonly refundAmount?: number;
  /** Буцаан олголт авах татвар төлөгчийн ТТД */
  readonly tin?: string;
  /** Буцаан олгосон тайлбар */
  readonly refundReason?: string;
  /** УТХ төлсөн нэхэмжлэхийн дугаар */
  readonly invoiceNo?: string;
  /** Шүүхийн шийдвэрийн дугаар */
  readonly decisionNo?: string;
  /** Шүүхийн шийдвэрийн огноо дугаар (YYYY-MM-DD) */
  readonly decisionDate?: string;
  /** Шүүхийн шийдвэрийн илгээсэн файлын id */
  readonly fileId?: string;
}

/** WS100863_CourtDecisionReturn: Шүүхийн шийдвэрээр буцаалт хийх хүсэлт илгээх сервис */
export interface CourtDecisionReturnResponse {
  /** Хүсэлтийн дугаар */
  readonly code: string | null;
}

const SCHEMA: Schema = { code: "string" };

/**
 * WS100863_CourtDecisionReturn: Шүүхийн шийдвэрээр буцаалт хийх хүсэлт илгээх сервис
 *
 * Provider: Татварын ерөнхий газар
 */
export async function courtDecisionReturn(transport: Transport, params: CourtDecisionReturnParams = {}, options: CallOptions = {}): Promise<XypResult<CourtDecisionReturnResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      stateAccount: params.stateAccount,
      refundIban: params.refundIban,
      refundAmount: params.refundAmount,
      tin: params.tin,
      refundReason: params.refundReason,
      invoiceNo: params.invoiceNo,
      decisionNo: params.decisionNo,
      decisionDate: params.decisionDate,
      fileId: params.fileId,
    },
    options,
  );
  return decodeResponse<CourtDecisionReturnResponse>("CourtDecisionReturnResponse", SCHEMA, data);
}
