/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100831_citizenLoanInfo (payment, Монгол банк)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100831_citizenLoanInfo";

/** Input of WS100831_citizenLoanInfo. */
export interface CitizenLoanInfoParams {
  /** Иргэн, ААН регистрийн дугаар */
  readonly regnum?: string;
}

/** WS100831_citizenLoanInfo: Монгол улсын иргэний зээлийн мэдээллийг авах сервис */
export interface CitizenLoanInfoResponse {
  readonly listData: readonly unknown[] | null;
}

const SCHEMA: Schema = { listData: { list: "any" } };

/**
 * WS100831_citizenLoanInfo: Монгол улсын иргэний зээлийн мэдээллийг авах сервис
 *
 * Provider: Монгол банк
 */
export async function citizenLoanInfo(transport: Transport, params: CitizenLoanInfoParams = {}, options: CallOptions = {}): Promise<XypResult<CitizenLoanInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<CitizenLoanInfoResponse>("CitizenLoanInfoResponse", SCHEMA, data);
}
