/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100864_PaymentInfoByRegisterService (payment, Монгол улсын ерөнхий прокурорын газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100864_PaymentInfoByRegisterService";

/** Input of WS100864_PaymentInfoByRegisterService. */
export interface PaymentInfoByRegisterServiceParams {
  /** Регистрийн дугаар */
  readonly regnum?: string;
}

/** Жагсаалт */
export interface PaymentInfoByRegisterServiceListData {
  /** Торгуулийн нийт дүн (төгрөгөөр). */
  readonly amount: number | null;
  /** Торгуулийн нэхэмжлэлийн буюу баримтын дугаар (ихэвчлэн бар код хэлбэрээр ашиглагддаг). */
  readonly invoiceNumber: string | null;
  /** Төлөв */
  readonly statusName: string | null;
  /** Зөрчил гарсан газрын байршлын нарийвчилсан тодорхойлолт. */
  readonly localName: string | null;
  /** Зөрчил гарсан буюу торгууль ногдуулсан огноо (YYYY-MM-DD форматтай). */
  readonly passDate: string | null;
  /** Торгууль төлөх дансны дугаар */
  readonly paymentBankAccount: string | null;
  /** Торгуулийн орлогын дансны нэр. Жишээ: “УБ.Торгуулийн орлого”. */
  readonly paymentBankAccountName: string | null;
  /** Торгууль хүлээн авах банкны нэр. Жишээ: “Төрийн сан”. */
  readonly paymentBankName: string | null;
  /** Төлбөр хийсэн огноо (null бол төлөөгүй гэсэн үг). */
  readonly paymentDate: string | null;
  /** Зөрчил гаргасан автомашины улсын дугаар. */
  readonly plateNumber: string | null;
  /** QR кодын мэдээлэл (торгуулийн баримтад ашиглагддаг, null = хараахан үүсээгүй). */
  readonly qpQRcode: string | null;
  /** Хуулийн зөрчлийн нэр, шалтгааны тайлбар. Жишээ: “14.7. Замын хөдөлгөөний аюулгүй байдлын тухай хууль зөрчих”. */
  readonly reasonType: string | null;
  /** Хуулийн зөрчлийн код (1114070000 гэх мэт). */
  readonly reasonTypeCode: number | null;
  /** Шийтгэлийн нэмэлт мэдээлэл эсвэл тайлбар (null = одоогоор байхгүй). */
  readonly sanc: string | null;
  /** Торгууль төлөгдсөн эсэх (true = төлсөн, false = төлөөгүй). */
  readonly paid: boolean | null;
  /** Баркод, шийтгэлийн хуудасны дугаар болон гомдлын дугаарыг буцаана */
  readonly gomdolDugaar: string | null;
  /** Зөрчлийн хэргийн дугаарыг буцаана */
  readonly zurchilDugaar: string | null;
  /** Шийтгэлийн дугаарыг буцаана */
  readonly shiitgelDugaar: string | null;
  /** Эрх бүхий байгууллагын нэр (жишээ: “ТЦА, ЗЦГ”). */
  readonly ebbName: string | null;
  /** Хуулийн заалтын нэр. */
  readonly zuilZaaltName: string | null;
}

/** WS100864_PaymentInfoByRegisterService: Иргэн/хуулийн этгээдийн регистрийн дугаараар төлбөрийн мэдээлэл авах сервис */
export interface PaymentInfoByRegisterServiceResponse {
  /** Жагсаалт */
  readonly listData: readonly PaymentInfoByRegisterServiceListData[] | null;
}

const SCHEMA: Schema = {
  listData: {
    list: {
      object: {
        amount: "int",
        invoiceNumber: "string",
        statusName: "string",
        localName: "string",
        passDate: "string",
        paymentBankAccount: "string",
        paymentBankAccountName: "string",
        paymentBankName: "string",
        paymentDate: "string",
        plateNumber: "string",
        qpQRcode: "string",
        reasonType: "string",
        reasonTypeCode: "int",
        sanc: "string",
        paid: "bool",
        gomdolDugaar: "string",
        zurchilDugaar: "string",
        shiitgelDugaar: "string",
        ebbName: "string",
        zuilZaaltName: "string",
      },
    },
  },
};

/**
 * WS100864_PaymentInfoByRegisterService: Иргэн/хуулийн этгээдийн регистрийн дугаараар төлбөрийн мэдээлэл авах сервис
 *
 * Provider: Монгол улсын ерөнхий прокурорын газар
 */
export async function paymentInfoByRegisterService(transport: Transport, params: PaymentInfoByRegisterServiceParams = {}, options: CallOptions = {}): Promise<XypResult<PaymentInfoByRegisterServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<PaymentInfoByRegisterServiceResponse>("PaymentInfoByRegisterServiceResponse", SCHEMA, data);
}
