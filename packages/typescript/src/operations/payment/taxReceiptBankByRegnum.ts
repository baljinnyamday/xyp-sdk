/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100838_taxReceiptBankByRegnum (payment, Татварын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100838_taxReceiptBankByRegnum";

/** Input of WS100838_taxReceiptBankByRegnum. */
export interface TaxReceiptBankByRegnumParams {
  /** Суутган төлөгч байгууллагын татвар төлөгчийн дугаар */
  readonly authorityPin?: string;
  /** Татварын төрлийн код */
  readonly taxTypeCode?: string;
  /** Төрийн сан дахь татварын орлогын данс */
  readonly stateAccount?: string;
  /** Төлбөрийн дүн */
  readonly amount?: string;
  /** Татвар төлөгчийн утасны дугаар */
  readonly phoneNo?: string;
  /** цахим шуудан */
  readonly email?: string;
  /** Татвар төлөгчийн регистрийн дугаар */
  readonly regnum?: string;
  /** салбарын код */
  readonly branchCode?: string;
  /** дэд салбарын код */
  readonly subBranchCode?: string;
  /** Тайлбар */
  readonly description?: string;
  /** төрөл */
  readonly type?: string;
}

/** WS100838_taxReceiptBankByRegnum: Регистрийн дугаараар нэхэмжлэх үүсгэх сервис */
export interface TaxReceiptBankByRegnumResponse {
  /** ”Цахим төлбөрийн даалгавар”-ын дугаар */
  readonly invoiceNo: string | null;
  /** нэмэжлэлийн төрөл */
  readonly invoiceType: string | null;
  /** Үүссэн огноо */
  readonly createdDate: string | null;
}

const SCHEMA: Schema = { invoiceNo: "string", invoiceType: "string", createdDate: "string" };

/**
 * WS100838_taxReceiptBankByRegnum: Регистрийн дугаараар нэхэмжлэх үүсгэх сервис
 *
 * Provider: Татварын ерөнхий газар
 */
export async function taxReceiptBankByRegnum(transport: Transport, params: TaxReceiptBankByRegnumParams = {}, options: CallOptions = {}): Promise<XypResult<TaxReceiptBankByRegnumResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      authorityPin: params.authorityPin,
      taxTypeCode: params.taxTypeCode,
      stateAccount: params.stateAccount,
      amount: params.amount,
      phoneNo: params.phoneNo,
      email: params.email,
      regnum: params.regnum,
      branchCode: params.branchCode,
      subBranchCode: params.subBranchCode,
      description: params.description,
      type: params.type,
    },
    options,
  );
  return decodeResponse<TaxReceiptBankByRegnumResponse>("TaxReceiptBankByRegnumResponse", SCHEMA, data);
}
