/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100517_checkMotherTimeAllInfo (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100517_checkMotherTimeAllInfo";

/** Input of WS100517_checkMotherTimeAllInfo. */
export interface CheckMotherTimeAllInfoParams {
  /** Регистрийн дугаарын жагсаалт */
  readonly listRequestData?: readonly unknown[];
}

/** WS100517_checkMotherTimeAllInfo: Хүүхэд асарч буй эх ажилд орсон эсэхийг олноор шалгах сервис */
export interface CheckMotherTimeAllInfoResponse {
  /** Мэдээллийн жагсаалт */
  readonly list: readonly unknown[] | null;
}

const SCHEMA: Schema = { list: { list: "any" } };

/**
 * WS100517_checkMotherTimeAllInfo: Хүүхэд асарч буй эх ажилд орсон эсэхийг олноор шалгах сервис
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function checkMotherTimeAllInfo(transport: Transport, params: CheckMotherTimeAllInfoParams = {}, options: CallOptions = {}): Promise<XypResult<CheckMotherTimeAllInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      listRequestData: params.listRequestData,
    },
    options,
  );
  return decodeResponse<CheckMotherTimeAllInfoResponse>("CheckMotherTimeAllInfoResponse", SCHEMA, data);
}
