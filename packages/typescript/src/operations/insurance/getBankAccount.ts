/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100593_GetBankAccount (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100593_GetBankAccount";

/** Input of WS100593_GetBankAccount. */
export interface GetBankAccountParams {
  /** Нэхэмжлэхийн дугаар */
  readonly invoiceNumber?: string;
}

export interface GetBankAccountList {
  /** Дансны нэр */
  readonly accountName: string | null;
  /** Банкны нэр */
  readonly bankName: string | null;
  /** Банкны код */
  readonly bankCode: string | null;
  /** Дансны дугаар */
  readonly accountNumber: string | null;
}

/** WS100593_GetBankAccount: Нэхэмжлэхийн дугаараар банкны дансны мэдээлэл авах */
export interface GetBankAccountResponse {
  readonly list: readonly GetBankAccountList[] | null;
}

const SCHEMA: Schema = { list: { list: { object: { accountName: "string", bankName: "string", bankCode: "string", accountNumber: "string" } } } };

/**
 * WS100593_GetBankAccount: Нэхэмжлэхийн дугаараар банкны дансны мэдээлэл авах
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function getBankAccount(transport: Transport, params: GetBankAccountParams = {}, options: CallOptions = {}): Promise<XypResult<GetBankAccountResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      invoiceNumber: params.invoiceNumber,
    },
    options,
  );
  return decodeResponse<GetBankAccountResponse>("GetBankAccountResponse", SCHEMA, data);
}
