/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100565_getCitizenSalaryInfoEng (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100565_getCitizenSalaryInfoEng";

/** Input of WS100565_getCitizenSalaryInfoEng. */
export interface GetCitizenSalaryInfoEngParams {
  /** Регистрийн дугаар */
  readonly regnum?: string;
  /** Эхлэх огноо */
  readonly startYear?: number;
  /** Дуусах огноо */
  readonly endYear?: number;
}

export interface GetCitizenSalaryInfoEngDetailDataList {
  readonly domName: string | null;
  readonly isPayed: string | null;
  readonly month: number | null;
  readonly occcode: string | null;
  readonly orgName: string | null;
  readonly orgndid: string | null;
  readonly orgRegId: string | null;
  readonly salary: number | null;
  readonly shim: number | null;
  readonly year: number | null;
}

/** WS100565_getCitizenSalaryInfoEng: Иргэний нийгмийн даатгалын мэдээлэл ангил хэл дээр дамжуулах сервис */
export interface GetCitizenSalaryInfoEngResponse {
  readonly detailDataList: readonly GetCitizenSalaryInfoEngDetailDataList[] | null;
  /** Нэр */
  readonly firstname: string | null;
  /** Овог */
  readonly lastname: string | null;
  /** Иргэний регистрийн дугаар */
  readonly regnum: string | null;
}

const SCHEMA: Schema = { detailDataList: { list: { object: { domName: "string", isPayed: "string", month: "int", occcode: "string", orgName: "string", orgndid: "string", orgRegId: "string", salary: "float", shim: "float", year: "int" } } }, firstname: "string", lastname: "string", regnum: "string" };

/**
 * WS100565_getCitizenSalaryInfoEng: Иргэний нийгмийн даатгалын мэдээлэл ангил хэл дээр дамжуулах сервис
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function getCitizenSalaryInfoEng(transport: Transport, params: GetCitizenSalaryInfoEngParams = {}, options: CallOptions = {}): Promise<XypResult<GetCitizenSalaryInfoEngResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      startYear: params.startYear,
      endYear: params.endYear,
    },
    options,
  );
  return decodeResponse<GetCitizenSalaryInfoEngResponse>("GetCitizenSalaryInfoEngResponse", SCHEMA, data);
}
