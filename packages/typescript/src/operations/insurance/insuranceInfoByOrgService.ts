/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100576_insuranceInfoByOrgService (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100576_insuranceInfoByOrgService";

/** Input of WS100576_insuranceInfoByOrgService. */
export interface InsuranceInfoByOrgServiceParams {
  /** Иргэний бүртгэлийн дугаар */
  readonly civilId?: string;
  /** Ажил олгогчийн НДБ дугаар */
  readonly orgNdId?: string;
}

/** мэдээллийн жагсаалт */
export interface InsuranceInfoByOrgServiceListData {
  /** Даатгуулагчийн төрөл */
  readonly benClass: string | null;
  /** Он */
  readonly calYear: number | null;
  /** Сар */
  readonly calMonth: number | null;
}

/** WS100576_insuranceInfoByOrgService: Даатгуулагчийн тухайн ажил олгогч дээрхи сүүлийн сар, даатгуулагчийн төрөл буцаах */
export interface InsuranceInfoByOrgServiceResponse {
  /** мэдээллийн жагсаалт */
  readonly listData: readonly InsuranceInfoByOrgServiceListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { benClass: "string", calYear: "int", calMonth: "int" } } } };

/**
 * WS100576_insuranceInfoByOrgService: Даатгуулагчийн тухайн ажил олгогч дээрхи сүүлийн сар, даатгуулагчийн төрөл буцаах
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function insuranceInfoByOrgService(transport: Transport, params: InsuranceInfoByOrgServiceParams = {}, options: CallOptions = {}): Promise<XypResult<InsuranceInfoByOrgServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      civilId: params.civilId,
      orgNdId: params.orgNdId,
    },
    options,
  );
  return decodeResponse<InsuranceInfoByOrgServiceResponse>("InsuranceInfoByOrgServiceResponse", SCHEMA, data);
}
