/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100579_pensionFiveYearsAverageChange (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100579_pensionFiveYearsAverageChange";

/** Input of WS100579_pensionFiveYearsAverageChange. */
export interface PensionFiveYearsAverageChangeParams {
  /** Тэтгэвэр авагчийн регистрийн дугаар */
  readonly regnum?: string;
  /** 1 – “Анх тэтгэвэр тогтоолгоход баримталсан 6 эсхүл 7 жилийн дундаж цалин хөлснөөс өөрт аль ашигтай дараалсан 5 жилийн дундаж цалин хөлсийг сонгоно“ “2” – “Тэтгэвэр тогтоолгосны өмнөх шимтгэл төлсөн сүүлийн 20 жилийн доторх өөрт аль ашигтай дараалсан өөр 5 жилийн дундаж цалин хөлсийг сонгоно“ */
  readonly requestId?: string;
  /** 2 кодтой хүсэлт сонгоод цалин эхлэх он сарыг сонгосон бол Цалин эхлэх огноо “yyyy-MM-01” форматаар хэрвээ “1” кодтой хүсэлт сонгосон эсвэл “2” кодтой хүсэлт сонгоод Цалин эхлэх огноо сонгоогүй бол хоосон string */
  readonly requestDate?: string;
}

/** WS100579_pensionFiveYearsAverageChange: “Тэтгэврээ 5 жилийн дундаж цалин хөлсөөр НЭГ УДАА өөрчлөн тогтоолгох хүсэлт” илгээх */
export interface PensionFiveYearsAverageChangeResponse {
  /** 0 – “Өмнө нь хүсэлт илгээгдсэн байна. Хадгалах боломжгүй.” 1 – “Амжилттай хадгаллаа.” -1 – “Алдаа гарлаа.” */
  readonly code: number | null;
  /** Хариу мессеж */
  readonly message: string | null;
}

const SCHEMA: Schema = { code: "int", message: "string" };

/**
 * WS100579_pensionFiveYearsAverageChange: “Тэтгэврээ 5 жилийн дундаж цалин хөлсөөр НЭГ УДАА өөрчлөн тогтоолгох хүсэлт” илгээх
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function pensionFiveYearsAverageChange(transport: Transport, params: PensionFiveYearsAverageChangeParams = {}, options: CallOptions = {}): Promise<XypResult<PensionFiveYearsAverageChangeResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      requestId: params.requestId,
      requestDate: params.requestDate,
    },
    options,
  );
  return decodeResponse<PensionFiveYearsAverageChangeResponse>("PensionFiveYearsAverageChangeResponse", SCHEMA, data);
}
