/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100545_medicalCertCheck (insurance, Хөдөлмөр халамж, үйлчилгээний ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100545_medicalCertCheck";

/** Input of WS100545_medicalCertCheck. */
export interface MedicalCertCheckParams {
  /** Иргэний регистрийн дугаар */
  readonly regnum?: string;
  /** Эхлэх огноо yyyy-MM-dd */
  readonly startDate?: string;
  /** Дуусах огноо yyyy-MM-dd */
  readonly endDate?: string;
}

/** WS100545_medicalCertCheck: Хөдөлмөрийн чадвар түр алдсаны эмнэлгийн хуудас шалгах */
export interface MedicalCertCheckResponse {
  /** 0 – success, 1 – error */
  readonly result: number | null;
  /** тайлбар */
  readonly resultMsg: string | null;
}

const SCHEMA: Schema = { result: "int", resultMsg: "string" };

/**
 * WS100545_medicalCertCheck: Хөдөлмөрийн чадвар түр алдсаны эмнэлгийн хуудас шалгах
 *
 * Provider: Хөдөлмөр халамж, үйлчилгээний ерөнхий газар
 */
export async function medicalCertCheck(transport: Transport, params: MedicalCertCheckParams = {}, options: CallOptions = {}): Promise<XypResult<MedicalCertCheckResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      startDate: params.startDate,
      endDate: params.endDate,
    },
    options,
  );
  return decodeResponse<MedicalCertCheckResponse>("MedicalCertCheckResponse", SCHEMA, data);
}
