/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100589_ListOfInvestmentRequests (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100589_ListOfInvestmentRequests";

/** Input of WS100589_ListOfInvestmentRequests. */
export interface ListOfInvestmentRequestsParams {
  /** Регистрийн дугаар */
  readonly regnum?: string;
  /** Он */
  readonly appYear?: number;
}

/** жагсаалт */
export interface ListOfInvestmentRequestsList {
  /** Хөрөнгө оруулах банкны нэр */
  readonly bankname: string | null;
  /** Хөрөнгө оруулах банкны код */
  readonly bankId: number | null;
  /** үсэлтийн дугаар */
  readonly orderNumber: string | null;
  readonly monetizeType: string | null;
  /** Мөнгөжүүлсэн дүн */
  readonly amount: string | null;
  /** ОН */
  readonly appYear: string | null;
  /** Регистрийн дугаар */
  readonly regnum: string | null;
}

/** WS100589_ListOfInvestmentRequests: ХӨРӨНГӨ ОРУУЛАХ ХҮСЭЛТИЙН ЖАГСААЛТ */
export interface ListOfInvestmentRequestsResponse {
  /** жагсаалт */
  readonly list: readonly ListOfInvestmentRequestsList[] | null;
}

const SCHEMA: Schema = { list: { list: { object: { bankname: "string", bankId: "int", orderNumber: "string", monetizeType: "string", amount: "string", appYear: "string", regnum: "string" } } } };

/**
 * WS100589_ListOfInvestmentRequests: ХӨРӨНГӨ ОРУУЛАХ ХҮСЭЛТИЙН ЖАГСААЛТ
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function listOfInvestmentRequests(transport: Transport, params: ListOfInvestmentRequestsParams = {}, options: CallOptions = {}): Promise<XypResult<ListOfInvestmentRequestsResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      appYear: params.appYear,
    },
    options,
  );
  return decodeResponse<ListOfInvestmentRequestsResponse>("ListOfInvestmentRequestsResponse", SCHEMA, data);
}
