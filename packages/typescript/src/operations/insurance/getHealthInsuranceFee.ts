/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100556_getHealthInsuranceFee (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100556_getHealthInsuranceFee";

/** Input of WS100556_getHealthInsuranceFee. */
export interface GetHealthInsuranceFeeParams {
  /** регистр */
  readonly regnum?: string;
}

/** мэдээллийн жагсаалт */
export interface GetHealthInsuranceFeeListData {
  /** Аймаг, дүүргийн код */
  readonly aimagCode: string | null;
  /** Аймаг, дүүргийн нэр */
  readonly aimagName: string | null;
  /** Даатгуулагчийн төрөл код */
  readonly benClass: string | null;
  /** Даатгуулагчийн төрөл нэр */
  readonly benClassName: string | null;
  /** Эхлэх огноо */
  readonly startDate: Date | string | null;
  /** Дуусах огноо */
  readonly endDate: Date | string | null;
  /** Нэр */
  readonly firstname: string | null;
  /** Овог */
  readonly lastname: string | null;
  /** Төрөл */
  readonly meta: number | null;
  /** Регистр */
  readonly regnum: string | null;
  /** Бүртгэсэн огноо */
  readonly regDate: Date | string | null;
}

/** WS100556_getHealthInsuranceFee: Эрүүл мэндийн даатгал шалгах */
export interface GetHealthInsuranceFeeResponse {
  /** мэдээллийн жагсаалт */
  readonly listData: readonly GetHealthInsuranceFeeListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { aimagCode: "string", aimagName: "string", benClass: "string", benClassName: "string", startDate: "date", endDate: "date", firstname: "string", lastname: "string", meta: "int", regnum: "string", regDate: "date" } } } };

/**
 * WS100556_getHealthInsuranceFee: Эрүүл мэндийн даатгал шалгах
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function getHealthInsuranceFee(transport: Transport, params: GetHealthInsuranceFeeParams = {}, options: CallOptions = {}): Promise<XypResult<GetHealthInsuranceFeeResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<GetHealthInsuranceFeeResponse>("GetHealthInsuranceFeeResponse", SCHEMA, data);
}
