/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100587_CheckInsuredEngInfo (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100587_CheckInsuredEngInfo";

/** Input of WS100587_CheckInsuredEngInfo. */
export interface CheckInsuredEngInfoParams {
  readonly regnum?: string;
  readonly civilId?: string;
}

/** WS100587_CheckInsuredEngInfo: Нийгмийн даатгал төлж байсан эсэх тодорхойлолт үйлчилгээг англи хэлээр */
export interface CheckInsuredEngInfoResponse {
  readonly status: boolean | null;
  readonly firstname: string | null;
  readonly lastname: string | null;
  readonly totalWorkMonth: number | null;
  readonly regnum: string | null;
  readonly civilid: string | null;
}

const SCHEMA: Schema = { status: "bool", firstname: "string", lastname: "string", totalWorkMonth: "int", regnum: "string", civilid: "string" };

/**
 * WS100587_CheckInsuredEngInfo: Нийгмийн даатгал төлж байсан эсэх тодорхойлолт үйлчилгээг англи хэлээр
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function checkInsuredEngInfo(transport: Transport, params: CheckInsuredEngInfoParams = {}, options: CallOptions = {}): Promise<XypResult<CheckInsuredEngInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      civilId: params.civilId,
    },
    options,
  );
  return decodeResponse<CheckInsuredEngInfoResponse>("CheckInsuredEngInfoResponse", SCHEMA, data);
}
