/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100543_retirementListInfo (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100543_retirementListInfo";

/** Input of WS100543_retirementListInfo. */
export interface RetirementListInfoParams {
  /** Регистрийн дугаар */
  readonly regnum?: string;
}

/** мэдээллийн жагсаалт */
export interface RetirementListInfoListData {
  /** Аймаг, дүүргийн нэр */
  readonly province: string | null;
  /** Регистр */
  readonly regnum: string | null;
  /** овог */
  readonly lastname: string | null;
  /** нар */
  readonly firstname: string | null;
  /** тэтгэврийн төрөл */
  readonly type: string | null;
  /** Өргөдөл өгсөн огноо */
  readonly appDate: Date | string | null;
  /** Дуусах огноо */
  readonly endDate: Date | string | null;
}

/** WS100543_retirementListInfo: Тэтгэврийн жагсаалт */
export interface RetirementListInfoResponse {
  /** мэдээллийн жагсаалт */
  readonly listData: readonly RetirementListInfoListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { province: "string", regnum: "string", lastname: "string", firstname: "string", type: "string", appDate: "date", endDate: "date" } } } };

/**
 * WS100543_retirementListInfo: Тэтгэврийн жагсаалт
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function retirementListInfo(transport: Transport, params: RetirementListInfoParams = {}, options: CallOptions = {}): Promise<XypResult<RetirementListInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<RetirementListInfoResponse>("RetirementListInfoResponse", SCHEMA, data);
}
