/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100537_pregnancyChildbirthInfo (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100537_pregnancyChildbirthInfo";

/** Input of WS100537_pregnancyChildbirthInfo. */
export interface PregnancyChildbirthInfoParams {
  /** Эмнэлгийн хуудас бичих програм код */
  readonly companyId?: number;
  /** Иргэний Эцэг /эх/-ийн нэр */
  readonly lastname?: string;
  /** Иргэний Нэр */
  readonly firsthname?: string;
  /** Иргэний регистрийн дугаар */
  readonly regnum?: string;
  /** Иргэний хаяг */
  readonly address?: string;
  /** Аж ахуйн нэгж байгууллагын Аймаг, дүүргийн код */
  readonly aimagCode?: string;
  /** Аж ахуйн нэгж байгууллагын код */
  readonly orgId?: string;
  /** Эмнэлгийн хуудасны төрлийн код */
  readonly medicalCertTypeId?: number;
  /** Эмнэлгийн хуудасны төрлийн нэр */
  readonly medicalCertTypeName?: string;
  /** Эхлэх огноо "yyyy-MM-dd" */
  readonly startDate?: string;
  /** Дуусах огноо "yyyy-MM-dd" */
  readonly endDate?: string;
  /** Эмнэлгийн хуудасны бичсэн огноо "yyyy-MM-dd" */
  readonly createdDate?: string;
  /** Эмнэлгийн хуудасны зассан огноо "yyyy-MM-dd" */
  readonly updatedDate?: string;
  /** Эмнэлгийн хуудас олгосон эмнэлгийн код */
  readonly hospitalId?: string;
  /** Эмнэлгийн хуудас олгосон эмнэлгийн нэр */
  readonly hospitalName?: string;
  /** Ерөнхий эмчийн код */
  readonly generalDoctorId?: string;
  /** Ерөнхий эмчийн нэр */
  readonly generalDoctorName?: string;
  /** Эмчлэгч эмчийн код */
  readonly confirmDoctorId?: string;
  /** Эмчлэгч эмчийн нэр */
  readonly confirmDoctorName?: string;
  /** утасны дугаар */
  readonly phone?: string;
  /** Жирэмсний долоо хоног */
  readonly pregnancyPeriod?: string;
  /** Өвчний түүхийн дугаар */
  readonly historyNumber?: string;
  /** Хэрэв гадаад улсад төрсөн бол улсын нэр */
  readonly counterName?: string;
  /** Эмнэлэг хяналтын комисийн шийдвэрийн дугаар. */
  readonly ehkDecisionNumber?: string;
  /** Ихэр хүүхэд мөн эсэх */
  readonly isTwins?: boolean;
}

/** WS100537_pregnancyChildbirthInfo: Жирэмсэн амаржсаны эмнэлгийн хуудас мэдээлэл авах */
export interface PregnancyChildbirthInfoResponse {
  /** Эмнэлгийн хуудасны дугаар */
  readonly medicalCertnumber: string | null;
}

const SCHEMA: Schema = { medicalCertnumber: "string" };

/**
 * WS100537_pregnancyChildbirthInfo: Жирэмсэн амаржсаны эмнэлгийн хуудас мэдээлэл авах
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function pregnancyChildbirthInfo(transport: Transport, params: PregnancyChildbirthInfoParams = {}, options: CallOptions = {}): Promise<XypResult<PregnancyChildbirthInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      companyId: params.companyId,
      lastname: params.lastname,
      firsthname: params.firsthname,
      regnum: params.regnum,
      address: params.address,
      aimagCode: params.aimagCode,
      orgId: params.orgId,
      medicalCertTypeId: params.medicalCertTypeId,
      medicalCertTypeName: params.medicalCertTypeName,
      startDate: params.startDate,
      endDate: params.endDate,
      createdDate: params.createdDate,
      updatedDate: params.updatedDate,
      hospitalId: params.hospitalId,
      hospitalName: params.hospitalName,
      generalDoctorId: params.generalDoctorId,
      generalDoctorName: params.generalDoctorName,
      confirmDoctorId: params.confirmDoctorId,
      confirmDoctorName: params.confirmDoctorName,
      phone: params.phone,
      pregnancyPeriod: params.pregnancyPeriod,
      historyNumber: params.historyNumber,
      counterName: params.counterName,
      ehkDecisionNumber: params.ehkDecisionNumber,
      isTwins: params.isTwins,
    },
    options,
  );
  return decodeResponse<PregnancyChildbirthInfoResponse>("PregnancyChildbirthInfoResponse", SCHEMA, data);
}
