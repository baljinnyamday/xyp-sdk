/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100549_disabilityBenefitsApply (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100549_disabilityBenefitsApply";

/** Input of WS100549_disabilityBenefitsApply. */
export interface DisabilityBenefitsApplyParams {
  /** Эмнэлгийн хуудасны дугаар */
  readonly medicalCertnumber?: string;
  /** Иргэний регистрийн дугаар */
  readonly regnum?: string;
  /** утасны дугаар */
  readonly phone?: string;
  /** И-мэйл хаяг */
  readonly email?: string;
  /** Ажилласан сарын тоо */
  readonly workMonth?: number;
  /** Тэтгэмж бодох хувь */
  readonly calculatePercent?: number;
  /** Банкны дугаар */
  readonly bankId?: number;
  /** Дансны дугаар */
  readonly bankAccount?: string;
}

/** WS100549_disabilityBenefitsApply: Хөдөлмөрийн чадвар түр алдсаны тэтгэмж авах хүсэлт илгээх */
export interface DisabilityBenefitsApplyResponse {
  /** 0 – success, 1 – error */
  readonly result: number | null;
  /** тайлбар */
  readonly resultMsg: string | null;
}

const SCHEMA: Schema = { result: "int", resultMsg: "string" };

/**
 * WS100549_disabilityBenefitsApply: Хөдөлмөрийн чадвар түр алдсаны тэтгэмж авах хүсэлт илгээх
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function disabilityBenefitsApply(transport: Transport, params: DisabilityBenefitsApplyParams = {}, options: CallOptions = {}): Promise<XypResult<DisabilityBenefitsApplyResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      medicalCertnumber: params.medicalCertnumber,
      regnum: params.regnum,
      phone: params.phone,
      email: params.email,
      workMonth: params.workMonth,
      calculatePercent: params.calculatePercent,
      bankId: params.bankId,
      bankAccount: params.bankAccount,
    },
    options,
  );
  return decodeResponse<DisabilityBenefitsApplyResponse>("DisabilityBenefitsApplyResponse", SCHEMA, data);
}
