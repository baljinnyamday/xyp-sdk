/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100512_checkIsInsuredInfo (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100512_checkIsInsuredInfo";

/** Input of WS100512_checkIsInsuredInfo. */
export interface CheckIsInsuredInfoParams {
  /** Регистрийн дугаар */
  readonly regnum?: string;
}

/** WS100512_checkIsInsuredInfo: Нийгмийн даатгалд даатгуусан эсэх */
export interface CheckIsInsuredInfoResponse {
  /** Даатгуулсан эсэх */
  readonly insured: boolean | null;
}

const SCHEMA: Schema = { insured: "bool" };

/**
 * WS100512_checkIsInsuredInfo: Нийгмийн даатгалд даатгуусан эсэх
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function checkIsInsuredInfo(transport: Transport, params: CheckIsInsuredInfoParams = {}, options: CallOptions = {}): Promise<XypResult<CheckIsInsuredInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<CheckIsInsuredInfoResponse>("CheckIsInsuredInfoResponse", SCHEMA, data);
}
