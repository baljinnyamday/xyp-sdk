/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS101502_InvestmentBankService (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS101502_InvestmentBankService";

/** Input of WS101502_InvestmentBankService. */
export interface InvestmentBankServiceParams {
  /** төлөв */
  readonly status?: boolean;
}

/** Жагсаалт */
export interface InvestmentBankServiceMonetizeTypeData {
  readonly code: string | null;
  readonly name: string | null;
  readonly status: string | null;
}

/** Жагсаалт */
export interface InvestmentBankServiceBankData {
  readonly id: number | null;
  readonly name: string | null;
  readonly code: string | null;
  readonly status: string | null;
}

/** WS101502_InvestmentBankService: ХӨРӨНГӨ ОРУУЛАХ АРИЛЖААНЫ БАНКУУД */
export interface InvestmentBankServiceResponse {
  /** Жагсаалт */
  readonly monetizeTypeData: readonly InvestmentBankServiceMonetizeTypeData[] | null;
  /** Жагсаалт */
  readonly bankData: readonly InvestmentBankServiceBankData[] | null;
}

const SCHEMA: Schema = { monetizeTypeData: { list: { object: { code: "string", name: "string", status: "string" } } }, bankData: { list: { object: { id: "int", name: "string", code: "string", status: "string" } } } };

/**
 * WS101502_InvestmentBankService: ХӨРӨНГӨ ОРУУЛАХ АРИЛЖААНЫ БАНКУУД
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function investmentBankService(transport: Transport, params: InvestmentBankServiceParams = {}, options: CallOptions = {}): Promise<XypResult<InvestmentBankServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      status: params.status,
    },
    options,
  );
  return decodeResponse<InvestmentBankServiceResponse>("InvestmentBankServiceResponse", SCHEMA, data);
}
