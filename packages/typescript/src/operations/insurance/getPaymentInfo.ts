/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100527_getPaymentInfo (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100527_getPaymentInfo";

/** Input of WS100527_getPaymentInfo. */
export interface GetPaymentInfoParams {
  /** регистрийн дугаар */
  readonly regnum?: string;
  /** иргэний дугаар */
  readonly civilId?: string;
  /** цахим шуудан */
  readonly email?: string;
}

/** WS100527_getPaymentInfo: Сайн дурын даатгалын төлбөрийн мэдээлэл */
export interface GetPaymentInfoResponse {
  /** Нэхэмжлжхийн дугаар */
  readonly invoiceNumber: string | null;
  /** Регистрийн дугаар */
  readonly regnum: string | null;
  /** Нэхэмжлэхийн огноо */
  readonly invoiceDate: string | null;
  /** Хийх гүйлгээний жагсаалт */
  readonly transactions: readonly unknown[] | null;
  /** Гүйлгээ хийх боломжтой дансны жагсаалт */
  readonly accountData: readonly unknown[] | null;
  /** Төлбөр төлөх хуваарь */
  readonly schedules: readonly unknown[] | null;
  /** Нийгмийн даатгалын сайн дурын гэрээний мэдээлэл */
  readonly contract: readonly unknown[] | null;
}

const SCHEMA: Schema = { invoiceNumber: "string", regnum: "string", invoiceDate: "string", transactions: { list: "any" }, accountData: { list: "any" }, schedules: { list: "any" }, contract: { list: "any" } };

/**
 * WS100527_getPaymentInfo: Сайн дурын даатгалын төлбөрийн мэдээлэл
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function getPaymentInfo(transport: Transport, params: GetPaymentInfoParams = {}, options: CallOptions = {}): Promise<XypResult<GetPaymentInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      civilId: params.civilId,
      email: params.email,
    },
    options,
  );
  return decodeResponse<GetPaymentInfoResponse>("GetPaymentInfoResponse", SCHEMA, data);
}
