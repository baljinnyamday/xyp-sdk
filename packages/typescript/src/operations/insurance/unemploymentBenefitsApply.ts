/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100553_unemploymentBenefitsApply (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100553_unemploymentBenefitsApply";

/** Input of WS100553_unemploymentBenefitsApply. */
export interface UnemploymentBenefitsApplyParams {
  /** Ургийн овог */
  readonly surname?: string;
  /** Эцэг /эх/-ийн нэр */
  readonly lastname?: string;
  /** Нэр */
  readonly firstname?: string;
  /** Иргэний регистрийн дугаар */
  readonly regnum?: string;
  /** Хаяг */
  readonly address?: string;
  /** Утасны дугаар */
  readonly phone?: string;
  /** И-мэйл хаяг */
  readonly email?: string;
  /** Ажилгүйдлийн даатгалын санд шимтгэл төлсөн нийт сарын тоо */
  readonly workMonth?: number;
  /** Ажилгүйдлийн тэтгэмж бодох хувь */
  readonly calculatePercent?: number;
  /** Оршин суургаа хаягт бүртгэлтэй аймаг, нийслэлийн код */
  readonly aimagCode?: number;
  /** Оршин суургаа хаягт бүртгэлтэй сум, дүүргийн код */
  readonly soumCode?: number;
  /** Банк код */
  readonly bankId?: number;
  /** Дансны дугаар */
  readonly bankAccount?: string;
  /** Ажлаас чөлөөлөгдсөн тушаал base64 */
  readonly dismissalOrder?: string;
  /** Ажил олгогчтой тооцоо хийж дууссан огноо */
  readonly handoverDate?: string;
  /** Ажил олгогчтой тооцоо хийж дууссан тушаал */
  readonly handoverOrder?: string;
  /** ХХҮЕГ-н хөдөлмөр зохицуулалтын албанд бүртгүүлсэн огноо. */
  readonly registrationDate?: string;
}

/** WS100553_unemploymentBenefitsApply: Ажилгүйдлийн тэтгэмж авах хүсэлт илгээх */
export interface UnemploymentBenefitsApplyResponse {
  /** 0 – success, 1 – error */
  readonly result: number | null;
  /** тайлбар */
  readonly resultMsg: string | null;
}

const SCHEMA: Schema = { result: "int", resultMsg: "string" };

/**
 * WS100553_unemploymentBenefitsApply: Ажилгүйдлийн тэтгэмж авах хүсэлт илгээх
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function unemploymentBenefitsApply(transport: Transport, params: UnemploymentBenefitsApplyParams = {}, options: CallOptions = {}): Promise<XypResult<UnemploymentBenefitsApplyResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      surname: params.surname,
      lastname: params.lastname,
      firstname: params.firstname,
      regnum: params.regnum,
      address: params.address,
      phone: params.phone,
      email: params.email,
      workMonth: params.workMonth,
      calculatePercent: params.calculatePercent,
      aimagCode: params.aimagCode,
      soumCode: params.soumCode,
      bankId: params.bankId,
      bankAccount: params.bankAccount,
      dismissalOrder: params.dismissalOrder,
      handoverDate: params.handoverDate,
      handoverOrder: params.handoverOrder,
      registrationDate: params.registrationDate,
    },
    options,
  );
  return decodeResponse<UnemploymentBenefitsApplyResponse>("UnemploymentBenefitsApplyResponse", SCHEMA, data);
}
