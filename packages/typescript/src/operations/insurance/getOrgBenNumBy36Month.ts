/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100585_GetOrgBenNumBy36Month (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100585_GetOrgBenNumBy36Month";

/** Input of WS100585_GetOrgBenNumBy36Month. */
export interface GetOrgBenNumBy36MonthParams {
  /** Захирал (нябо)-ийн регистрийн дугаар */
  readonly regnum?: string;
  /** Ажил олгогчийн регистрийн дугаар */
  readonly orgRegnum?: string;
  /** Ажил олгогчийн НД-ийн бүртгэлийн дугаар */
  readonly orgId?: string;
}

/** жагсаалт */
export interface GetOrgBenNumBy36MonthList {
  /** Гадаад даатгуулагчийн тоо */
  readonly benNumberFN: number | null;
  /** Монгол даатгуулагчийн тоо */
  readonly bemNumberMN: number | null;
  /** Сар */
  readonly month: number | null;
  /** НДШ-ийн өр */
  readonly shim: number | null;
  /** Он */
  readonly year: number | null;
}

/** WS100585_GetOrgBenNumBy36Month: Ажил олгогчийн даатгуулагчийн тоо, өрийн мэдээлэл (сүүлийн 36 сар) */
export interface GetOrgBenNumBy36MonthResponse {
  /** жагсаалт */
  readonly list: readonly GetOrgBenNumBy36MonthList[] | null;
}

const SCHEMA: Schema = { list: { list: { object: { benNumberFN: "int", bemNumberMN: "int", month: "int", shim: "float", year: "int" } } } };

/**
 * WS100585_GetOrgBenNumBy36Month: Ажил олгогчийн даатгуулагчийн тоо, өрийн мэдээлэл (сүүлийн 36 сар)
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function getOrgBenNumBy36Month(transport: Transport, params: GetOrgBenNumBy36MonthParams = {}, options: CallOptions = {}): Promise<XypResult<GetOrgBenNumBy36MonthResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      orgRegnum: params.orgRegnum,
      orgId: params.orgId,
    },
    options,
  );
  return decodeResponse<GetOrgBenNumBy36MonthResponse>("GetOrgBenNumBy36MonthResponse", SCHEMA, data);
}
