/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100501_getCitizenSalaryInfo (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100501_getCitizenSalaryInfo";

/** Input of WS100501_getCitizenSalaryInfo. */
export interface GetCitizenSalaryInfoParams {
  /** Регистрийн дугаар */
  readonly regnum?: string;
  /** Эхлэх огноо */
  readonly startYear?: number;
  /** Дуусах огноо */
  readonly endYear?: number;
}

/** Иргэний цалингийн мэдээлэл */
export interface GetCitizenSalaryInfoList {
  /** төлөгдсөн эсэх */
  readonly isPaid: boolean | null;
  /** Байгууллагын нэр */
  readonly orgName: string | null;
  /** Нийгмийн даатгал дээрх бүртгэлтэй байгууллагын ID дугаар */
  readonly orgSiID: string | null;
  /** цалин */
  readonly salaryAmount: number | null;
  /** шимтгэл */
  readonly salaryFee: number | null;
  /** төлөгдсөн жил */
  readonly year: number | null;
  /** төлөгдсөн сар */
  readonly month: number | null;
  /** хэлтэсийн нэр */
  readonly domName: string | null;
  /** даатгуулагчийн төрөл */
  readonly benClass: string | null;
}

/** WS100501_getCitizenSalaryInfo: Иргэний нийгмийн даатгалын мэдээлэл дамжуулах сервис */
export interface GetCitizenSalaryInfoResponse {
  /** Иргэний цалингийн мэдээлэл */
  readonly list: readonly GetCitizenSalaryInfoList[] | null;
}

const SCHEMA: Schema = { list: { list: { object: { isPaid: "bool", orgName: "string", orgSiID: "string", salaryAmount: "float", salaryFee: "float", year: "int", month: "int", domName: "string", benClass: "string" } } } };

/**
 * WS100501_getCitizenSalaryInfo: Иргэний нийгмийн даатгалын мэдээлэл дамжуулах сервис
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function getCitizenSalaryInfo(transport: Transport, params: GetCitizenSalaryInfoParams = {}, options: CallOptions = {}): Promise<XypResult<GetCitizenSalaryInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      startYear: params.startYear,
      endYear: params.endYear,
    },
    options,
  );
  return decodeResponse<GetCitizenSalaryInfoResponse>("GetCitizenSalaryInfoResponse", SCHEMA, data);
}
