/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100586_OrgHasDebt (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100586_OrgHasDebt";

/** Input of WS100586_OrgHasDebt. */
export interface OrgHasDebtParams {
  /** Ажил олгогчийн регистрийн дугаар */
  readonly orgRegnum?: string;
}

/** WS100586_OrgHasDebt: Ажил олгогч НДШ-ийн өртэй эсэх */
export interface OrgHasDebtResponse {
  /** өртэй эсэх */
  readonly status: boolean | null;
}

const SCHEMA: Schema = { status: "bool" };

/**
 * WS100586_OrgHasDebt: Ажил олгогч НДШ-ийн өртэй эсэх
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function orgHasDebt(transport: Transport, params: OrgHasDebtParams = {}, options: CallOptions = {}): Promise<XypResult<OrgHasDebtResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      orgRegnum: params.orgRegnum,
    },
    options,
  );
  return decodeResponse<OrgHasDebtResponse>("OrgHasDebtResponse", SCHEMA, data);
}
