/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS101501_InvestmentApplicationService (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS101501_InvestmentApplicationService";

/** Input of WS101501_InvestmentApplicationService. */
export interface InvestmentApplicationServiceParams {
  /** Регистрийн дугаар */
  readonly regnum?: string;
  /** Он */
  readonly appYear?: number;
  /** Мөнгөжүүлсэн дүн */
  readonly amount?: number;
  /** Хөрөнгө оруулах санхүүгийн хэрэгсэл сонгосон code */
  readonly monetizeType?: string;
  /** Хөрөнгө оруулах арилжааны банкууд сонгосон банк id */
  readonly bankId?: number;
}

/** WS101501_InvestmentApplicationService: ХӨРӨНГӨ ОРУУЛАХ ХҮСЭЛТ ИЛГЭЭХ */
export interface InvestmentApplicationServiceResponse {
  /** Регистр дугаар */
  readonly regnum: string | null;
  /** Банкны дугаар */
  readonly bankId: number | null;
  /** Хүсэлт гаргасан он */
  readonly appYear: number | null;
  /** Хөрөнгө оруулалтын дүн */
  readonly amount: number | null;
  /** Санхүүжилтийн төрөл */
  readonly monetizeType: string | null;
  /** Хүсэлтийн төлөв */
  readonly status: string | null;
}

const SCHEMA: Schema = { regnum: "string", bankId: "int", appYear: "int", amount: "int", monetizeType: "string", status: "string" };

/**
 * WS101501_InvestmentApplicationService: ХӨРӨНГӨ ОРУУЛАХ ХҮСЭЛТ ИЛГЭЭХ
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function investmentApplicationService(transport: Transport, params: InvestmentApplicationServiceParams = {}, options: CallOptions = {}): Promise<XypResult<InvestmentApplicationServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      appYear: params.appYear,
      amount: params.amount,
      monetizeType: params.monetizeType,
      bankId: params.bankId,
    },
    options,
  );
  return decodeResponse<InvestmentApplicationServiceResponse>("InvestmentApplicationServiceResponse", SCHEMA, data);
}
