/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100563_funeralAllowanceInfo (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100563_funeralAllowanceInfo";

/** Input of WS100563_funeralAllowanceInfo. */
export interface FuneralAllowanceInfoParams {
  /** Регистрийн дугаар */
  readonly regnum?: string;
}

/** WS100563_funeralAllowanceInfo: Нийгмийн даатгалын сангаас оршуулгын тэтгэмж авсан эсэх */
export interface FuneralAllowanceInfoResponse {
  /** оршуулгын тэтгэмж авсан эсэх */
  readonly isReceived: boolean | null;
}

const SCHEMA: Schema = { isReceived: "bool" };

/**
 * WS100563_funeralAllowanceInfo: Нийгмийн даатгалын сангаас оршуулгын тэтгэмж авсан эсэх
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function funeralAllowanceInfo(transport: Transport, params: FuneralAllowanceInfoParams = {}, options: CallOptions = {}): Promise<XypResult<FuneralAllowanceInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<FuneralAllowanceInfoResponse>("FuneralAllowanceInfoResponse", SCHEMA, data);
}
