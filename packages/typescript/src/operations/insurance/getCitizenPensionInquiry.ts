/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100502_getCitizenPensionInquiry (insurance, Нийгмийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100502_getCitizenPensionInquiry";

/** Input of WS100502_getCitizenPensionInquiry. */
export interface GetCitizenPensionInquiryParams {
  readonly regnum?: string;
  /** иргэний дугаар */
  readonly civilId?: string;
  readonly startYear?: number;
  readonly endYear?: number;
}

/** WS100502_getCitizenPensionInquiry: Тэтгэвэрийн лавлагааны мэдээлэл дамжуулах сервис */
export interface GetCitizenPensionInquiryResponse {
  /** Ургийн овог */
  readonly surname: string | null;
  /** Регистрийн дугаар */
  readonly regnum: string | null;
  readonly isPensioner: boolean | null;
  /** Овог */
  readonly lastName: string | null;
  /** Нэр */
  readonly firstName: string | null;
}

const SCHEMA: Schema = { surname: "string", regnum: "string", isPensioner: "bool", lastName: "string", firstName: "string" };

/**
 * WS100502_getCitizenPensionInquiry: Тэтгэвэрийн лавлагааны мэдээлэл дамжуулах сервис
 *
 * Provider: Нийгмийн даатгалын ерөнхий газар
 */
export async function getCitizenPensionInquiry(transport: Transport, params: GetCitizenPensionInquiryParams = {}, options: CallOptions = {}): Promise<XypResult<GetCitizenPensionInquiryResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      civilId: params.civilId,
      startYear: params.startYear,
      endYear: params.endYear,
    },
    options,
  );
  return decodeResponse<GetCitizenPensionInquiryResponse>("GetCitizenPensionInquiryResponse", SCHEMA, data);
}
