/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100519_checkChildMoney (insurance, Хөдөлмөр халамж, үйлчилгээний ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100519_checkChildMoney";

/** Input of WS100519_checkChildMoney. */
export interface CheckChildMoneyParams {
  /** регистрийн дугаар */
  readonly regnum?: string;
}

/** WS100519_checkChildMoney: Хүүхдийн мөнгө шалгах */
export interface CheckChildMoneyResponse {
  /** Мэдээлэл */
  readonly info: string | null;
}

const SCHEMA: Schema = { info: "string" };

/**
 * WS100519_checkChildMoney: Хүүхдийн мөнгө шалгах
 *
 * Provider: Хөдөлмөр халамж, үйлчилгээний ерөнхий газар
 */
export async function checkChildMoney(transport: Transport, params: CheckChildMoneyParams = {}, options: CallOptions = {}): Promise<XypResult<CheckChildMoneyResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<CheckChildMoneyResponse>("CheckChildMoneyResponse", SCHEMA, data);
}
