/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100301_getLegalEntityInfo (legalEntity, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100301_getLegalEntityInfo";

/** Input of WS100301_getLegalEntityInfo. */
export interface GetLegalEntityInfoParams {
  readonly legalEntityNumber?: number;
}

/** Хувьцаа эзэмшигч, гишүүний мэдээлэл */
export interface GetLegalEntityInfoFounder {
  /** Бүртгэлийн дугаар */
  readonly registeredNumber: string | null;
}

/** Эрх барих этгээдийн тухай мэдээлэл */
export interface GetLegalEntityInfoStakeHolders {
  /** Бүртгэлийн дугаар */
  readonly registeredNumber: string | null;
}

/** Үүсгэн байгуулагчийн тухай мэдээлэл */
export interface GetLegalEntityInfoShareHolders {
  /** Бүртгэлийн дугаар */
  readonly registeredNumber: string | null;
}

/** Захирлын мэдээлэл */
export interface GetLegalEntityInfoGeneralR {
  /** Бүртгэлийн дугаар */
  readonly registeredNumber: string | null;
}

/** WS100301_getLegalEntityInfo: Хуулийн этгээдийн мэдээлэл дамжуулах сервис */
export interface GetLegalEntityInfoResponse {
  /** Овог */
  readonly firstName: string | null;
  /** Нэр */
  readonly lastName: string | null;
  /** Регистрийн дугаар */
  readonly regnum: string | null;
  /** Ургийн овог */
  readonly surname: string | null;
  /** Ерөнхий мэдээлэл */
  readonly general: readonly unknown[] | null;
  /** Хаягийн мэдээлэл */
  readonly address: readonly unknown[] | null;
  /** Хувьцаа эзэмшигч, гишүүний мэдээлэл */
  readonly founder: readonly GetLegalEntityInfoFounder[] | null;
  /** Үйл ажиллагааны чиглэл */
  readonly induty: readonly unknown[] | null;
  /** Хөрөнгийн мэдээлэл */
  readonly capital: readonly unknown[] | null;
  /** Эрх барих этгээдийн тухай мэдээлэл */
  readonly stakeHolders: readonly GetLegalEntityInfoStakeHolders[] | null;
  /** Үүсгэн байгуулагчийн тухай мэдээлэл */
  readonly shareHolders: readonly GetLegalEntityInfoShareHolders[] | null;
  /** Захирлын мэдээлэл */
  readonly generalR: readonly GetLegalEntityInfoGeneralR[] | null;
  /** Салбарын мэдээлэл */
  readonly branch: readonly unknown[] | null;
  /** Татан буугдсан мэдээлэл */
  readonly liquidation: readonly unknown[] | null;
  /** Өөрчлөлтийн мэдээлэл */
  readonly changeFond: readonly unknown[] | null;
  /** Өөрчлөлтийн мэдээлэл */
  readonly changeName: readonly unknown[] | null;
}

const SCHEMA: Schema = {
  firstName: "string",
  lastName: "string",
  regnum: "string",
  surname: "string",
  general: { list: "any" },
  address: { list: "any" },
  founder: { list: { object: { registeredNumber: "string" } } },
  induty: { list: "any" },
  capital: { list: "any" },
  stakeHolders: { list: { object: { registeredNumber: "string" } } },
  shareHolders: { list: { object: { registeredNumber: "string" } } },
  generalR: { list: { object: { registeredNumber: "string" } } },
  branch: { list: "any" },
  liquidation: { list: "any" },
  changeFond: { list: "any" },
  changeName: { list: "any" },
};

/**
 * WS100301_getLegalEntityInfo: Хуулийн этгээдийн мэдээлэл дамжуулах сервис
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function getLegalEntityInfo(transport: Transport, params: GetLegalEntityInfoParams = {}, options: CallOptions = {}): Promise<XypResult<GetLegalEntityInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      legalEntityNumber: params.legalEntityNumber,
    },
    options,
  );
  return decodeResponse<GetLegalEntityInfoResponse>("GetLegalEntityInfoResponse", SCHEMA, data);
}
