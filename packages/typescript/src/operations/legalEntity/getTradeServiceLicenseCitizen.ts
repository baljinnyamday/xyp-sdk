/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100340_getTradeServiceLicenseCitizen (legalEntity, Нийслэлийн засаг даргын тамгын газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100340_getTradeServiceLicenseCitizen";

/** Input of WS100340_getTradeServiceLicenseCitizen. */
export interface GetTradeServiceLicenseCitizenParams {
  /** регистрийн дугаар */
  readonly regnum?: string;
}

/** Мэдээллийн жагсаалт */
export interface GetTradeServiceLicenseCitizenListData {
  /** Эхлэх огноо */
  readonly startDate: Date | string | null;
  /** Иргэн, хуулийн этгээдийн төрөл */
  readonly type: string | null;
  /** Үйлчилгээний нэр */
  readonly serviceName: string | null;
  /** Зөвшөөрлийн төлөв */
  readonly statusName: string | null;
  /** Иргэний нэр */
  readonly citizenName: string | null;
  /** Иргэний регистрийн дугаар */
  readonly citizenRegnum: string | null;
  /** Хаяг өргөрөг */
  readonly latitude: number | null;
  /** Хаягийн уртраг */
  readonly longitude: number | null;
  /** Хаягийн мэдээлэл */
  readonly address: string | null;
}

/** WS100340_getTradeServiceLicenseCitizen: Иргэний худалдаа, үйлчилгээ эрхлэх бүртгэл шалгах сервис */
export interface GetTradeServiceLicenseCitizenResponse {
  /** Мэдээллийн жагсаалт */
  readonly listData: readonly GetTradeServiceLicenseCitizenListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { startDate: "date", type: "string", serviceName: "string", statusName: "string", citizenName: "string", citizenRegnum: "string", latitude: "float", longitude: "float", address: "string" } } } };

/**
 * WS100340_getTradeServiceLicenseCitizen: Иргэний худалдаа, үйлчилгээ эрхлэх бүртгэл шалгах сервис
 *
 * Provider: Нийслэлийн засаг даргын тамгын газар
 */
export async function getTradeServiceLicenseCitizen(transport: Transport, params: GetTradeServiceLicenseCitizenParams = {}, options: CallOptions = {}): Promise<XypResult<GetTradeServiceLicenseCitizenResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<GetTradeServiceLicenseCitizenResponse>("GetTradeServiceLicenseCitizenResponse", SCHEMA, data);
}
