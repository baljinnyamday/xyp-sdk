/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100323_legalEntityNonPaymentInfo (legalEntity, Шүүхийн Шийдвэр Гүйцэтгэх Ерөнхий Газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100323_legalEntityNonPaymentInfo";

/** Input of WS100323_legalEntityNonPaymentInfo. */
export interface LegalEntityNonPaymentInfoParams {
  /** Хуулийн этгээдийн дугаар */
  readonly legalEntityNumber?: number;
  /** регистрийн дугаар */
  readonly regnum?: string;
}

/** WS100323_legalEntityNonPaymentInfo: Хуулийн этгээдийн шүүхийн шийдвэрээр өртэй эсэх */
export interface LegalEntityNonPaymentInfoResponse {
  /** Компаний регистрийн дугаар */
  readonly companyRegnum: string | null;
  /** Компаний нэр */
  readonly companyName: string | null;
  /** гүйцэтгэх хуудасны дугаар */
  readonly sheetNum: string | null;
  /** Хувийн хэргийн дугаар */
  readonly caseNum: string | null;
  /** Шүүхийн шийдвэрийн төрөл */
  readonly decisionTypeName: string | null;
  /** Шийдвэрийн дугаар */
  readonly decisionNum: string | null;
  /** Шийдвэрийн огноо */
  readonly decisionDate: Date | string | null;
  /** Хүлээн авсан огноо */
  readonly receivedDate: Date | string | null;
  /** Хувийн хэргийн төлөв ID */
  readonly caseStatusId: string | null;
  /** Хувийн хэргийн төлөв */
  readonly caseStatusName: string | null;
  /** Төлөгдсөн мөнгөн дүн */
  readonly tollBalance: string | null;
}

const SCHEMA: Schema = { companyRegnum: "string", companyName: "string", sheetNum: "string", caseNum: "string", decisionTypeName: "string", decisionNum: "string", decisionDate: "date", receivedDate: "date", caseStatusId: "string", caseStatusName: "string", tollBalance: "string" };

/**
 * WS100323_legalEntityNonPaymentInfo: Хуулийн этгээдийн шүүхийн шийдвэрээр өртэй эсэх
 *
 * Provider: Шүүхийн Шийдвэр Гүйцэтгэх Ерөнхий Газар
 */
export async function legalEntityNonPaymentInfo(transport: Transport, params: LegalEntityNonPaymentInfoParams = {}, options: CallOptions = {}): Promise<XypResult<LegalEntityNonPaymentInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      legalEntityNumber: params.legalEntityNumber,
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<LegalEntityNonPaymentInfoResponse>("LegalEntityNonPaymentInfoResponse", SCHEMA, data);
}
