/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100331_citizenLegalEntityInfoList (legalEntity, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100331_citizenLegalEntityInfoList";

/** Input of WS100331_citizenLegalEntityInfoList. */
export interface CitizenLegalEntityInfoListParams {
  /** иргэний регистрийн дугаар */
  readonly regnum?: string;
}

/** мэдээллийн жагсаалт */
export interface CitizenLegalEntityInfoListListData {
  /** Хуулийн этгээдийн регистрийн дугаа */
  readonly orgRegnum: string | null;
  /** Хуулийн этгээдийн нэр */
  readonly orgName: string | null;
  /** хуулийн этгээдийн үүсгэн байгууллагдсан огноо */
  readonly orgDate: string | null;
}

/** WS100331_citizenLegalEntityInfoList: Иргэний нэр дээр бүртгэлтэй байгууллагын жагсаалт буцаах сервис */
export interface CitizenLegalEntityInfoListResponse {
  /** мэдээллийн жагсаалт */
  readonly listData: readonly CitizenLegalEntityInfoListListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { orgRegnum: "string", orgName: "string", orgDate: "string" } } } };

/**
 * WS100331_citizenLegalEntityInfoList: Иргэний нэр дээр бүртгэлтэй байгууллагын жагсаалт буцаах сервис
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function citizenLegalEntityInfoList(transport: Transport, params: CitizenLegalEntityInfoListParams = {}, options: CallOptions = {}): Promise<XypResult<CitizenLegalEntityInfoListResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<CitizenLegalEntityInfoListResponse>("CitizenLegalEntityInfoListResponse", SCHEMA, data);
}
