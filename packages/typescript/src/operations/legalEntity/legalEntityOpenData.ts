/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100316_legalEntityOpenData (legalEntity, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100316_legalEntityOpenData";

/** Input of WS100316_legalEntityOpenData. */
export interface LegalEntityOpenDataParams {
  /** Хуулийн этгээдийн дугаар */
  readonly legalEntityNumber?: number;
  /** регистрийн дугаар */
  readonly regnum?: string;
}

/** WS100316_legalEntityOpenData: Хуулийн этгээдийн нээлттэй мэдээлэл */
export interface LegalEntityOpenDataResponse {
  /** Ерөнхий мэдээлэл */
  readonly general: unknown | null;
  /** Хаягийн мэдээлэл */
  readonly address: readonly unknown[] | null;
  /** Хувьцаа эзэмшигч, гишүүний мэдээлэл */
  readonly founder: readonly unknown[] | null;
  /** Үйл ажиллагааны чиглэл */
  readonly induty: readonly unknown[] | null;
  /** Захирлын мэдээлэл */
  readonly generalR: unknown | null;
}

const SCHEMA: Schema = { general: "any", address: { list: "any" }, founder: { list: "any" }, induty: { list: "any" }, generalR: "any" };

/**
 * WS100316_legalEntityOpenData: Хуулийн этгээдийн нээлттэй мэдээлэл
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function legalEntityOpenData(transport: Transport, params: LegalEntityOpenDataParams = {}, options: CallOptions = {}): Promise<XypResult<LegalEntityOpenDataResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      legalEntityNumber: params.legalEntityNumber,
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<LegalEntityOpenDataResponse>("LegalEntityOpenDataResponse", SCHEMA, data);
}
