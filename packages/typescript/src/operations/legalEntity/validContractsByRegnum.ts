/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS100348_ValidContractsByRegnum (legalEntity, Нийслэлийн засаг даргын тамгын газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS100348_ValidContractsByRegnum";

/** Input of WS100348_ValidContractsByRegnum. */
export interface ValidContractsByRegnumParams {
  /** Регистр */
  readonly regnum?: string;
}

/** Жагсаалт */
export interface ValidContractsByRegnumListData {
  /** contract id */
  readonly id: string | null;
  /** гэрээний нэр */
  readonly title: string | null;
  /** COMPLETED гэх мэт */
  readonly status: string | null;
  /** Баталгаажсан */
  readonly statusDisplayText: string | null;
  /** гэрээ pdf */
  readonly contractPdfUrl: string | null;
  /** certificate pdf */
  readonly certificatePdfUrl: string | null;
  /** дуусах огноо */
  readonly expireDate: string | null;
  /** хүчинтэй огноо */
  readonly validDate: string | null;
  /** FREE гэх мэт */
  readonly paymentType: string | null;
  /** Жагсаалт */
  readonly participantList: readonly unknown[] | null;
}

/** WS100348_ValidContractsByRegnum: Иргэн болон байгууллагын регистрийн дугаараар хүчинтэй гэрээний жагсаалт авах сервис */
export interface ValidContractsByRegnumResponse {
  /** Жагсаалт */
  readonly listData: readonly ValidContractsByRegnumListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { id: "string", title: "string", status: "string", statusDisplayText: "string", contractPdfUrl: "string", certificatePdfUrl: "string", expireDate: "string", validDate: "string", paymentType: "string", participantList: { list: "any" } } } } };

/**
 * WS100348_ValidContractsByRegnum: Иргэн болон байгууллагын регистрийн дугаараар хүчинтэй гэрээний жагсаалт авах сервис
 *
 * Provider: Нийслэлийн засаг даргын тамгын газар
 */
export async function validContractsByRegnum(transport: Transport, params: ValidContractsByRegnumParams = {}, options: CallOptions = {}): Promise<XypResult<ValidContractsByRegnumResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<ValidContractsByRegnumResponse>("ValidContractsByRegnumResponse", SCHEMA, data);
}
