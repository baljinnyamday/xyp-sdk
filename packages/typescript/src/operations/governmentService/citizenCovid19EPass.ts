/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * GS10002_citizenCovid19EPass (governmentService, Эрүүл мэндийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "GS10002_citizenCovid19EPass";

/** Input of GS10002_citizenCovid19EPass. */
export interface CitizenCovid19EPassParams {
  /** регистрийн дугаар */
  readonly regnum?: string;
}

/** GS10002_citizenCovid19EPass: Иргэний ковид 19 цахим гэрчилгээ */
export interface CitizenCovid19EPassResponse {
  /** цахим мэдээлэл */
  readonly qrData: string | null;
  /** дархлаажуулалтын төлөв 0. Дархлаажсан 1. Хязгаарлагдмал дархлаатай 2. Дархлаажаагүй */
  readonly status: number | null;
}

const SCHEMA: Schema = { qrData: "string", status: "int" };

/**
 * GS10002_citizenCovid19EPass: Иргэний ковид 19 цахим гэрчилгээ
 *
 * Provider: Эрүүл мэндийн яам
 */
export async function citizenCovid19EPass(transport: Transport, params: CitizenCovid19EPassParams = {}, options: CallOptions = {}): Promise<XypResult<CitizenCovid19EPassResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<CitizenCovid19EPassResponse>("CitizenCovid19EPassResponse", SCHEMA, data);
}
