/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * GS10058_CreateFeedbackSession (governmentService, Засгийн газрын Хэрэг эрхлэх газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "GS10058_CreateFeedbackSession";

/** Input of GS10058_CreateFeedbackSession. */
export interface CreateFeedbackSessionParams {
  /** isUpdate талбар 1 үед заавал явуулна. */
  readonly sessionId?: number;
  readonly khorooId?: number;
  readonly districtId?: number;
  readonly channelId?: number;
  readonly typeId?: number;
  readonly clientFirstname?: string;
  readonly clientRegnum?: string;
  readonly clientLastname?: string;
  /** 1 бол өөрчлөлт, 0 бол нэмнэ. */
  readonly isUpdate?: number;
  readonly addressDetail?: string;
  readonly body?: string;
  readonly url?: string;
  readonly description?: string;
  readonly clientTypeId?: number;
  readonly userFullName?: string;
  readonly userJobTittle?: string;
  readonly provinceId?: number;
}

/** GS10058_CreateFeedbackSession: Өргөдөл, гомдол бүртгэх */
export interface CreateFeedbackSessionResponse {
  readonly sessionId: number | null;
}

const SCHEMA: Schema = { sessionId: "int" };

/**
 * GS10058_CreateFeedbackSession: Өргөдөл, гомдол бүртгэх
 *
 * Provider: Засгийн газрын Хэрэг эрхлэх газар
 */
export async function createFeedbackSession(transport: Transport, params: CreateFeedbackSessionParams = {}, options: CallOptions = {}): Promise<XypResult<CreateFeedbackSessionResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      sessionId: params.sessionId,
      khorooId: params.khorooId,
      districtId: params.districtId,
      channelId: params.channelId,
      typeId: params.typeId,
      clientFirstname: params.clientFirstname,
      clientRegnum: params.clientRegnum,
      clientLastname: params.clientLastname,
      isUpdate: params.isUpdate,
      addressDetail: params.addressDetail,
      body: params.body,
      url: params.url,
      description: params.description,
      clientTypeId: params.clientTypeId,
      userFullName: params.userFullName,
      userJobTittle: params.userJobTittle,
      provinceId: params.provinceId,
    },
    options,
  );
  return decodeResponse<CreateFeedbackSessionResponse>("CreateFeedbackSessionResponse", SCHEMA, data);
}
