/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * GS10066_CreateComplaintRating (governmentService, Засгийн газрын Хэрэг эрхлэх газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "GS10066_CreateComplaintRating";

/** Input of GS10066_CreateComplaintRating. */
export interface CreateComplaintRatingParams {
  readonly sessionId?: number;
  readonly lastDepartment?: string;
  readonly rating?: number;
  readonly description?: string;
  readonly ratingReasonIds?: readonly unknown[];
}

/** GS10066_CreateComplaintRating: Санал асуулга Үнэлгээ өгөх */
export interface CreateComplaintRatingResponse {
  readonly ratingId: number | null;
}

const SCHEMA: Schema = { ratingId: "int" };

/**
 * GS10066_CreateComplaintRating: Санал асуулга Үнэлгээ өгөх
 *
 * Provider: Засгийн газрын Хэрэг эрхлэх газар
 */
export async function createComplaintRating(transport: Transport, params: CreateComplaintRatingParams = {}, options: CallOptions = {}): Promise<XypResult<CreateComplaintRatingResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      sessionId: params.sessionId,
      lastDepartment: params.lastDepartment,
      rating: params.rating,
      description: params.description,
      ratingReasonIds: params.ratingReasonIds,
    },
    options,
  );
  return decodeResponse<CreateComplaintRatingResponse>("CreateComplaintRatingResponse", SCHEMA, data);
}
