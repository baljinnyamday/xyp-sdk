/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * GS10073_CitizenLegalEntityWeapons (governmentService, Шүүхийн Шинжилгээний Ерөнхий Газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "GS10073_CitizenLegalEntityWeapons";

/** Input of GS10073_CitizenLegalEntityWeapons. */
export interface CitizenLegalEntityWeaponsParams {
  /** регистр */
  readonly regnum?: string;
  /** gun, airGun, specialTool */
  readonly authType?: string;
}

/** Жагсаалт */
export interface CitizenLegalEntityWeaponsListData {
  /** Гол төмөр, их биеийн дугаар */
  readonly bodyNum: string | null;
  /** Бүртгэлийн дугаар */
  readonly regNumber: string | null;
  /** Бүртгүүлсэн он, сар, өдөр */
  readonly createdAt: string | null;
  /** Тодорхойлолтын төрөл */
  readonly regType: string | null;
  /** Тодорхойлолтын гарчиг */
  readonly regTitle: string | null;
}

/** GS10073_CitizenLegalEntityWeapons: Иргэн (Хуулийн этгээд) -н эмшдэг гал зэвсэг, хийн буу, тусгай хэрэгслийн жагсаалт */
export interface CitizenLegalEntityWeaponsResponse {
  /** Жагсаалт */
  readonly listData: readonly CitizenLegalEntityWeaponsListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { bodyNum: "string", regNumber: "string", createdAt: "string", regType: "string", regTitle: "string" } } } };

/**
 * GS10073_CitizenLegalEntityWeapons: Иргэн (Хуулийн этгээд) -н эмшдэг гал зэвсэг, хийн буу, тусгай хэрэгслийн жагсаалт
 *
 * Provider: Шүүхийн Шинжилгээний Ерөнхий Газар
 */
export async function citizenLegalEntityWeapons(transport: Transport, params: CitizenLegalEntityWeaponsParams = {}, options: CallOptions = {}): Promise<XypResult<CitizenLegalEntityWeaponsResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      authType: params.authType,
    },
    options,
  );
  return decodeResponse<CitizenLegalEntityWeaponsResponse>("CitizenLegalEntityWeaponsResponse", SCHEMA, data);
}
