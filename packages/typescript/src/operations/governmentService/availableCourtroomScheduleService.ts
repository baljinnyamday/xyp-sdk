/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * GS10080_AvailableCourtroomScheduleService (governmentService, Шүүхийн Шинжилгээний Ерөнхий Газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "GS10080_AvailableCourtroomScheduleService";

/** Input of GS10080_AvailableCourtroomScheduleService. */
export interface AvailableCourtroomScheduleServiceParams {
  readonly date?: string;
  readonly deceasedInfoId?: string;
}

/** Жагсаалт */
export interface AvailableCourtroomScheduleServiceListData {
  readonly hallId: string | null;
  readonly hallName: string | null;
  readonly date: string | null;
  /** Жагсаалт */
  readonly list: readonly unknown[] | null;
}

/** GS10080_AvailableCourtroomScheduleService: Сул танхим, цаг дуудах сервис */
export interface AvailableCourtroomScheduleServiceResponse {
  /** Жагсаалт */
  readonly listData: readonly AvailableCourtroomScheduleServiceListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { hallId: "string", hallName: "string", date: "string", list: { list: "any" } } } } };

/**
 * GS10080_AvailableCourtroomScheduleService: Сул танхим, цаг дуудах сервис
 *
 * Provider: Шүүхийн Шинжилгээний Ерөнхий Газар
 */
export async function availableCourtroomScheduleService(transport: Transport, params: AvailableCourtroomScheduleServiceParams = {}, options: CallOptions = {}): Promise<XypResult<AvailableCourtroomScheduleServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      date: params.date,
      deceasedInfoId: params.deceasedInfoId,
    },
    options,
  );
  return decodeResponse<AvailableCourtroomScheduleServiceResponse>("AvailableCourtroomScheduleServiceResponse", SCHEMA, data);
}
