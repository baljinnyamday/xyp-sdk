/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * GS10059_CreateFeedbackFactCall (governmentService, Засгийн газрын Хэрэг эрхлэх газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "GS10059_CreateFeedbackFactCall";

/** Input of GS10059_CreateFeedbackFactCall. */
export interface CreateFeedbackFactCallParams {
  readonly sessionId?: number;
  readonly departmentId?: number;
  readonly fromApi?: string;
  readonly requestId?: string;
  readonly sequence?: number;
  readonly reason?: string;
  readonly description?: string;
  readonly userFullName?: string;
  readonly userJobTittle?: string;
  readonly typeId?: number;
  readonly tagId?: number;
  readonly statusId?: number;
  readonly clientSessionId?: string;
}

/** GS10059_CreateFeedbackFactCall: Өргөдөл, гомдол төлөв өөрчлөлт */
export interface CreateFeedbackFactCallResponse {
  readonly callId: number | null;
}

const SCHEMA: Schema = { callId: "int" };

/**
 * GS10059_CreateFeedbackFactCall: Өргөдөл, гомдол төлөв өөрчлөлт
 *
 * Provider: Засгийн газрын Хэрэг эрхлэх газар
 */
export async function createFeedbackFactCall(transport: Transport, params: CreateFeedbackFactCallParams = {}, options: CallOptions = {}): Promise<XypResult<CreateFeedbackFactCallResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      sessionId: params.sessionId,
      departmentId: params.departmentId,
      fromApi: params.fromApi,
      requestId: params.requestId,
      sequence: params.sequence,
      reason: params.reason,
      description: params.description,
      userFullName: params.userFullName,
      userJobTittle: params.userJobTittle,
      typeId: params.typeId,
      tagId: params.tagId,
      statusId: params.statusId,
      clientSessionId: params.clientSessionId,
    },
    options,
  );
  return decodeResponse<CreateFeedbackFactCallResponse>("CreateFeedbackFactCallResponse", SCHEMA, data);
}
