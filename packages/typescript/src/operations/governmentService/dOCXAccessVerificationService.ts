/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * GS10084_DOCXAccessVerificationService (governmentService, И-Монголиа академи)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "GS10084_DOCXAccessVerificationService";

/** Input of GS10084_DOCXAccessVerificationService. */
export interface DOCXAccessVerificationServiceParams {
  /** Байгууллагын токен */
  readonly programAccessToken?: string;
  /** Албан бичгийн системийн токен */
  readonly docxOrgToken?: string;
}

/** GS10084_DOCXAccessVerificationService: Албан бичиг солилцооны дундын системд хандах эрх баталгаажуулах сервис */
export interface DOCXAccessVerificationServiceResponse {
  /** Docx хандалтын токен */
  readonly createDocxToken: string | null;
}

const SCHEMA: Schema = { createDocxToken: "string" };

/**
 * GS10084_DOCXAccessVerificationService: Албан бичиг солилцооны дундын системд хандах эрх баталгаажуулах сервис
 *
 * Provider: И-Монголиа академи
 */
export async function dOCXAccessVerificationService(transport: Transport, params: DOCXAccessVerificationServiceParams = {}, options: CallOptions = {}): Promise<XypResult<DOCXAccessVerificationServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      programAccessToken: params.programAccessToken,
      docxOrgToken: params.docxOrgToken,
    },
    options,
  );
  return decodeResponse<DOCXAccessVerificationServiceResponse>("DOCXAccessVerificationServiceResponse", SCHEMA, data);
}
