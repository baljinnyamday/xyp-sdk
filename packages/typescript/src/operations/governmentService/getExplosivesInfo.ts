/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * GS10037_getExplosivesInfo (governmentService, Цагдаагийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "GS10037_getExplosivesInfo";

/** Input of GS10037_getExplosivesInfo. */
export interface GetExplosivesInfoParams {
  /** төлөв */
  readonly status?: boolean;
}

/** жагсаалт */
export interface GetExplosivesInfoData {
  /** нэр */
  readonly name: string | null;
  /** дугаар */
  readonly id: number | null;
}

/** GS10037_getExplosivesInfo: Тэсрэх бодисийн мэдээлэл авах сервис */
export interface GetExplosivesInfoResponse {
  /** жагсаалт */
  readonly data: readonly GetExplosivesInfoData[] | null;
}

const SCHEMA: Schema = { data: { list: { object: { name: "string", id: "int" } } } };

/**
 * GS10037_getExplosivesInfo: Тэсрэх бодисийн мэдээлэл авах сервис
 *
 * Provider: Цагдаагийн ерөнхий газар
 */
export async function getExplosivesInfo(transport: Transport, params: GetExplosivesInfoParams = {}, options: CallOptions = {}): Promise<XypResult<GetExplosivesInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      status: params.status,
    },
    options,
  );
  return decodeResponse<GetExplosivesInfoResponse>("GetExplosivesInfoResponse", SCHEMA, data);
}
