/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * GS10019_firearmOwnerCertificateInfo (governmentService, Цагдаагийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "GS10019_firearmOwnerCertificateInfo";

/** Input of GS10019_firearmOwnerCertificateInfo. */
export interface FirearmOwnerCertificateInfoParams {
  /** Регистрийн дугаар */
  readonly regnum?: string;
}

/** GS10019_firearmOwnerCertificateInfo: Галт зэвсэг эзэмших гэрчилгээний мэдээлэл */
export interface FirearmOwnerCertificateInfoResponse {
  /** Овог */
  readonly lastname: string | null;
  /** Нэр */
  readonly firstname: string | null;
  /** Регистрийн дугаар */
  readonly regnum: string | null;
  /** Гэрчилгээний дугаар */
  readonly certificateNumber: string | null;
  /** Сургалт дууссан огноо */
  readonly SDate: string | null;
  /** Сургалт эхэлсэн огноо */
  readonly EDate: string | null;
}

const SCHEMA: Schema = { lastname: "string", firstname: "string", regnum: "string", certificateNumber: "string", SDate: "string", EDate: "string" };

/**
 * GS10019_firearmOwnerCertificateInfo: Галт зэвсэг эзэмших гэрчилгээний мэдээлэл
 *
 * Provider: Цагдаагийн ерөнхий газар
 */
export async function firearmOwnerCertificateInfo(transport: Transport, params: FirearmOwnerCertificateInfoParams = {}, options: CallOptions = {}): Promise<XypResult<FirearmOwnerCertificateInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<FirearmOwnerCertificateInfoResponse>("FirearmOwnerCertificateInfoResponse", SCHEMA, data);
}
