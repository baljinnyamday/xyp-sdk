/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * GS10082_PaymentConfirmationService (governmentService, Шүүхийн Шинжилгээний Ерөнхий Газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "GS10082_PaymentConfirmationService";

/** Input of GS10082_PaymentConfirmationService. */
export interface PaymentConfirmationServiceParams {
  /** Танхимийн дугаар */
  readonly hallId?: string;
  /** Танхим ашиглах огноо, цаг */
  readonly hallDate?: string;
  /** Насан өндөрлөгсдийн мэдээллийн дугаар */
  readonly deceasedInfoId?: string;
  /** Нийт төлбөр */
  readonly paymentTotal?: number;
  /** Регистрийн дугаар */
  readonly regnum?: string;
  /** Овог */
  readonly surname?: string;
  /** Нэр */
  readonly givenName?: string;
  /** Утасны дугаар */
  readonly phoneNumber?: string;
  /** Төлбөрийн лог дугаар */
  readonly paymentLogId?: string;
  /** Төлбөр төлсөн огноо */
  readonly paymentDate?: string;
}

/** GS10082_PaymentConfirmationService: Төлбөр төлсөн мэдээлэл илгээж, цаг баталгаажуулах */
export interface PaymentConfirmationServiceResponse {
  /** Захиалгын дугаар */
  readonly id: string | null;
  /** Насан өндөрлөгсдийн мэдээллийн дугаар */
  readonly deceasedInfoId: string | null;
  /** Төлбөр төлсөн огноо */
  readonly paymentDate: string | null;
  /** Нийт төлбөр */
  readonly paymentTotal: number | null;
  /** Танхимийн дугаар */
  readonly hallId: string | null;
  /** Танхимийн нэр */
  readonly hallName: string | null;
  /** Танхим ашиглах огноо, цаг */
  readonly hallDate: string | null;
}

const SCHEMA: Schema = { id: "string", deceasedInfoId: "string", paymentDate: "string", paymentTotal: "int", hallId: "string", hallName: "string", hallDate: "string" };

/**
 * GS10082_PaymentConfirmationService: Төлбөр төлсөн мэдээлэл илгээж, цаг баталгаажуулах
 *
 * Provider: Шүүхийн Шинжилгээний Ерөнхий Газар
 */
export async function paymentConfirmationService(transport: Transport, params: PaymentConfirmationServiceParams = {}, options: CallOptions = {}): Promise<XypResult<PaymentConfirmationServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      hallId: params.hallId,
      hallDate: params.hallDate,
      deceasedInfoId: params.deceasedInfoId,
      paymentTotal: params.paymentTotal,
      regnum: params.regnum,
      surname: params.surname,
      givenName: params.givenName,
      phoneNumber: params.phoneNumber,
      paymentLogId: params.paymentLogId,
      paymentDate: params.paymentDate,
    },
    options,
  );
  return decodeResponse<PaymentConfirmationServiceResponse>("PaymentConfirmationServiceResponse", SCHEMA, data);
}
