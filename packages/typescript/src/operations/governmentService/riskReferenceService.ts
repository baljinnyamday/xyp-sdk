/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * GS10085_RiskReferenceService (governmentService, И-Монголиа академи)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "GS10085_RiskReferenceService";

/** Input of GS10085_RiskReferenceService. */
export interface RiskReferenceServiceParams {
  /** лавлах сангуудын шүүлт (заавал биш). Зөвшөөрөгдөх утга: envs, types, ctypes, status. */
  readonly only?: readonly string[];
  /** Эрсдлийн жагсаалт харах systempid шүүлт (UUID) (заавал биш) */
  readonly systempid?: string;
  /** Эрсдлийн жагсаалт харах orgregnum шүүлт orgregnum ба systempid хоёуланг өгвөл AND шүүлт (заавал биш) */
  readonly orgregnum?: string;
  /** Эрсдлийн жагсаалт хуудасны дугаар (default 1) (заавал биш) */
  readonly page?: number;
  /** Эрсдлийн жагсаалт хуудас тутмын тоо (default 20, max 100) (заавал биш) */
  readonly limit?: number;
}

/** Эрсдлийн орчин */
export interface RiskReferenceServiceEnvs {
  readonly publicId: string | null;
  readonly id: string | null;
  readonly title: string | null;
}

/** Эрсдлийн төрөл */
export interface RiskReferenceServiceTypes {
  readonly id: string | null;
  readonly publicId: string | null;
  readonly title: string | null;
}

/** Эрсдэл бүртгэгч (хувь хүн, байгууллага) */
export interface RiskReferenceServiceCtypes {
  readonly id: string | null;
  readonly publicId: string | null;
  readonly title: string | null;
}

/** Эрсдлийн төлөв */
export interface RiskReferenceServiceStatus {
  readonly id: string | null;
  readonly publicId: string | null;
  readonly title: string | null;
  readonly desc: string | null;
}

/** risks - Эрсдлийн жагсаалт харах */
export interface RiskReferenceServiceRisks {
  readonly id: string | null;
  readonly publicId: string | null;
  readonly ctpid: string | null;
  readonly sttpid: string | null;
  readonly desc: string | null;
  readonly lt: string | null;
  readonly ln: string | null;
  readonly civilid: string | null;
  readonly ctypes: unknown | null;
  readonly status: unknown | null;
  readonly types: unknown | null;
  readonly systems: unknown | null;
}

/** GS10085_RiskReferenceService: Эрсдлийн лавлах сан */
export interface RiskReferenceServiceResponse {
  /** Эрсдлийн орчин */
  readonly envs: readonly RiskReferenceServiceEnvs[] | null;
  /** Эрсдлийн төрөл */
  readonly types: readonly RiskReferenceServiceTypes[] | null;
  /** Эрсдэл бүртгэгч (хувь хүн, байгууллага) */
  readonly ctypes: readonly RiskReferenceServiceCtypes[] | null;
  /** Эрсдлийн төлөв */
  readonly status: readonly RiskReferenceServiceStatus[] | null;
  /** risks - Эрсдлийн жагсаалт харах */
  readonly risks: readonly RiskReferenceServiceRisks[] | null;
}

const SCHEMA: Schema = {
  envs: { list: { object: { publicId: "string", id: "string", title: "string" } } },
  types: { list: { object: { id: "string", publicId: "string", title: "string" } } },
  ctypes: { list: { object: { id: "string", publicId: "string", title: "string" } } },
  status: { list: { object: { id: "string", publicId: "string", title: "string", desc: "string" } } },
  risks: { list: { object: { id: "string", publicId: "string", ctpid: "string", sttpid: "string", desc: "string", lt: "string", ln: "string", civilid: "string", ctypes: "any", status: "any", types: "any", systems: "any" } } },
};

/**
 * GS10085_RiskReferenceService: Эрсдлийн лавлах сан
 *
 * Provider: И-Монголиа академи
 */
export async function riskReferenceService(transport: Transport, params: RiskReferenceServiceParams = {}, options: CallOptions = {}): Promise<XypResult<RiskReferenceServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      only: params.only,
      systempid: params.systempid,
      orgregnum: params.orgregnum,
      page: params.page,
      limit: params.limit,
    },
    options,
  );
  return decodeResponse<RiskReferenceServiceResponse>("RiskReferenceServiceResponse", SCHEMA, data);
}
