/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * GS10021_covidVaccineDoseCount (governmentService, Эрүүл мэндийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "GS10021_covidVaccineDoseCount";

/** Input of GS10021_covidVaccineDoseCount. */
export interface CovidVaccineDoseCountParams {
  /** Регистрийн дугаар */
  readonly regnum?: string;
  /** Иргэний дугаар */
  readonly civilId?: string;
}

/** GS10021_covidVaccineDoseCount: Ковид вакцины тунгийн тоо */
export interface CovidVaccineDoseCountResponse {
  /** Вакцины тунгийн тоо */
  readonly vaccineDoseCount: number | null;
}

const SCHEMA: Schema = { vaccineDoseCount: "int" };

/**
 * GS10021_covidVaccineDoseCount: Ковид вакцины тунгийн тоо
 *
 * Provider: Эрүүл мэндийн яам
 */
export async function covidVaccineDoseCount(transport: Transport, params: CovidVaccineDoseCountParams = {}, options: CallOptions = {}): Promise<XypResult<CovidVaccineDoseCountResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      civilId: params.civilId,
    },
    options,
  );
  return decodeResponse<CovidVaccineDoseCountResponse>("CovidVaccineDoseCountResponse", SCHEMA, data);
}
