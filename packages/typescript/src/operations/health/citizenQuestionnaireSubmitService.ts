/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300192_CitizenQuestionnaireSubmitService (health, Эрүүл мэндийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300192_CitizenQuestionnaireSubmitService";

/** Input of WS300192_CitizenQuestionnaireSubmitService. */
export interface CitizenQuestionnaireSubmitServiceParams {
  /** Иргэний РД */
  readonly regnum?: string;
  /** Тусламж үйлчилгээний бүртгэлийн ID */
  readonly rcad?: number;
  /** Иргэний утасны дугаар */
  readonly phoneNo?: number;
  /** Санал, сэтгэгдэл */
  readonly feedBack?: string;
  /** Хариулт */
  readonly answers?: readonly unknown[];
}

/** WS300192_CitizenQuestionnaireSubmitService: Иргэний хариулсан асуумжийг илгээх сервис */
export interface CitizenQuestionnaireSubmitServiceResponse {
  /** Хариу тайлбар */
  readonly description: string | null;
}

const SCHEMA: Schema = { description: "string" };

/**
 * WS300192_CitizenQuestionnaireSubmitService: Иргэний хариулсан асуумжийг илгээх сервис
 *
 * Provider: Эрүүл мэндийн даатгалын ерөнхий газар
 */
export async function citizenQuestionnaireSubmitService(transport: Transport, params: CitizenQuestionnaireSubmitServiceParams = {}, options: CallOptions = {}): Promise<XypResult<CitizenQuestionnaireSubmitServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      rcad: params.rcad,
      phoneNo: params.phoneNo,
      feedBack: params.feedBack,
      answers: params.answers,
    },
    options,
  );
  return decodeResponse<CitizenQuestionnaireSubmitServiceResponse>("CitizenQuestionnaireSubmitServiceResponse", SCHEMA, data);
}
