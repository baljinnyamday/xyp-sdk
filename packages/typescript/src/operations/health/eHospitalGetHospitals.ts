/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300132_eHospitalGetHospitals (health, Эрүүл мэндийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300132_eHospitalGetHospitals";

/** Input of WS300132_eHospitalGetHospitals. */
export interface EHospitalGetHospitalsParams {
  readonly isGov?: number;
}

/** эмнэлгийн жагсаалт */
export interface EHospitalGetHospitalsListData {
  /** эмнэлгийн дугаар */
  readonly hospitalId: number | null;
  /** эмнэлгийн нэр */
  readonly hospitalName: string | null;
  /** эрэмбэ */
  readonly priority: string | null;
}

/** WS300132_eHospitalGetHospitals: EHospital эмнэлгийн жагсаалт дуудах сервис */
export interface EHospitalGetHospitalsResponse {
  /** эмнэлгийн жагсаалт */
  readonly listData: readonly EHospitalGetHospitalsListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { hospitalId: "int", hospitalName: "string", priority: "string" } } } };

/**
 * WS300132_eHospitalGetHospitals: EHospital эмнэлгийн жагсаалт дуудах сервис
 *
 * Provider: Эрүүл мэндийн даатгалын ерөнхий газар
 */
export async function eHospitalGetHospitals(transport: Transport, params: EHospitalGetHospitalsParams = {}, options: CallOptions = {}): Promise<XypResult<EHospitalGetHospitalsResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      isGov: params.isGov,
    },
    options,
  );
  return decodeResponse<EHospitalGetHospitalsResponse>("EHospitalGetHospitalsResponse", SCHEMA, data);
}
