/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300152_GetHealthInsurancePremiumList (health, Эрүүл мэндийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300152_GetHealthInsurancePremiumList";

/** Input of WS300152_GetHealthInsurancePremiumList. */
export interface GetHealthInsurancePremiumListParams {
  /** Регистр */
  readonly regnum?: string;
}

/** төлөгдсөн саруудын жагсаалт */
export interface GetHealthInsurancePremiumListPayedList {
  /** жил */
  readonly year: number | null;
  /** сар */
  readonly month: number | null;
}

/** төлөгдөөгүй саруудын жагсаалт */
export interface GetHealthInsurancePremiumListNotPayedList {
  /** жил */
  readonly year: number | null;
  /** сар */
  readonly month: number | null;
}

/** WS300152_GetHealthInsurancePremiumList: Эрүүл мэндийн даатгалын шимтгэл төлөгдсөн болон төлөгдөөгүй саруудыг ялгаж буцаах сервис */
export interface GetHealthInsurancePremiumListResponse {
  /** төлөгдсөн саруудын жагсаалт */
  readonly payedList: readonly GetHealthInsurancePremiumListPayedList[] | null;
  /** төлөгдөөгүй саруудын жагсаалт */
  readonly notPayedList: readonly GetHealthInsurancePremiumListNotPayedList[] | null;
}

const SCHEMA: Schema = { payedList: { list: { object: { year: "int", month: "int" } } }, notPayedList: { list: { object: { year: "int", month: "int" } } } };

/**
 * WS300152_GetHealthInsurancePremiumList: Эрүүл мэндийн даатгалын шимтгэл төлөгдсөн болон төлөгдөөгүй саруудыг ялгаж буцаах сервис
 *
 * Provider: Эрүүл мэндийн даатгалын ерөнхий газар
 */
export async function getHealthInsurancePremiumList(transport: Transport, params: GetHealthInsurancePremiumListParams = {}, options: CallOptions = {}): Promise<XypResult<GetHealthInsurancePremiumListResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<GetHealthInsurancePremiumListResponse>("GetHealthInsurancePremiumListResponse", SCHEMA, data);
}
