/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300125_eHospitalByAddress (health, Эрүүл мэндийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300125_eHospitalByAddress";

/** Input of WS300125_eHospitalByAddress. */
export interface EHospitalByAddressParams {
  readonly addressid?: string;
}

export interface EHospitalByAddressListData {
  readonly addressId: number | null;
  readonly hospitalId: number | null;
  readonly hospitalName: string | null;
}

/** WS300125_eHospitalByAddress: Сонгосон баг хороонд байгаа өрхийн эмнэлгүүд */
export interface EHospitalByAddressResponse {
  readonly listData: readonly EHospitalByAddressListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { addressId: "int", hospitalId: "int", hospitalName: "string" } } } };

/**
 * WS300125_eHospitalByAddress: Сонгосон баг хороонд байгаа өрхийн эмнэлгүүд
 *
 * Provider: Эрүүл мэндийн даатгалын ерөнхий газар
 */
export async function eHospitalByAddress(transport: Transport, params: EHospitalByAddressParams = {}, options: CallOptions = {}): Promise<XypResult<EHospitalByAddressResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      addressid: params.addressid,
    },
    options,
  );
  return decodeResponse<EHospitalByAddressResponse>("EHospitalByAddressResponse", SCHEMA, data);
}
