/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300135_eHospitalOnlineTimeOrderList (health, Эрүүл мэндийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300135_eHospitalOnlineTimeOrderList";

/** Input of WS300135_eHospitalOnlineTimeOrderList. */
export interface EHospitalOnlineTimeOrderListParams {
  /** регистрийн дугаар */
  readonly regnum?: string;
  /** эмнэлгийн дугаар */
  readonly hospitalId?: number;
}

/** Цахимаар авсан цагийн жагсаалт */
export interface EHospitalOnlineTimeOrderListListData {
  /** цаг захиалгын дугаар */
  readonly id: string | null;
  /** цаг захиалгын төрлийн дугаар */
  readonly typeId: number | null;
  /** цаг захиалгын төрлийн нэр */
  readonly typeName: string | null;
  /** Кабинетийн нэр */
  readonly cabinet: string | null;
  /** Өрөөний дугаар */
  readonly location: string | null;
  /** Тайлбар */
  readonly desc: string | null;
  /** Огноо */
  readonly date: string | null;
  /** Цагийн дугаар */
  readonly num: number | null;
  /** Цаг */
  readonly time: string | null;
  /** Үйлчилгээний үнэ */
  readonly price: number | null;
  /** Эрүүл мэндийн даатгалаас */
  readonly insuranceDiscount: number | null;
  /** төлөв */
  readonly status: string | null;
}

/** WS300135_eHospitalOnlineTimeOrderList: EHospital иргэний online-аар авсан цагийн жагсаалт */
export interface EHospitalOnlineTimeOrderListResponse {
  /** Цахимаар авсан цагийн жагсаалт */
  readonly listData: readonly EHospitalOnlineTimeOrderListListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { id: "string", typeId: "int", typeName: "string", cabinet: "string", location: "string", desc: "string", date: "string", num: "int", time: "string", price: "int", insuranceDiscount: "int", status: "string" } } } };

/**
 * WS300135_eHospitalOnlineTimeOrderList: EHospital иргэний online-аар авсан цагийн жагсаалт
 *
 * Provider: Эрүүл мэндийн даатгалын ерөнхий газар
 */
export async function eHospitalOnlineTimeOrderList(transport: Transport, params: EHospitalOnlineTimeOrderListParams = {}, options: CallOptions = {}): Promise<XypResult<EHospitalOnlineTimeOrderListResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      hospitalId: params.hospitalId,
    },
    options,
  );
  return decodeResponse<EHospitalOnlineTimeOrderListResponse>("EHospitalOnlineTimeOrderListResponse", SCHEMA, data);
}
