/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300112_eHealthTimeOrderCancel (health, Эрүүл мэндийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300112_eHealthTimeOrderCancel";

/** Input of WS300112_eHealthTimeOrderCancel. */
export interface EHealthTimeOrderCancelParams {
  /** Эмнэлгийн дугаар */
  readonly hospitalId?: number;
  /** Баталгаажуулах код */
  readonly verifyCode?: string;
}

/** WS300112_eHealthTimeOrderCancel: EHealth Цаг захиалгын бүртгэл цуцлах */
export interface EHealthTimeOrderCancelResponse {
  /** хүсэлтийн хариу код */
  readonly checkCode: number | null;
}

const SCHEMA: Schema = { checkCode: "int" };

/**
 * WS300112_eHealthTimeOrderCancel: EHealth Цаг захиалгын бүртгэл цуцлах
 *
 * Provider: Эрүүл мэндийн яам
 */
export async function eHealthTimeOrderCancel(transport: Transport, params: EHealthTimeOrderCancelParams = {}, options: CallOptions = {}): Promise<XypResult<EHealthTimeOrderCancelResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      hospitalId: params.hospitalId,
      verifyCode: params.verifyCode,
    },
    options,
  );
  return decodeResponse<EHealthTimeOrderCancelResponse>("EHealthTimeOrderCancelResponse", SCHEMA, data);
}
