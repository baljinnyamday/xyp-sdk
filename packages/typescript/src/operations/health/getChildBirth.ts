/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300171_GetChildBirth (health, Эрүүл мэндийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300171_GetChildBirth";

/** Input of WS300171_GetChildBirth. */
export interface GetChildBirthParams {
  /** Ээжийн регистр */
  readonly motherRegnum?: string;
  /** Хүүхдийн регистр */
  readonly childRegnum?: string;
}

/** WS300171_GetChildBirth: Тухайн хүүхдийн төрөлтийн мэдээллийг буцаах сервис */
export interface GetChildBirthResponse {
  readonly birthDate: string | null;
  readonly hospital: string | null;
  readonly weight: string | null;
  readonly height: string | null;
  readonly tcircle: string | null;
  readonly mcircle: string | null;
  readonly gender: string | null;
}

const SCHEMA: Schema = { birthDate: "string", hospital: "string", weight: "string", height: "string", tcircle: "string", mcircle: "string", gender: "string" };

/**
 * WS300171_GetChildBirth: Тухайн хүүхдийн төрөлтийн мэдээллийг буцаах сервис
 *
 * Provider: Эрүүл мэндийн яам
 */
export async function getChildBirth(transport: Transport, params: GetChildBirthParams = {}, options: CallOptions = {}): Promise<XypResult<GetChildBirthResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      motherRegnum: params.motherRegnum,
      childRegnum: params.childRegnum,
    },
    options,
  );
  return decodeResponse<GetChildBirthResponse>("GetChildBirthResponse", SCHEMA, data);
}
