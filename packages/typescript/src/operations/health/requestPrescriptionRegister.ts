/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300143_RequestPrescriptionRegister (health, Эрүүл мэндийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300143_RequestPrescriptionRegister";

/** Input of WS300143_RequestPrescriptionRegister. */
export interface RequestPrescriptionRegisterParams {
  /** Үргэлжлэх хугацаа төрөл */
  readonly continueType?: number;
  /** Үргэлжлэх хугацаа утга */
  readonly continueValue?: number;
  /** Жорын дугаар */
  readonly receiptNumber?: number;
  /** Жорын дэлэнгэргүй мэдээллийн дугаар */
  readonly receiptDetailId?: number;
  /** Гаж нөлөөний тухай текст */
  readonly effect?: string;
  /** Үр дүнгийн дугаар */
  readonly resultId?: number;
  /** Эхэлсэн огноо */
  readonly startDate?: string;
  /** Дууссан огноо */
  readonly endDate?: string;
  /** Тайлбар */
  readonly description?: string;
  /** Урвалын төгсгөл */
  readonly effectEndId?: number;
}

/** WS300143_RequestPrescriptionRegister: Энэхүү сервис нь тухайн иргэнийн хэрэглэсэн эм дээр гаж нөлөө илэрсэн тохиолдолд бүртгэх сервис юм. */
export interface RequestPrescriptionRegisterResponse {
  /** төлөв */
  readonly status: boolean | null;
}

const SCHEMA: Schema = { status: "bool" };

/**
 * WS300143_RequestPrescriptionRegister: Энэхүү сервис нь тухайн иргэнийн хэрэглэсэн эм дээр гаж нөлөө илэрсэн тохиолдолд бүртгэх сервис юм.
 *
 * Provider: Эрүүл мэндийн яам
 */
export async function requestPrescriptionRegister(transport: Transport, params: RequestPrescriptionRegisterParams = {}, options: CallOptions = {}): Promise<XypResult<RequestPrescriptionRegisterResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      continueType: params.continueType,
      continueValue: params.continueValue,
      receiptNumber: params.receiptNumber,
      receiptDetailId: params.receiptDetailId,
      effect: params.effect,
      resultId: params.resultId,
      startDate: params.startDate,
      endDate: params.endDate,
      description: params.description,
      effectEndId: params.effectEndId,
    },
    options,
  );
  return decodeResponse<RequestPrescriptionRegisterResponse>("RequestPrescriptionRegisterResponse", SCHEMA, data);
}
