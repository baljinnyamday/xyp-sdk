/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300129_eHospitalGetAnalysisDates (health, Эрүүл мэндийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300129_eHospitalGetAnalysisDates";

/** Input of WS300129_eHospitalGetAnalysisDates. */
export interface EHospitalGetAnalysisDatesParams {
  /** регистрийн дугаар */
  readonly regnum?: string;
  /** эмнэлгийн дугаар */
  readonly hospitalId?: number;
}

/** WS300129_eHospitalGetAnalysisDates: EHospital шинжилгээнд бүртгүүлсэн огнооны жагсаалт дуудах сервис */
export interface EHospitalGetAnalysisDatesResponse {
  /** шинжилгээнд бүртгүүлсэн огнооны жагсаалт */
  readonly listDates: readonly string[] | null;
}

const SCHEMA: Schema = { listDates: { list: "string" } };

/**
 * WS300129_eHospitalGetAnalysisDates: EHospital шинжилгээнд бүртгүүлсэн огнооны жагсаалт дуудах сервис
 *
 * Provider: Эрүүл мэндийн даатгалын ерөнхий газар
 */
export async function eHospitalGetAnalysisDates(transport: Transport, params: EHospitalGetAnalysisDatesParams = {}, options: CallOptions = {}): Promise<XypResult<EHospitalGetAnalysisDatesResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      hospitalId: params.hospitalId,
    },
    options,
  );
  return decodeResponse<EHospitalGetAnalysisDatesResponse>("EHospitalGetAnalysisDatesResponse", SCHEMA, data);
}
