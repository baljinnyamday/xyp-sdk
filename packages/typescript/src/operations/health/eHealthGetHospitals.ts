/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300102_eHealthGetHospitals (health, Эрүүл мэндийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300102_eHealthGetHospitals";

/** Input of WS300102_eHealthGetHospitals. */
export interface EHealthGetHospitalsParams {
  /** регистрийн дугаар; */
  readonly regnum?: string;
}

/** эмнэлгийн жагсаалт */
export interface EHealthGetHospitalsListData {
  /** эмнэлгийн дугаар */
  readonly hospitalId: number | null;
  /** эмнэлгийн нэр */
  readonly hospitalName: string | null;
  /** эрэмбэ */
  readonly priority: string | null;
}

/** WS300102_eHealthGetHospitals: EHealth эмнэлгийн жагсаалт дуудах сервис */
export interface EHealthGetHospitalsResponse {
  /** эмнэлгийн жагсаалт */
  readonly listData: readonly EHealthGetHospitalsListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { hospitalId: "int", hospitalName: "string", priority: "string" } } } };

/**
 * WS300102_eHealthGetHospitals: EHealth эмнэлгийн жагсаалт дуудах сервис
 *
 * Provider: Эрүүл мэндийн яам
 */
export async function eHealthGetHospitals(transport: Transport, params: EHealthGetHospitalsParams = {}, options: CallOptions = {}): Promise<XypResult<EHealthGetHospitalsResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<EHealthGetHospitalsResponse>("EHealthGetHospitalsResponse", SCHEMA, data);
}
