/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300117_earlyDetectionService (health, Эрүүл мэндийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300117_earlyDetectionService";

/** Input of WS300117_earlyDetectionService. */
export interface EarlyDetectionServiceParams {
  /** регистрийн дугаар */
  readonly regnum?: string;
  /** Эрт илрүүлэгийн тайлангийн дугаар 2022 оных тогтмол 20220301-001 */
  readonly report?: string;
  /** төрөл 1 бол Хариуны PDF хаяг 2 бол Асуумж бөглөх хаяг авах */
  readonly type?: number;
}

/** WS300117_earlyDetectionService: Эрт илрүүлэгийн сервис */
export interface EarlyDetectionServiceResponse {
  /** Төлөв */
  readonly status: number | null;
  /** pdf татах эсвэл асуумжын линк */
  readonly url: string | null;
}

const SCHEMA: Schema = { status: "int", url: "string" };

/**
 * WS300117_earlyDetectionService: Эрт илрүүлэгийн сервис
 *
 * Provider: Эрүүл мэндийн яам
 */
export async function earlyDetectionService(transport: Transport, params: EarlyDetectionServiceParams = {}, options: CallOptions = {}): Promise<XypResult<EarlyDetectionServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      report: params.report,
      type: params.type,
    },
    options,
  );
  return decodeResponse<EarlyDetectionServiceResponse>("EarlyDetectionServiceResponse", SCHEMA, data);
}
