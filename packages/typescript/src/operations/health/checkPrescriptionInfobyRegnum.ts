/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300141_CheckPrescriptionInfobyRegnum (health, Эрүүл мэндийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300141_CheckPrescriptionInfobyRegnum";

/** Input of WS300141_CheckPrescriptionInfobyRegnum. */
export interface CheckPrescriptionInfobyRegnumParams {
  /** Иргэний регистрийн дугаар */
  readonly regnum?: string;
}

/** жагсаалт */
export interface CheckPrescriptionInfobyRegnumList {
  /** Жорын огноо */
  readonly receiptDate: string | null;
  /** Жорын дугаар */
  readonly receiptNumber: number | null;
  /** Жорын төрөл */
  readonly receiptType: number | null;
  /** Жорын төрөл /Монгол/ */
  readonly receiptTypeStr: string | null;
  /** Жорын төрөл /Англи/ */
  readonly receiptTypeStrEng: string | null;
  /** Жорын хүчинтэй хугацаа */
  readonly receiptExpireDate: string | null;
  /** Жорын төлөв */
  readonly receiptStatus: number | null;
  /** Жорын төлөв /Монгол/ */
  readonly receiptStatusStr: string | null;
  /** Жорын төлөв /Англи/ */
  readonly receiptStatusStrEng: string | null;
  /** эмчийн нэр */
  readonly doctorName: string | null;
  /** эмнэлгийн нэр */
  readonly hospitalName: string | null;
  /** утасны дугаар */
  readonly phoneNumber: string | null;
  /** эмнэлгийн хаяг */
  readonly hospitalAddress: string | null;
}

/** WS300141_CheckPrescriptionInfobyRegnum: Жорын мэдээллийг регистрийн дугаараа шалгах сервис */
export interface CheckPrescriptionInfobyRegnumResponse {
  /** жагсаалт */
  readonly list: readonly CheckPrescriptionInfobyRegnumList[] | null;
}

const SCHEMA: Schema = {
  list: {
    list: {
      object: {
        receiptDate: "string",
        receiptNumber: "int",
        receiptType: "int",
        receiptTypeStr: "string",
        receiptTypeStrEng: "string",
        receiptExpireDate: "string",
        receiptStatus: "int",
        receiptStatusStr: "string",
        receiptStatusStrEng: "string",
        doctorName: "string",
        hospitalName: "string",
        phoneNumber: "string",
        hospitalAddress: "string",
      },
    },
  },
};

/**
 * WS300141_CheckPrescriptionInfobyRegnum: Жорын мэдээллийг регистрийн дугаараа шалгах сервис
 *
 * Provider: Эрүүл мэндийн яам
 */
export async function checkPrescriptionInfobyRegnum(transport: Transport, params: CheckPrescriptionInfobyRegnumParams = {}, options: CallOptions = {}): Promise<XypResult<CheckPrescriptionInfobyRegnumResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<CheckPrescriptionInfobyRegnumResponse>("CheckPrescriptionInfobyRegnumResponse", SCHEMA, data);
}
