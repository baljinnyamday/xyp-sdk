/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300134_eHospitalOnlineAppointmentOrder (health, Эрүүл мэндийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300134_eHospitalOnlineAppointmentOrder";

/** Input of WS300134_eHospitalOnlineAppointmentOrder. */
export interface EHospitalOnlineAppointmentOrderParams {
  /** Эмнэлгийн дугаар */
  readonly hospitalId?: number;
  /** Регистрийн дугаар */
  readonly regnum?: string;
  /** Үйлчилгээний дугаар */
  readonly serviceId?: string;
  /** Үйлчилгээнийогноо yyyy-MM-dd */
  readonly date?: string;
  /** Цагийн дугаарлалт */
  readonly num?: number;
  /** Цаг */
  readonly time?: string;
  /** Утасны дугаар */
  readonly phoneNo?: string;
}

/** WS300134_eHospitalOnlineAppointmentOrder: EHospital цаг сонгож захиалах сервис */
export interface EHospitalOnlineAppointmentOrderResponse {
  /** хариу */
  readonly result: string | null;
  /** алдааны тайлбар, Захиалгыг баталгаажуулах код */
  readonly resultMessage: string | null;
}

const SCHEMA: Schema = { result: "string", resultMessage: "string" };

/**
 * WS300134_eHospitalOnlineAppointmentOrder: EHospital цаг сонгож захиалах сервис
 *
 * Provider: Эрүүл мэндийн даатгалын ерөнхий газар
 */
export async function eHospitalOnlineAppointmentOrder(transport: Transport, params: EHospitalOnlineAppointmentOrderParams = {}, options: CallOptions = {}): Promise<XypResult<EHospitalOnlineAppointmentOrderResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      hospitalId: params.hospitalId,
      regnum: params.regnum,
      serviceId: params.serviceId,
      date: params.date,
      num: params.num,
      time: params.time,
      phoneNo: params.phoneNo,
    },
    options,
  );
  return decodeResponse<EHospitalOnlineAppointmentOrderResponse>("EHospitalOnlineAppointmentOrderResponse", SCHEMA, data);
}
