/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300106_eHealthCreatePatient (health, Эрүүл мэндийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300106_eHealthCreatePatient";

/** Input of WS300106_eHealthCreatePatient. */
export interface EHealthCreatePatientParams {
  /** Эмнэлэгийн дугаар */
  readonly hospitalId?: number;
  /** Регистрийн дугаар */
  readonly regnum?: string;
  /** Ургийн овог */
  readonly surname?: string;
  /** Овог */
  readonly lastname?: string;
  /** нэр */
  readonly firstname?: string;
  /** Хүйс – 1 бол Эр; 2 бол Эм */
  readonly sex?: number;
  /** төрсөн огноо "yyyy-MM-dd" */
  readonly birthDate?: string;
  /** зип код */
  readonly addressCode?: number;
  /** хаяг */
  readonly address?: string;
}

/** WS300106_eHealthCreatePatient: EHealth Тухайн эмнэлэгт хэрэглэгчийг шинээр бүртгэх */
export interface EHealthCreatePatientResponse {
  /** хүсэлтийн хариу код */
  readonly checkCode: number | null;
}

const SCHEMA: Schema = { checkCode: "int" };

/**
 * WS300106_eHealthCreatePatient: EHealth Тухайн эмнэлэгт хэрэглэгчийг шинээр бүртгэх
 *
 * Provider: Эрүүл мэндийн яам
 */
export async function eHealthCreatePatient(transport: Transport, params: EHealthCreatePatientParams = {}, options: CallOptions = {}): Promise<XypResult<EHealthCreatePatientResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      hospitalId: params.hospitalId,
      regnum: params.regnum,
      surname: params.surname,
      lastname: params.lastname,
      firstname: params.firstname,
      sex: params.sex,
      birthDate: params.birthDate,
      addressCode: params.addressCode,
      address: params.address,
    },
    options,
  );
  return decodeResponse<EHealthCreatePatientResponse>("EHealthCreatePatientResponse", SCHEMA, data);
}
