/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300163_GetActivePregnancy (health, Эрүүл мэндийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300163_GetActivePregnancy";

/** Input of WS300163_GetActivePregnancy. */
export interface GetActivePregnancyParams {
  /** регистр */
  readonly regnum?: string;
}

/** Жагсаалт */
export interface GetActivePregnancyResult {
  /** Жирэмсний хяналтын цахим дугаар */
  readonly pregnancyNo: string | null;
  /** Он */
  readonly activeYear: string | null;
  /** Төлөв */
  readonly status: string | null;
  /** Жирэмсний 7 хоног */
  readonly pregnancyWeeks: string | null;
  /** Төрөх огноо */
  readonly remainingDays: string | null;
}

/** WS300163_GetActivePregnancy: Жирэмсэн update */
export interface GetActivePregnancyResponse {
  /** Жагсаалт */
  readonly result: readonly GetActivePregnancyResult[] | null;
}

const SCHEMA: Schema = { result: { list: { object: { pregnancyNo: "string", activeYear: "string", status: "string", pregnancyWeeks: "string", remainingDays: "string" } } } };

/**
 * WS300163_GetActivePregnancy: Жирэмсэн update
 *
 * Provider: Эрүүл мэндийн яам
 */
export async function getActivePregnancy(transport: Transport, params: GetActivePregnancyParams = {}, options: CallOptions = {}): Promise<XypResult<GetActivePregnancyResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<GetActivePregnancyResponse>("GetActivePregnancyResponse", SCHEMA, data);
}
