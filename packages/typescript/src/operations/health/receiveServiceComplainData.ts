/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300158_ReceiveServiceComplainData (health, Эрүүл мэндийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300158_ReceiveServiceComplainData";

/** Input of WS300158_ReceiveServiceComplainData. */
export interface ReceiveServiceComplainDataParams {
  /** Санал, гомдол илгээж буй иргэний РД */
  readonly confirmedRegisterInfo?: string;
  /** Утасны дугаар */
  readonly phoneNumber?: string;
  /** Тусламж, үйлчилгээний мэдээлэл */
  readonly services?: readonly unknown[];
}

/** WS300158_ReceiveServiceComplainData: Эрүүл мэндийн даатгалаар авсан тусламж, үйлчилгээтэй холбоотой санал, гомдол илгээх сервис */
export interface ReceiveServiceComplainDataResponse {
  /** Хариу мессэж код */
  readonly code: number | null;
  /** Хариу мессэж */
  readonly description: string | null;
  /** Мэдээлэл */
  readonly result: string | null;
}

const SCHEMA: Schema = { code: "int", description: "string", result: "string" };

/**
 * WS300158_ReceiveServiceComplainData: Эрүүл мэндийн даатгалаар авсан тусламж, үйлчилгээтэй холбоотой санал, гомдол илгээх сервис
 *
 * Provider: Эрүүл мэндийн яам
 */
export async function receiveServiceComplainData(transport: Transport, params: ReceiveServiceComplainDataParams = {}, options: CallOptions = {}): Promise<XypResult<ReceiveServiceComplainDataResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      confirmedRegisterInfo: params.confirmedRegisterInfo,
      phoneNumber: params.phoneNumber,
      services: params.services,
    },
    options,
  );
  return decodeResponse<ReceiveServiceComplainDataResponse>("ReceiveServiceComplainDataResponse", SCHEMA, data);
}
