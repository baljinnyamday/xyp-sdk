/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300114_eHealthListService (health, Эрүүл мэндийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300114_eHealthListService";

/** Input of WS300114_eHealthListService. */
export interface EHealthListServiceParams {
  /** эмнэлгийн дугаар */
  readonly hospitalId?: number;
  /** Эмнэлгийн хуудасны дугаар */
  readonly pid?: string;
}

/** WS300114_eHealthListService: EHealth Эмнэлгийн хуудасны мэдээлэл харах */
export interface EHealthListServiceResponse {
  /** үр дүн */
  readonly resultData: string | null;
}

const SCHEMA: Schema = { resultData: "string" };

/**
 * WS300114_eHealthListService: EHealth Эмнэлгийн хуудасны мэдээлэл харах
 *
 * Provider: Эрүүл мэндийн яам
 */
export async function eHealthListService(transport: Transport, params: EHealthListServiceParams = {}, options: CallOptions = {}): Promise<XypResult<EHealthListServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      hospitalId: params.hospitalId,
      pid: params.pid,
    },
    options,
  );
  return decodeResponse<EHealthListServiceResponse>("EHealthListServiceResponse", SCHEMA, data);
}
