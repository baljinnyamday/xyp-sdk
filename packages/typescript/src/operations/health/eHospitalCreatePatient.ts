/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300128_eHospitalCreatePatient (health, Эрүүл мэндийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300128_eHospitalCreatePatient";

/** Input of WS300128_eHospitalCreatePatient. */
export interface EHospitalCreatePatientParams {
  /** Эмнэлэгийн дугаар */
  readonly hospitalId?: number;
  /** Регистрийн дугаар */
  readonly regnum?: string;
  /** Ургийн овог */
  readonly surname?: string;
  /** Овог */
  readonly lastname?: string;
  /** нэр */
  readonly firstname?: string;
  /** Хүйс – 1 бол Эр 2 бол Эм */
  readonly sex?: number;
  /** төрсөн огноо "yyyy-MM-dd" */
  readonly birthDate?: string;
  /** зип код */
  readonly addressCode?: number;
  /** хаяг */
  readonly address?: string;
}

/** WS300128_eHospitalCreatePatient: EHospital Тухайн эмнэлэгт хэрэглэгчийг шинээр бүртгэх */
export interface EHospitalCreatePatientResponse {
  /** хүсэлтийн хариу код */
  readonly checkCode: number | null;
}

const SCHEMA: Schema = { checkCode: "int" };

/**
 * WS300128_eHospitalCreatePatient: EHospital Тухайн эмнэлэгт хэрэглэгчийг шинээр бүртгэх
 *
 * Provider: Эрүүл мэндийн даатгалын ерөнхий газар
 */
export async function eHospitalCreatePatient(transport: Transport, params: EHospitalCreatePatientParams = {}, options: CallOptions = {}): Promise<XypResult<EHospitalCreatePatientResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      hospitalId: params.hospitalId,
      regnum: params.regnum,
      surname: params.surname,
      lastname: params.lastname,
      firstname: params.firstname,
      sex: params.sex,
      birthDate: params.birthDate,
      addressCode: params.addressCode,
      address: params.address,
    },
    options,
  );
  return decodeResponse<EHospitalCreatePatientResponse>("EHospitalCreatePatientResponse", SCHEMA, data);
}
