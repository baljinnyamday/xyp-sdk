/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300172_GetChildBookCheckUps (health, Эрүүл мэндийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300172_GetChildBookCheckUps";

/** Input of WS300172_GetChildBookCheckUps. */
export interface GetChildBookCheckUpsParams {
  /** 1- Хүүхэд, 2- Ээж */
  readonly type?: number;
}

/** Жагсаалт */
export interface GetChildBookCheckUpsResult {
  readonly checkUpId: number | null;
  readonly checkUpName: string | null;
}

/** WS300172_GetChildBookCheckUps: Эх хүүхдийн дэвтрийн эрүүл мэндийн үзлэгийн жагсаалт татах сервис */
export interface GetChildBookCheckUpsResponse {
  /** Жагсаалт */
  readonly result: readonly GetChildBookCheckUpsResult[] | null;
}

const SCHEMA: Schema = { result: { list: { object: { checkUpId: "int", checkUpName: "string" } } } };

/**
 * WS300172_GetChildBookCheckUps: Эх хүүхдийн дэвтрийн эрүүл мэндийн үзлэгийн жагсаалт татах сервис
 *
 * Provider: Эрүүл мэндийн яам
 */
export async function getChildBookCheckUps(transport: Transport, params: GetChildBookCheckUpsParams = {}, options: CallOptions = {}): Promise<XypResult<GetChildBookCheckUpsResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      type: params.type,
    },
    options,
  );
  return decodeResponse<GetChildBookCheckUpsResponse>("GetChildBookCheckUpsResponse", SCHEMA, data);
}
