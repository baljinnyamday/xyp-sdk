/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300124_eHospitalFindAddress (health, Эрүүл мэндийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300124_eHospitalFindAddress";

/** Input of WS300124_eHospitalFindAddress. */
export type EHospitalFindAddressParams = Readonly<Record<string, never>>;

export interface EHospitalFindAddressListData {
  readonly addressId: number | null;
  readonly addressName: string | null;
  readonly sumDuureg: readonly unknown[] | null;
}

/** WS300124_eHospitalFindAddress: Хаягийн жагсаалт дуудах */
export interface EHospitalFindAddressResponse {
  readonly listData: readonly EHospitalFindAddressListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { addressId: "int", addressName: "string", sumDuureg: { list: "any" } } } } };

/**
 * WS300124_eHospitalFindAddress: Хаягийн жагсаалт дуудах
 *
 * Provider: Эрүүл мэндийн даатгалын ерөнхий газар
 */
export async function eHospitalFindAddress(transport: Transport, _params: EHospitalFindAddressParams = {}, options: CallOptions = {}): Promise<XypResult<EHospitalFindAddressResponse>> {
  const data = await transport.call(OPERATION, {}, options);
  return decodeResponse<EHospitalFindAddressResponse>("EHospitalFindAddressResponse", SCHEMA, data);
}
