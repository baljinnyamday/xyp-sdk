/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300153_CheckPaymentMonths (health, Эрүүл мэндийн даатгалын ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300153_CheckPaymentMonths";

/** Input of WS300153_CheckPaymentMonths. */
export interface CheckPaymentMonthsParams {
  /** Регистр */
  readonly regnum?: string;
  /** Сарууд */
  readonly months?: string;
}

/** WS300153_CheckPaymentMonths: Эрүүл мэндийн даатгалын шимтгэл иргэнийг давхардуулан төлөхгүй байх хаалт хийх сервис */
export interface CheckPaymentMonthsResponse {
  /** ResultCode */
  readonly code: number | null;
  /** Тайлбар */
  readonly description: string | null;
  /** Үр дүн */
  readonly result: string | null;
}

const SCHEMA: Schema = { code: "int", description: "string", result: "string" };

/**
 * WS300153_CheckPaymentMonths: Эрүүл мэндийн даатгалын шимтгэл иргэнийг давхардуулан төлөхгүй байх хаалт хийх сервис
 *
 * Provider: Эрүүл мэндийн даатгалын ерөнхий газар
 */
export async function checkPaymentMonths(transport: Transport, params: CheckPaymentMonthsParams = {}, options: CallOptions = {}): Promise<XypResult<CheckPaymentMonthsResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      months: params.months,
    },
    options,
  );
  return decodeResponse<CheckPaymentMonthsResponse>("CheckPaymentMonthsResponse", SCHEMA, data);
}
