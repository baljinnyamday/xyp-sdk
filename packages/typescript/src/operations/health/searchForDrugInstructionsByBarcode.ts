/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS300148_SearchForDrugInstructionsByBarcode (health, Эм, эмнэлгийн хэрэгслийн хяналт, зохицуулалтын газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS300148_SearchForDrugInstructionsByBarcode";

/** Input of WS300148_SearchForDrugInstructionsByBarcode. */
export interface SearchForDrugInstructionsByBarcodeParams {
  /** Баркод */
  readonly barcode?: string;
}

/** Жагсаалт */
export interface SearchForDrugInstructionsByBarcodeData {
  /** Баркод */
  readonly pk: number | null;
  /** Хэрэглэх заавар */
  readonly instruction: string | null;
}

/** WS300148_SearchForDrugInstructionsByBarcode: Эмийн хэрэглэх заавар баркодоор хайх сервис */
export interface SearchForDrugInstructionsByBarcodeResponse {
  readonly retType: number | null;
  readonly retDesc: string | null;
  readonly retParams: string | null;
  readonly affectedRows: number | null;
  /** Жагсаалт */
  readonly data: readonly SearchForDrugInstructionsByBarcodeData[] | null;
}

const SCHEMA: Schema = { retType: "int", retDesc: "string", retParams: "string", affectedRows: "int", data: { list: { object: { pk: "int", instruction: "string" } } } };

/**
 * WS300148_SearchForDrugInstructionsByBarcode: Эмийн хэрэглэх заавар баркодоор хайх сервис
 *
 * Provider: Эм, эмнэлгийн хэрэгслийн хяналт, зохицуулалтын газар
 */
export async function searchForDrugInstructionsByBarcode(transport: Transport, params: SearchForDrugInstructionsByBarcodeParams = {}, options: CallOptions = {}): Promise<XypResult<SearchForDrugInstructionsByBarcodeResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      barcode: params.barcode,
    },
    options,
  );
  return decodeResponse<SearchForDrugInstructionsByBarcodeResponse>("SearchForDrugInstructionsByBarcodeResponse", SCHEMA, data);
}
