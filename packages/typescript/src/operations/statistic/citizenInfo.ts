/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS200101_citizenInfo (statistic, Үндэсний статистикийн хороо)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS200101_citizenInfo";

/** Input of WS200101_citizenInfo. */
export interface CitizenInfoParams {
  /** регистрийн дугаар */
  readonly regnum?: string;
}

/** WS200101_citizenInfo: Хүн амын өрхийн мэдээллийн сангийн сервис */
export interface CitizenInfoResponse {
  /** овог */
  readonly lastName: string | null;
  /** нэр */
  readonly firstName: string | null;
  /** жил */
  readonly year: number | null;
  /** аймаг код */
  readonly provinceCode: number | null;
  /** сум дүүрэг код */
  readonly districtCode: number | null;
  /** баг хороо код */
  readonly microDistrictCode: number | null;
}

const SCHEMA: Schema = { lastName: "string", firstName: "string", year: "int", provinceCode: "int", districtCode: "int", microDistrictCode: "int" };

/**
 * WS200101_citizenInfo: Хүн амын өрхийн мэдээллийн сангийн сервис
 *
 * Provider: Үндэсний статистикийн хороо
 */
export async function citizenInfo(transport: Transport, params: CitizenInfoParams = {}, options: CallOptions = {}): Promise<XypResult<CitizenInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<CitizenInfoResponse>("CitizenInfoResponse", SCHEMA, data);
}
