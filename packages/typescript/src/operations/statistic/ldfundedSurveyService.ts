/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS200107_ldfundedSurveyService (statistic, Сангийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS200107_ldfundedSurveyService";

/** Input of WS200107_ldfundedSurveyService. */
export interface LdfundedSurveyServiceParams {
  /** Иргэний регистр */
  readonly regnum?: string;
  /** Төсөл, арга хэмжээний код */
  readonly activityCode?: number;
  /** Тийм/Үгүй утга Тийм – 1 Үгүй – 0 */
  readonly value?: number;
  /** {Санал-1-PK} /Хэрэв мэдээлэл засахаар бол энэ баганад PK онооно/ */
  readonly code?: number;
}

/** WS200107_ldfundedSurveyService: Хөгжлийн бодлого, төлөвлөлттэй уялдуулан ОНХС-ийн хөрөнгөөр хийгдэх ажлыг дэмжиж санал өгөх сервис. */
export interface LdfundedSurveyServiceResponse {
  /** Амжилттай бол “Success” Амжилтгүй бол “Алдааны мессеж”; */
  readonly result: string | null;
}

const SCHEMA: Schema = { result: "string" };

/**
 * WS200107_ldfundedSurveyService: Хөгжлийн бодлого, төлөвлөлттэй уялдуулан ОНХС-ийн хөрөнгөөр хийгдэх ажлыг дэмжиж санал өгөх сервис.
 *
 * Provider: Сангийн яам
 */
export async function ldfundedSurveyService(transport: Transport, params: LdfundedSurveyServiceParams = {}, options: CallOptions = {}): Promise<XypResult<LdfundedSurveyServiceResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      activityCode: params.activityCode,
      value: params.value,
      code: params.code,
    },
    options,
  );
  return decodeResponse<LdfundedSurveyServiceResponse>("LdfundedSurveyServiceResponse", SCHEMA, data);
}
