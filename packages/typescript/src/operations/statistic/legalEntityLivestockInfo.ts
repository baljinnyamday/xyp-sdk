/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS200118_legalEntityLivestockInfo (statistic, Үндэсний статистикийн хороо)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS200118_legalEntityLivestockInfo";

/** Input of WS200118_legalEntityLivestockInfo. */
export interface LegalEntityLivestockInfoParams {
  /** хуулийн этгээдийн регистрийн дугаар */
  readonly legalEntityRegnum?: string;
  /** Жил */
  readonly year?: number;
}

/** WS200118_legalEntityLivestockInfo: Хуулийн этгээдийн эзэмшлийн малын тоо мэдээлэл авах сервис */
export interface LegalEntityLivestockInfoResponse {
  /** аймгийн нэр */
  readonly cityName: string | null;
  /** жил */
  readonly year: number | null;
  /** сумын нэр */
  readonly districtName: string | null;
  /** багийн нэр */
  readonly bagName: string | null;
  /** хуулийн этгээдийн регистрийн дугаар */
  readonly legalEntityRegnum: string | null;
  /** хуулийн этгээдийн нэр */
  readonly legalEntityName: string | null;
  /** тэмээний тоо */
  readonly camelCount: number | null;
  /** морины тоо */
  readonly horseCount: number | null;
  /** үхрийн тоо */
  readonly cowCount: number | null;
  /** хонины тоо */
  readonly sheepCount: number | null;
  /** ямааны тоо */
  readonly goatCount: number | null;
  /** нийт малын тоо */
  readonly totalCount: number | null;
}

const SCHEMA: Schema = { cityName: "string", year: "int", districtName: "string", bagName: "string", legalEntityRegnum: "string", legalEntityName: "string", camelCount: "int", horseCount: "int", cowCount: "int", sheepCount: "int", goatCount: "int", totalCount: "int" };

/**
 * WS200118_legalEntityLivestockInfo: Хуулийн этгээдийн эзэмшлийн малын тоо мэдээлэл авах сервис
 *
 * Provider: Үндэсний статистикийн хороо
 */
export async function legalEntityLivestockInfo(transport: Transport, params: LegalEntityLivestockInfoParams = {}, options: CallOptions = {}): Promise<XypResult<LegalEntityLivestockInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      legalEntityRegnum: params.legalEntityRegnum,
      year: params.year,
    },
    options,
  );
  return decodeResponse<LegalEntityLivestockInfoResponse>("LegalEntityLivestockInfoResponse", SCHEMA, data);
}
