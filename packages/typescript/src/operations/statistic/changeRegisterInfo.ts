/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS200115_changeRegisterInfo (statistic, Улсын бүртгэлийн ерөнхий газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS200115_changeRegisterInfo";

/** Input of WS200115_changeRegisterInfo. */
export interface ChangeRegisterInfoParams {
  /** огноо */
  readonly date?: Date | string;
}

/** мэдээллийн жагсаалт */
export interface ChangeRegisterInfoListData {
  /** Шинэ регистр */
  readonly regnumOld: string | null;
  /** Хуучин регистр */
  readonly regnumNew: string | null;
  /** Регистр сольсон он сар өдөр цаг минут секунд */
  readonly date: Date | string | null;
}

/** WS200115_changeRegisterInfo: Регистр сольсон мэдээлэл */
export interface ChangeRegisterInfoResponse {
  /** мэдээллийн жагсаалт */
  readonly listData: readonly ChangeRegisterInfoListData[] | null;
}

const SCHEMA: Schema = { listData: { list: { object: { regnumOld: "string", regnumNew: "string", date: "date" } } } };

/**
 * WS200115_changeRegisterInfo: Регистр сольсон мэдээлэл
 *
 * Provider: Улсын бүртгэлийн ерөнхий газар
 */
export async function changeRegisterInfo(transport: Transport, params: ChangeRegisterInfoParams = {}, options: CallOptions = {}): Promise<XypResult<ChangeRegisterInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      date: params.date,
    },
    options,
  );
  return decodeResponse<ChangeRegisterInfoResponse>("ChangeRegisterInfoResponse", SCHEMA, data);
}
