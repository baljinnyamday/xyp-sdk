/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS200124_umnsLivestockInfo (statistic, Үндэсний статистикийн хороо)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS200124_umnsLivestockInfo";

/** Input of WS200124_umnsLivestockInfo. */
export interface UmnsLivestockInfoParams {
  /** Регистрийн дугаар */
  readonly regnum?: string;
}

/** WS200124_umnsLivestockInfo: ӨМНС малын тоо мэдээлэл авах сервис */
export interface UmnsLivestockInfoResponse {
  /** Регистрийн дугаар */
  readonly regnum: string | null;
  /** Тэмээний тоо */
  readonly camelCount: number | null;
  /** Адууны тоо */
  readonly horseCount: number | null;
  /** Үхэрний тоо */
  readonly cattleCount: number | null;
  /** Хонины тоо */
  readonly sheepCount: number | null;
  /** Ямааны тоо */
  readonly goatCount: number | null;
}

const SCHEMA: Schema = { regnum: "string", camelCount: "int", horseCount: "int", cattleCount: "int", sheepCount: "int", goatCount: "int" };

/**
 * WS200124_umnsLivestockInfo: ӨМНС малын тоо мэдээлэл авах сервис
 *
 * Provider: Үндэсний статистикийн хороо
 */
export async function umnsLivestockInfo(transport: Transport, params: UmnsLivestockInfoParams = {}, options: CallOptions = {}): Promise<XypResult<UmnsLivestockInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
    },
    options,
  );
  return decodeResponse<UmnsLivestockInfoResponse>("UmnsLivestockInfoResponse", SCHEMA, data);
}
