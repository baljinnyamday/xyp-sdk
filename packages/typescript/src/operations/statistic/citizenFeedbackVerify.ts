/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * WS200113_citizenFeedbackVerify (statistic, Сангийн яам)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "WS200113_citizenFeedbackVerify";

/** Input of WS200113_citizenFeedbackVerify. */
export interface CitizenFeedbackVerifyParams {
  /** Иргэний регистр */
  readonly regnum?: string;
  /** Төсвийн жил */
  readonly year?: number;
  /** Тийм – 1 Үгүй – 0 */
  readonly value?: number;
}

/** WS200113_citizenFeedbackVerify: Иргэн санал илгээх баталгаажуулах сервис. */
export interface CitizenFeedbackVerifyResponse {
  /** Амжилттай бол “Success” Амжилтгүй бол “Алдааны мессеж”; */
  readonly result: string | null;
}

const SCHEMA: Schema = { result: "string" };

/**
 * WS200113_citizenFeedbackVerify: Иргэн санал илгээх баталгаажуулах сервис.
 *
 * Provider: Сангийн яам
 */
export async function citizenFeedbackVerify(transport: Transport, params: CitizenFeedbackVerifyParams = {}, options: CallOptions = {}): Promise<XypResult<CitizenFeedbackVerifyResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      regnum: params.regnum,
      year: params.year,
      value: params.value,
    },
    options,
  );
  return decodeResponse<CitizenFeedbackVerifyResponse>("CitizenFeedbackVerifyResponse", SCHEMA, data);
}
