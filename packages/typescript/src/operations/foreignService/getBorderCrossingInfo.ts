/**
 * GENERATED by xyp-generator from spec/services.json. Do not edit.
 *
 * FS100111_getBorderCrossingInfo (foreignService, Гадаадын иргэн харьяатын газар)
 */

import { decodeResponse, type Schema, type XypResult } from "../../schema.js";
import type { CallOptions, Transport } from "../../transport.js";

const OPERATION = "FS100111_getBorderCrossingInfo";

/** Input of FS100111_getBorderCrossingInfo. */
export interface GetBorderCrossingInfoParams {
  /** Фрегистр */
  readonly fregistryNumber?: string;
  /** Нэр */
  readonly firstName?: string;
  /** Овог */
  readonly lastName?: string;
  /** Төрсөн огноо */
  readonly birthDate?: string;
  /** Иргэншил */
  readonly citizenShip?: string;
  /** Хүйс */
  readonly gender?: string;
  /** Хил нэвтэрсэн чиглэл */
  readonly direction?: string;
  /** Хил нэвтэрсэн огноо */
  readonly crossedDate?: string;
  /** Боомтын код */
  readonly portCode?: string;
  /** Зөвшөөрлийн дугаар */
  readonly approvalNumber?: string;
  /** Паспортын дугаар */
  readonly passportNumber?: string;
  /** Буцсан эсэх */
  readonly back?: boolean;
  /** Аялалын дугаар */
  readonly travelNumber?: string;
  /** Аялалын зорилго */
  readonly travelPurpose?: string;
}

/** FS100111_getBorderCrossingInfo: Хил нэвтрэлтийн мэдээлэл дамжуулах сервис */
export interface GetBorderCrossingInfoResponse {
  /** амжилттай эсэх */
  readonly result: boolean | null;
  /** тайлбар */
  readonly message: string | null;
}

const SCHEMA: Schema = { result: "bool", message: "string" };

/**
 * FS100111_getBorderCrossingInfo: Хил нэвтрэлтийн мэдээлэл дамжуулах сервис
 *
 * Provider: Гадаадын иргэн харьяатын газар
 */
export async function getBorderCrossingInfo(transport: Transport, params: GetBorderCrossingInfoParams = {}, options: CallOptions = {}): Promise<XypResult<GetBorderCrossingInfoResponse>> {
  const data = await transport.call(
    OPERATION,
    {
      fregistryNumber: params.fregistryNumber,
      firstName: params.firstName,
      lastName: params.lastName,
      birthDate: params.birthDate,
      citizenShip: params.citizenShip,
      gender: params.gender,
      direction: params.direction,
      crossedDate: params.crossedDate,
      portCode: params.portCode,
      approvalNumber: params.approvalNumber,
      passportNumber: params.passportNumber,
      back: params.back,
      travelNumber: params.travelNumber,
      travelPurpose: params.travelPurpose,
    },
    options,
  );
  return decodeResponse<GetBorderCrossingInfoResponse>("GetBorderCrossingInfoResponse", SCHEMA, data);
}
