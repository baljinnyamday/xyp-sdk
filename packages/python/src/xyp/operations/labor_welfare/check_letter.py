"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS100909_checkLetter (labor_welfare, Хөдөлмөр халамж, үйлчилгээний ерөнхий газар)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS100909_checkLetter"


class CheckLetterListData(XypModel):
    """Мэдээллийн жагсаалт"""

    action_num: str | None = Field(default=None, alias="actionNum", description="Дугаар")
    check_num: str | None = Field(
        default=None, alias="checkNum", description="ХХҮЕГ-ын албан бичгийн дугаар"
    )


class CheckLetterResponse(XypModel):
    """WS100909_checkLetter: Байгууллагын регистрын дугаараар ХХҮЕГ-ын албан бичгийн дугаар, төрөл, оны мэдээлэл харах"""

    list_data: list[CheckLetterListData] | None = Field(
        default=None, alias="listData", description="Мэдээллийн жагсаалт"
    )


def check_letter(
    self: SyncService,
    *,
    org_code: str | None = None,
    year_check: int | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> CheckLetterResponse:
    """WS100909_checkLetter: Байгууллагын регистрын дугаараар ХХҮЕГ-ын албан бичгийн дугаар, төрөл, оны мэдээлэл харах

    Provider: Хөдөлмөр халамж, үйлчилгээний ерөнхий газар

    Args:
        org_code: Байгууллагын регистерийн дугаар
        year_check: Он
    """
    data = self._transport.call(
        _OPERATION,
        {
            "orgCode": org_code,
            "yearCheck": year_check,
        },
        auth,
        operator,
    )
    return parse_model(CheckLetterResponse, data)


async def check_letter_async(
    self: AsyncService,
    *,
    org_code: str | None = None,
    year_check: int | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> CheckLetterResponse:
    """WS100909_checkLetter: Байгууллагын регистрын дугаараар ХХҮЕГ-ын албан бичгийн дугаар, төрөл, оны мэдээлэл харах

    Provider: Хөдөлмөр халамж, үйлчилгээний ерөнхий газар

    Args:
        org_code: Байгууллагын регистерийн дугаар
        year_check: Он
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "orgCode": org_code,
            "yearCheck": year_check,
        },
        auth,
        operator,
    )
    return parse_model(CheckLetterResponse, data)
