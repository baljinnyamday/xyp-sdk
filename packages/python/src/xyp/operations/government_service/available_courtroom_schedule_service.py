"""GENERATED by xyp-generator from spec/services.json. Do not edit.

GS10080_AvailableCourtroomScheduleService (government_service, Шүүхийн Шинжилгээний Ерөнхий Газар)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from typing import Any

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "GS10080_AvailableCourtroomScheduleService"


class AvailableCourtroomScheduleServiceListData(XypModel):
    """Жагсаалт"""

    hall_id: str | None = Field(default=None, alias="hallId")
    hall_name: str | None = Field(default=None, alias="hallName")
    date: str | None = Field(default=None, alias="date")
    list_: list[Any] | None = Field(default=None, alias="list", description="Жагсаалт")


class AvailableCourtroomScheduleServiceResponse(XypModel):
    """GS10080_AvailableCourtroomScheduleService: Сул танхим, цаг дуудах сервис"""

    list_data: list[AvailableCourtroomScheduleServiceListData] | None = Field(
        default=None, alias="listData", description="Жагсаалт"
    )


def available_courtroom_schedule_service(
    self: SyncService,
    *,
    date: str | None = None,
    deceased_info_id: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> AvailableCourtroomScheduleServiceResponse:
    """GS10080_AvailableCourtroomScheduleService: Сул танхим, цаг дуудах сервис

    Provider: Шүүхийн Шинжилгээний Ерөнхий Газар

    Args:
        date: date
        deceased_info_id: deceasedInfoId
    """
    data = self._transport.call(
        _OPERATION,
        {
            "date": date,
            "deceasedInfoId": deceased_info_id,
        },
        auth,
        operator,
    )
    return parse_model(AvailableCourtroomScheduleServiceResponse, data)


async def available_courtroom_schedule_service_async(
    self: AsyncService,
    *,
    date: str | None = None,
    deceased_info_id: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> AvailableCourtroomScheduleServiceResponse:
    """GS10080_AvailableCourtroomScheduleService: Сул танхим, цаг дуудах сервис

    Provider: Шүүхийн Шинжилгээний Ерөнхий Газар

    Args:
        date: date
        deceased_info_id: deceasedInfoId
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "date": date,
            "deceasedInfoId": deceased_info_id,
        },
        auth,
        operator,
    )
    return parse_model(AvailableCourtroomScheduleServiceResponse, data)
