"""GENERATED by xyp-generator from spec/services.json. Do not edit.

GS10066_CreateComplaintRating (government_service, Засгийн газрын Хэрэг эрхлэх газар)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from collections.abc import Sequence
from typing import Any

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "GS10066_CreateComplaintRating"


class CreateComplaintRatingResponse(XypModel):
    """GS10066_CreateComplaintRating: Санал асуулга Үнэлгээ өгөх"""

    rating_id: int | None = Field(default=None, alias="ratingId")


def create_complaint_rating(
    self: SyncService,
    *,
    session_id: int | None = None,
    last_department: str | None = None,
    rating: int | None = None,
    description: str | None = None,
    rating_reason_ids: Sequence[Any] | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> CreateComplaintRatingResponse:
    """GS10066_CreateComplaintRating: Санал асуулга Үнэлгээ өгөх

    Provider: Засгийн газрын Хэрэг эрхлэх газар

    Args:
        session_id: sessionId
        last_department: lastDepartment
        rating: rating
        description: description
        rating_reason_ids: ratingReasonIds
    """
    data = self._transport.call(
        _OPERATION,
        {
            "sessionId": session_id,
            "lastDepartment": last_department,
            "rating": rating,
            "description": description,
            "ratingReasonIds": rating_reason_ids,
        },
        auth,
        operator,
    )
    return parse_model(CreateComplaintRatingResponse, data)


async def create_complaint_rating_async(
    self: AsyncService,
    *,
    session_id: int | None = None,
    last_department: str | None = None,
    rating: int | None = None,
    description: str | None = None,
    rating_reason_ids: Sequence[Any] | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> CreateComplaintRatingResponse:
    """GS10066_CreateComplaintRating: Санал асуулга Үнэлгээ өгөх

    Provider: Засгийн газрын Хэрэг эрхлэх газар

    Args:
        session_id: sessionId
        last_department: lastDepartment
        rating: rating
        description: description
        rating_reason_ids: ratingReasonIds
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "sessionId": session_id,
            "lastDepartment": last_department,
            "rating": rating,
            "description": description,
            "ratingReasonIds": rating_reason_ids,
        },
        auth,
        operator,
    )
    return parse_model(CreateComplaintRatingResponse, data)
