"""GENERATED by xyp-generator from spec/services.json. Do not edit.

GS10085_RiskReferenceService (government_service, И-Монголиа академи)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from collections.abc import Sequence
from typing import Any

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "GS10085_RiskReferenceService"


class RiskReferenceServiceEnvs(XypModel):
    """Эрсдлийн орчин"""

    public_id: str | None = Field(default=None, alias="publicId")
    id: str | None = Field(default=None, alias="id")
    title: str | None = Field(default=None, alias="title")


class RiskReferenceServiceTypes(XypModel):
    """Эрсдлийн төрөл"""

    id: str | None = Field(default=None, alias="id")
    public_id: str | None = Field(default=None, alias="publicId")
    title: str | None = Field(default=None, alias="title")


class RiskReferenceServiceCtypes(XypModel):
    """Эрсдэл бүртгэгч (хувь хүн, байгууллага)"""

    id: str | None = Field(default=None, alias="id")
    public_id: str | None = Field(default=None, alias="publicId")
    title: str | None = Field(default=None, alias="title")


class RiskReferenceServiceStatus(XypModel):
    """Эрсдлийн төлөв"""

    id: str | None = Field(default=None, alias="id")
    public_id: str | None = Field(default=None, alias="publicId")
    title: str | None = Field(default=None, alias="title")
    desc: str | None = Field(default=None, alias="desc")


class RiskReferenceServiceRisks(XypModel):
    """risks - Эрсдлийн жагсаалт харах"""

    id: str | None = Field(default=None, alias="id")
    public_id: str | None = Field(default=None, alias="publicId")
    ctpid: str | None = Field(default=None, alias="ctpid")
    sttpid: str | None = Field(default=None, alias="sttpid")
    desc: str | None = Field(default=None, alias="desc")
    lt: str | None = Field(default=None, alias="lt")
    ln: str | None = Field(default=None, alias="ln")
    civilid: str | None = Field(default=None, alias="civilid")
    ctypes: Any | None = Field(default=None, alias="ctypes")
    status: Any | None = Field(default=None, alias="status")
    types: Any | None = Field(default=None, alias="types")
    systems: Any | None = Field(default=None, alias="systems")


class RiskReferenceServiceResponse(XypModel):
    """GS10085_RiskReferenceService: Эрсдлийн лавлах сан"""

    envs: list[RiskReferenceServiceEnvs] | None = Field(
        default=None, alias="envs", description="Эрсдлийн орчин"
    )
    types: list[RiskReferenceServiceTypes] | None = Field(
        default=None, alias="types", description="Эрсдлийн төрөл"
    )
    ctypes: list[RiskReferenceServiceCtypes] | None = Field(
        default=None, alias="ctypes", description="Эрсдэл бүртгэгч (хувь хүн, байгууллага)"
    )
    status: list[RiskReferenceServiceStatus] | None = Field(
        default=None, alias="status", description="Эрсдлийн төлөв"
    )
    risks: list[RiskReferenceServiceRisks] | None = Field(
        default=None, alias="risks", description="risks - Эрсдлийн жагсаалт харах"
    )


def risk_reference_service(
    self: SyncService,
    *,
    only: Sequence[str] | None = None,
    systempid: str | None = None,
    orgregnum: str | None = None,
    page: int | None = None,
    limit: int | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> RiskReferenceServiceResponse:
    """GS10085_RiskReferenceService: Эрсдлийн лавлах сан

    Provider: И-Монголиа академи

    Args:
        only: лавлах сангуудын шүүлт (заавал биш). Зөвшөөрөгдөх утга: envs, types, ctypes, status.
        systempid: Эрсдлийн жагсаалт харах systempid шүүлт (UUID) (заавал биш)
        orgregnum: Эрсдлийн жагсаалт харах orgregnum шүүлт orgregnum ба systempid хоёуланг өгвөл AND шүүлт (заавал биш)
        page: Эрсдлийн жагсаалт хуудасны дугаар (default 1) (заавал биш)
        limit: Эрсдлийн жагсаалт хуудас тутмын тоо (default 20, max 100) (заавал биш)
    """
    data = self._transport.call(
        _OPERATION,
        {
            "only": only,
            "systempid": systempid,
            "orgregnum": orgregnum,
            "page": page,
            "limit": limit,
        },
        auth,
        operator,
    )
    return parse_model(RiskReferenceServiceResponse, data)


async def risk_reference_service_async(
    self: AsyncService,
    *,
    only: Sequence[str] | None = None,
    systempid: str | None = None,
    orgregnum: str | None = None,
    page: int | None = None,
    limit: int | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> RiskReferenceServiceResponse:
    """GS10085_RiskReferenceService: Эрсдлийн лавлах сан

    Provider: И-Монголиа академи

    Args:
        only: лавлах сангуудын шүүлт (заавал биш). Зөвшөөрөгдөх утга: envs, types, ctypes, status.
        systempid: Эрсдлийн жагсаалт харах systempid шүүлт (UUID) (заавал биш)
        orgregnum: Эрсдлийн жагсаалт харах orgregnum шүүлт orgregnum ба systempid хоёуланг өгвөл AND шүүлт (заавал биш)
        page: Эрсдлийн жагсаалт хуудасны дугаар (default 1) (заавал биш)
        limit: Эрсдлийн жагсаалт хуудас тутмын тоо (default 20, max 100) (заавал биш)
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "only": only,
            "systempid": systempid,
            "orgregnum": orgregnum,
            "page": page,
            "limit": limit,
        },
        auth,
        operator,
    )
    return parse_model(RiskReferenceServiceResponse, data)
