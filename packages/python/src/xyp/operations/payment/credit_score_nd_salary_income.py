"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS100858_CreditScoreNDSalaryIncome (payment, Бүрэн Скор ЗМС ХХК)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from typing import Any

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS100858_CreditScoreNDSalaryIncome"


class CreditScoreNdSalaryIncomeResponse(XypModel):
    """WS100858_CreditScoreNDSalaryIncome: Зээлжих зэрэглэлийн оноо /Бүрэн оноо/-г зөвхөн “Цалингийн орлого /НДШ/” орлогоор тооцох"""

    data: Any | None = Field(default=None, alias="data")
    rest_inquiry_summary_response: Any | None = Field(
        default=None, alias="restInquirySummaryResponse"
    )
    score: int | None = Field(default=None, alias="score")
    vendor: str | None = Field(default=None, alias="vendor")
    state_histories_data: list[Any] | None = Field(default=None, alias="stateHistoriesData")


def credit_score_nd_salary_income(
    self: SyncService,
    *,
    regnum: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> CreditScoreNdSalaryIncomeResponse:
    """WS100858_CreditScoreNDSalaryIncome: Зээлжих зэрэглэлийн оноо /Бүрэн оноо/-г зөвхөн “Цалингийн орлого /НДШ/” орлогоор тооцох

    Provider: Бүрэн Скор ЗМС ХХК

    Args:
        regnum: regnum
    """
    data = self._transport.call(
        _OPERATION,
        {
            "regnum": regnum,
        },
        auth,
        operator,
    )
    return parse_model(CreditScoreNdSalaryIncomeResponse, data)


async def credit_score_nd_salary_income_async(
    self: AsyncService,
    *,
    regnum: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> CreditScoreNdSalaryIncomeResponse:
    """WS100858_CreditScoreNDSalaryIncome: Зээлжих зэрэглэлийн оноо /Бүрэн оноо/-г зөвхөн “Цалингийн орлого /НДШ/” орлогоор тооцох

    Provider: Бүрэн Скор ЗМС ХХК

    Args:
        regnum: regnum
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "regnum": regnum,
        },
        auth,
        operator,
    )
    return parse_model(CreditScoreNdSalaryIncomeResponse, data)
