"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS100495_ECLicenseCreator (transport, Нийтийн тээврийн бодлогын газар)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS100495_ECLicenseCreator"


class EcLicenseCreatorResponse(XypModel):
    """WS100495_ECLicenseCreator: EC лиценз үүсгэх сервис"""

    pdf: str | None = Field(default=None, alias="pdf", description="pdf")
    licence_id: int | None = Field(default=None, alias="licenceId", description="licenceId")
    message: str | None = Field(default=None, alias="message", description="Хариу мессеж")
    season: str | None = Field(default=None, alias="season", description="Улирлын нэр")


def ec_license_creator(
    self: SyncService,
    *,
    max_load: int | None = None,
    regnum: str | None = None,
    plate_no: str | None = None,
    last_name: str | None = None,
    app_token: str | None = None,
    user_type: str | None = None,
    first_name: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> EcLicenseCreatorResponse:
    """WS100495_ECLicenseCreator: EC лиценз үүсгэх сервис

    Provider: Нийтийн тээврийн бодлогын газар

    Args:
        max_load: maxLoad
        regnum: regnum
        plate_no: plateNo
        last_name: lastName
        app_token: Системийн баталгаажуулалтын токен
        user_type: userType
        first_name: firstName
    """
    data = self._transport.call(
        _OPERATION,
        {
            "maxLoad": max_load,
            "regnum": regnum,
            "plateNo": plate_no,
            "lastName": last_name,
            "appToken": app_token,
            "userType": user_type,
            "firstName": first_name,
        },
        auth,
        operator,
    )
    return parse_model(EcLicenseCreatorResponse, data)


async def ec_license_creator_async(
    self: AsyncService,
    *,
    max_load: int | None = None,
    regnum: str | None = None,
    plate_no: str | None = None,
    last_name: str | None = None,
    app_token: str | None = None,
    user_type: str | None = None,
    first_name: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> EcLicenseCreatorResponse:
    """WS100495_ECLicenseCreator: EC лиценз үүсгэх сервис

    Provider: Нийтийн тээврийн бодлогын газар

    Args:
        max_load: maxLoad
        regnum: regnum
        plate_no: plateNo
        last_name: lastName
        app_token: Системийн баталгаажуулалтын токен
        user_type: userType
        first_name: firstName
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "maxLoad": max_load,
            "regnum": regnum,
            "plateNo": plate_no,
            "lastName": last_name,
            "appToken": app_token,
            "userType": user_type,
            "firstName": first_name,
        },
        auth,
        operator,
    )
    return parse_model(EcLicenseCreatorResponse, data)
