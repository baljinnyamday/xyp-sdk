"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS100496_ECLicenseChecker (transport, Нийтийн тээврийн бодлогын газар)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS100496_ECLicenseChecker"


class EcLicenseCheckerResponse(XypModel):
    """WS100496_ECLicenseChecker: EC лиценз шалгах сервис"""

    licence_id: int | None = Field(default=None, alias="licenceId", description="EC лиценз дугаар")
    is_ec_licence: bool | None = Field(
        default=None, alias="isEcLicence", description="EC лицензтэй эсэх"
    )
    message: str | None = Field(default=None, alias="message", description="Алдааны мессеж")


def ec_license_checker(
    self: SyncService,
    *,
    plate_no: str | None = None,
    app_token: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> EcLicenseCheckerResponse:
    """WS100496_ECLicenseChecker: EC лиценз шалгах сервис

    Provider: Нийтийн тээврийн бодлогын газар

    Args:
        plate_no: Тээврийн хэрэгслийн улсын дугаар
        app_token: Системийн баталгаажуулалтын токен
    """
    data = self._transport.call(
        _OPERATION,
        {
            "plateNo": plate_no,
            "appToken": app_token,
        },
        auth,
        operator,
    )
    return parse_model(EcLicenseCheckerResponse, data)


async def ec_license_checker_async(
    self: AsyncService,
    *,
    plate_no: str | None = None,
    app_token: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> EcLicenseCheckerResponse:
    """WS100496_ECLicenseChecker: EC лиценз шалгах сервис

    Provider: Нийтийн тээврийн бодлогын газар

    Args:
        plate_no: Тээврийн хэрэгслийн улсын дугаар
        app_token: Системийн баталгаажуулалтын токен
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "plateNo": plate_no,
            "appToken": app_token,
        },
        auth,
        operator,
    )
    return parse_model(EcLicenseCheckerResponse, data)
