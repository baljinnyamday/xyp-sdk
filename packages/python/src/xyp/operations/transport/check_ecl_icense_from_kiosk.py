"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS100434_checkECLIcenseFromKiosk (transport, Үндэсний дата төв)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypDate, XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS100434_checkECLIcenseFromKiosk"


class CheckEclIcenseFromKioskResponse(XypModel):
    """WS100434_checkECLIcenseFromKiosk: 6 сар тутамд олгодог нийслэлийн нутаг дэвсгэрт ачаа тээвэрлэх лавлагааг ТҮЦ машинаас авсан эсэхийг шалгах сервис"""

    id: int | None = Field(default=None, alias="id", description="Дэс дугаар")
    kiosk_name: str | None = Field(
        default=None, alias="kioskName", description="Лавлагаа авсан ТҮЦ машины К дугаар"
    )
    service_fee: int | None = Field(
        default=None, alias="serviceFee", description="Үйлчилгээний төлбөр"
    )
    service_date: XypDate | None = Field(
        default=None, alias="serviceDate", description="Үйлчилгээ авсан огноо"
    )


def check_ecl_icense_from_kiosk(
    self: SyncService,
    *,
    service_date: str | None = None,
    car_number: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> CheckEclIcenseFromKioskResponse:
    """WS100434_checkECLIcenseFromKiosk: 6 сар тутамд олгодог нийслэлийн нутаг дэвсгэрт ачаа тээвэрлэх лавлагааг ТҮЦ машинаас авсан эсэхийг шалгах сервис

    Provider: Үндэсний дата төв

    Args:
        service_date: Тухайн үйлчилгээг авсан огноо (Утга оруулаагү
        car_number: Шалгах машины дугаар
    """
    data = self._transport.call(
        _OPERATION,
        {
            "serviceDate": service_date,
            "carNumber": car_number,
        },
        auth,
        operator,
    )
    return parse_model(CheckEclIcenseFromKioskResponse, data)


async def check_ecl_icense_from_kiosk_async(
    self: AsyncService,
    *,
    service_date: str | None = None,
    car_number: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> CheckEclIcenseFromKioskResponse:
    """WS100434_checkECLIcenseFromKiosk: 6 сар тутамд олгодог нийслэлийн нутаг дэвсгэрт ачаа тээвэрлэх лавлагааг ТҮЦ машинаас авсан эсэхийг шалгах сервис

    Provider: Үндэсний дата төв

    Args:
        service_date: Тухайн үйлчилгээг авсан огноо (Утга оруулаагү
        car_number: Шалгах машины дугаар
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "serviceDate": service_date,
            "carNumber": car_number,
        },
        auth,
        operator,
    )
    return parse_model(CheckEclIcenseFromKioskResponse, data)
