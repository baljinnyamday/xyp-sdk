"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS100409_getVehicleInspectionInfo (transport, Авто тээврийн үндэсний төв)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypDate, XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS100409_getVehicleInspectionInfo"


class GetVehicleInspectionInfoResponse(XypModel):
    """WS100409_getVehicleInspectionInfo: Тээврийн хэрэгслийн оншилгооний мэдээлэл шалгах сервис"""

    vehicle_number: str | None = Field(
        default=None, alias="vehicleNumber", description="Тээврийн хэрэгслийн дугаар"
    )
    cabin_number: str | None = Field(default=None, alias="cabinNumber", description="Арлын дугаар")
    check_date: XypDate | None = Field(
        default=None, alias="checkDate", description="Үзлэгт орсон огноо"
    )
    expire_date: XypDate | None = Field(
        default=None, alias="expireDate", description="Үзлэгт дахин орох огноо"
    )
    is_passed: bool | None = Field(default=None, alias="isPassed", description="Тэнцсэн эсэх")


def get_vehicle_inspection_info(
    self: SyncService,
    *,
    cabin_number: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> GetVehicleInspectionInfoResponse:
    """WS100409_getVehicleInspectionInfo: Тээврийн хэрэгслийн оншилгооний мэдээлэл шалгах сервис

    Provider: Авто тээврийн үндэсний төв

    Args:
        cabin_number: Тээврийн хэрэгслийн арлын дугаар
    """
    data = self._transport.call(
        _OPERATION,
        {
            "cabinNumber": cabin_number,
        },
        auth,
        operator,
    )
    return parse_model(GetVehicleInspectionInfoResponse, data)


async def get_vehicle_inspection_info_async(
    self: AsyncService,
    *,
    cabin_number: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> GetVehicleInspectionInfoResponse:
    """WS100409_getVehicleInspectionInfo: Тээврийн хэрэгслийн оншилгооний мэдээлэл шалгах сервис

    Provider: Авто тээврийн үндэсний төв

    Args:
        cabin_number: Тээврийн хэрэгслийн арлын дугаар
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "cabinNumber": cabin_number,
        },
        auth,
        operator,
    )
    return parse_model(GetVehicleInspectionInfoResponse, data)
