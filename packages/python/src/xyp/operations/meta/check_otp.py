"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS100009_checkOtp (meta, E-Mongolia)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS100009_checkOtp"


class CheckOtpResponse(XypModel):
    """WS100009_checkOtp: нэг удаагийн нууц үг шалгах сервис"""

    is_match: bool | None = Field(default=None, alias="isMatch", description="таарсан")


def check_otp(
    self: SyncService,
    *,
    otp: str | None = None,
    regnum: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> CheckOtpResponse:
    """WS100009_checkOtp: нэг удаагийн нууц үг шалгах сервис

    Provider: E-Mongolia

    Args:
        otp: otp
        regnum: regnum
    """
    data = self._transport.call(
        _OPERATION,
        {
            "otp": otp,
            "regnum": regnum,
        },
        auth,
        operator,
    )
    return parse_model(CheckOtpResponse, data)


async def check_otp_async(
    self: AsyncService,
    *,
    otp: str | None = None,
    regnum: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> CheckOtpResponse:
    """WS100009_checkOtp: нэг удаагийн нууц үг шалгах сервис

    Provider: E-Mongolia

    Args:
        otp: otp
        regnum: regnum
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "otp": otp,
            "regnum": regnum,
        },
        auth,
        operator,
    )
    return parse_model(CheckOtpResponse, data)
