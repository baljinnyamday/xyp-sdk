"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS100563_funeralAllowanceInfo (insurance, Нийгмийн даатгалын ерөнхий газар)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS100563_funeralAllowanceInfo"


class FuneralAllowanceInfoResponse(XypModel):
    """WS100563_funeralAllowanceInfo: Нийгмийн даатгалын сангаас оршуулгын тэтгэмж авсан эсэх"""

    is_received: bool | None = Field(
        default=None, alias="isReceived", description="оршуулгын тэтгэмж авсан эсэх"
    )


def funeral_allowance_info(
    self: SyncService,
    *,
    regnum: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> FuneralAllowanceInfoResponse:
    """WS100563_funeralAllowanceInfo: Нийгмийн даатгалын сангаас оршуулгын тэтгэмж авсан эсэх

    Provider: Нийгмийн даатгалын ерөнхий газар

    Args:
        regnum: Регистрийн дугаар
    """
    data = self._transport.call(
        _OPERATION,
        {
            "regnum": regnum,
        },
        auth,
        operator,
    )
    return parse_model(FuneralAllowanceInfoResponse, data)


async def funeral_allowance_info_async(
    self: AsyncService,
    *,
    regnum: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> FuneralAllowanceInfoResponse:
    """WS100563_funeralAllowanceInfo: Нийгмийн даатгалын сангаас оршуулгын тэтгэмж авсан эсэх

    Provider: Нийгмийн даатгалын ерөнхий газар

    Args:
        regnum: Регистрийн дугаар
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "regnum": regnum,
        },
        auth,
        operator,
    )
    return parse_model(FuneralAllowanceInfoResponse, data)
