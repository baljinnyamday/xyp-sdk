"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS101501_InvestmentApplicationService (insurance, Нийгмийн даатгалын ерөнхий газар)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS101501_InvestmentApplicationService"


class InvestmentApplicationServiceResponse(XypModel):
    """WS101501_InvestmentApplicationService: ХӨРӨНГӨ ОРУУЛАХ ХҮСЭЛТ ИЛГЭЭХ"""

    regnum: str | None = Field(default=None, alias="regnum", description="Регистр дугаар")
    bank_id: int | None = Field(default=None, alias="bankId", description="Банкны дугаар")
    app_year: int | None = Field(default=None, alias="appYear", description="Хүсэлт гаргасан он")
    amount: int | None = Field(default=None, alias="amount", description="Хөрөнгө оруулалтын дүн")
    monetize_type: str | None = Field(
        default=None, alias="monetizeType", description="Санхүүжилтийн төрөл"
    )
    status: str | None = Field(default=None, alias="status", description="Хүсэлтийн төлөв")


def investment_application_service(
    self: SyncService,
    *,
    regnum: str | None = None,
    app_year: int | None = None,
    amount: float | None = None,
    monetize_type: str | None = None,
    bank_id: int | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> InvestmentApplicationServiceResponse:
    """WS101501_InvestmentApplicationService: ХӨРӨНГӨ ОРУУЛАХ ХҮСЭЛТ ИЛГЭЭХ

    Provider: Нийгмийн даатгалын ерөнхий газар

    Args:
        regnum: Регистрийн дугаар
        app_year: Он
        amount: Мөнгөжүүлсэн дүн
        monetize_type: Хөрөнгө оруулах санхүүгийн хэрэгсэл сонгосон code
        bank_id: Хөрөнгө оруулах арилжааны банкууд сонгосон банк id
    """
    data = self._transport.call(
        _OPERATION,
        {
            "regnum": regnum,
            "appYear": app_year,
            "amount": amount,
            "monetizeType": monetize_type,
            "bankId": bank_id,
        },
        auth,
        operator,
    )
    return parse_model(InvestmentApplicationServiceResponse, data)


async def investment_application_service_async(
    self: AsyncService,
    *,
    regnum: str | None = None,
    app_year: int | None = None,
    amount: float | None = None,
    monetize_type: str | None = None,
    bank_id: int | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> InvestmentApplicationServiceResponse:
    """WS101501_InvestmentApplicationService: ХӨРӨНГӨ ОРУУЛАХ ХҮСЭЛТ ИЛГЭЭХ

    Provider: Нийгмийн даатгалын ерөнхий газар

    Args:
        regnum: Регистрийн дугаар
        app_year: Он
        amount: Мөнгөжүүлсэн дүн
        monetize_type: Хөрөнгө оруулах санхүүгийн хэрэгсэл сонгосон code
        bank_id: Хөрөнгө оруулах арилжааны банкууд сонгосон банк id
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "regnum": regnum,
            "appYear": app_year,
            "amount": amount,
            "monetizeType": monetize_type,
            "bankId": bank_id,
        },
        auth,
        operator,
    )
    return parse_model(InvestmentApplicationServiceResponse, data)
