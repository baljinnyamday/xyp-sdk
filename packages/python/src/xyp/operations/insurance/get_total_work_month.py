"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS100584_GetTotalWorkMonth (insurance, Нийгмийн даатгалын ерөнхий газар)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS100584_GetTotalWorkMonth"


class GetTotalWorkMonthResponse(XypModel):
    """WS100584_GetTotalWorkMonth: Даатгуулагчийн 2006 оноос хойшхи нийт ажилласан сарын тоо"""

    total_work_month: int | None = Field(
        default=None, alias="totalWorkMonth", description="Сарын тоо"
    )


def get_total_work_month(
    self: SyncService,
    *,
    regnum: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> GetTotalWorkMonthResponse:
    """WS100584_GetTotalWorkMonth: Даатгуулагчийн 2006 оноос хойшхи нийт ажилласан сарын тоо

    Provider: Нийгмийн даатгалын ерөнхий газар

    Args:
        regnum: регистэрийн дугаар
    """
    data = self._transport.call(
        _OPERATION,
        {
            "regnum": regnum,
        },
        auth,
        operator,
    )
    return parse_model(GetTotalWorkMonthResponse, data)


async def get_total_work_month_async(
    self: AsyncService,
    *,
    regnum: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> GetTotalWorkMonthResponse:
    """WS100584_GetTotalWorkMonth: Даатгуулагчийн 2006 оноос хойшхи нийт ажилласан сарын тоо

    Provider: Нийгмийн даатгалын ерөнхий газар

    Args:
        regnum: регистэрийн дугаар
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "regnum": regnum,
        },
        auth,
        operator,
    )
    return parse_model(GetTotalWorkMonthResponse, data)
