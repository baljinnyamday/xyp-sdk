"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS300102_eHealthGetHospitals (health, Эрүүл мэндийн яам)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS300102_eHealthGetHospitals"


class EHealthGetHospitalsListData(XypModel):
    """эмнэлгийн жагсаалт"""

    hospital_id: int | None = Field(
        default=None, alias="hospitalId", description="эмнэлгийн дугаар"
    )
    hospital_name: str | None = Field(
        default=None, alias="hospitalName", description="эмнэлгийн нэр"
    )
    priority: str | None = Field(default=None, alias="priority", description="эрэмбэ")


class EHealthGetHospitalsResponse(XypModel):
    """WS300102_eHealthGetHospitals: EHealth эмнэлгийн жагсаалт дуудах сервис"""

    list_data: list[EHealthGetHospitalsListData] | None = Field(
        default=None, alias="listData", description="эмнэлгийн жагсаалт"
    )


def e_health_get_hospitals(
    self: SyncService,
    *,
    regnum: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> EHealthGetHospitalsResponse:
    """WS300102_eHealthGetHospitals: EHealth эмнэлгийн жагсаалт дуудах сервис

    Provider: Эрүүл мэндийн яам

    Args:
        regnum: регистрийн дугаар;
    """
    data = self._transport.call(
        _OPERATION,
        {
            "regnum": regnum,
        },
        auth,
        operator,
    )
    return parse_model(EHealthGetHospitalsResponse, data)


async def e_health_get_hospitals_async(
    self: AsyncService,
    *,
    regnum: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> EHealthGetHospitalsResponse:
    """WS300102_eHealthGetHospitals: EHealth эмнэлгийн жагсаалт дуудах сервис

    Provider: Эрүүл мэндийн яам

    Args:
        regnum: регистрийн дугаар;
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "regnum": regnum,
        },
        auth,
        operator,
    )
    return parse_model(EHealthGetHospitalsResponse, data)
