"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS300172_GetChildBookCheckUps (health, Эрүүл мэндийн яам)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS300172_GetChildBookCheckUps"


class GetChildBookCheckUpsResult(XypModel):
    """Жагсаалт"""

    check_up_id: int | None = Field(default=None, alias="checkUpId")
    check_up_name: str | None = Field(default=None, alias="checkUpName")


class GetChildBookCheckUpsResponse(XypModel):
    """WS300172_GetChildBookCheckUps: Эх хүүхдийн дэвтрийн эрүүл мэндийн үзлэгийн жагсаалт татах сервис"""

    result: list[GetChildBookCheckUpsResult] | None = Field(
        default=None, alias="result", description="Жагсаалт"
    )


def get_child_book_check_ups(
    self: SyncService,
    *,
    type: int | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> GetChildBookCheckUpsResponse:
    """WS300172_GetChildBookCheckUps: Эх хүүхдийн дэвтрийн эрүүл мэндийн үзлэгийн жагсаалт татах сервис

    Provider: Эрүүл мэндийн яам

    Args:
        type: 1- Хүүхэд, 2- Ээж
    """
    data = self._transport.call(
        _OPERATION,
        {
            "type": type,
        },
        auth,
        operator,
    )
    return parse_model(GetChildBookCheckUpsResponse, data)


async def get_child_book_check_ups_async(
    self: AsyncService,
    *,
    type: int | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> GetChildBookCheckUpsResponse:
    """WS300172_GetChildBookCheckUps: Эх хүүхдийн дэвтрийн эрүүл мэндийн үзлэгийн жагсаалт татах сервис

    Provider: Эрүүл мэндийн яам

    Args:
        type: 1- Хүүхэд, 2- Ээж
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "type": type,
        },
        auth,
        operator,
    )
    return parse_model(GetChildBookCheckUpsResponse, data)
