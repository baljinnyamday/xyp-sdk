"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS300112_eHealthTimeOrderCancel (health, Эрүүл мэндийн яам)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS300112_eHealthTimeOrderCancel"


class EHealthTimeOrderCancelResponse(XypModel):
    """WS300112_eHealthTimeOrderCancel: EHealth Цаг захиалгын бүртгэл цуцлах"""

    check_code: int | None = Field(
        default=None, alias="checkCode", description="хүсэлтийн хариу код"
    )


def e_health_time_order_cancel(
    self: SyncService,
    *,
    hospital_id: int | None = None,
    verify_code: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> EHealthTimeOrderCancelResponse:
    """WS300112_eHealthTimeOrderCancel: EHealth Цаг захиалгын бүртгэл цуцлах

    Provider: Эрүүл мэндийн яам

    Args:
        hospital_id: Эмнэлгийн дугаар
        verify_code: Баталгаажуулах код
    """
    data = self._transport.call(
        _OPERATION,
        {
            "hospitalId": hospital_id,
            "verifyCode": verify_code,
        },
        auth,
        operator,
    )
    return parse_model(EHealthTimeOrderCancelResponse, data)


async def e_health_time_order_cancel_async(
    self: AsyncService,
    *,
    hospital_id: int | None = None,
    verify_code: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> EHealthTimeOrderCancelResponse:
    """WS300112_eHealthTimeOrderCancel: EHealth Цаг захиалгын бүртгэл цуцлах

    Provider: Эрүүл мэндийн яам

    Args:
        hospital_id: Эмнэлгийн дугаар
        verify_code: Баталгаажуулах код
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "hospitalId": hospital_id,
            "verifyCode": verify_code,
        },
        auth,
        operator,
    )
    return parse_model(EHealthTimeOrderCancelResponse, data)
