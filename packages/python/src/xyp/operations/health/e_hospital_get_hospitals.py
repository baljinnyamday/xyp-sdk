"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS300132_eHospitalGetHospitals (health, Эрүүл мэндийн даатгалын ерөнхий газар)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS300132_eHospitalGetHospitals"


class EHospitalGetHospitalsListData(XypModel):
    """эмнэлгийн жагсаалт"""

    hospital_id: int | None = Field(
        default=None, alias="hospitalId", description="эмнэлгийн дугаар"
    )
    hospital_name: str | None = Field(
        default=None, alias="hospitalName", description="эмнэлгийн нэр"
    )
    priority: str | None = Field(default=None, alias="priority", description="эрэмбэ")


class EHospitalGetHospitalsResponse(XypModel):
    """WS300132_eHospitalGetHospitals: EHospital эмнэлгийн жагсаалт дуудах сервис"""

    list_data: list[EHospitalGetHospitalsListData] | None = Field(
        default=None, alias="listData", description="эмнэлгийн жагсаалт"
    )


def e_hospital_get_hospitals(
    self: SyncService,
    *,
    is_gov: int | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> EHospitalGetHospitalsResponse:
    """WS300132_eHospitalGetHospitals: EHospital эмнэлгийн жагсаалт дуудах сервис

    Provider: Эрүүл мэндийн даатгалын ерөнхий газар

    Args:
        is_gov: isGov
    """
    data = self._transport.call(
        _OPERATION,
        {
            "isGov": is_gov,
        },
        auth,
        operator,
    )
    return parse_model(EHospitalGetHospitalsResponse, data)


async def e_hospital_get_hospitals_async(
    self: AsyncService,
    *,
    is_gov: int | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> EHospitalGetHospitalsResponse:
    """WS300132_eHospitalGetHospitals: EHospital эмнэлгийн жагсаалт дуудах сервис

    Provider: Эрүүл мэндийн даатгалын ерөнхий газар

    Args:
        is_gov: isGov
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "isGov": is_gov,
        },
        auth,
        operator,
    )
    return parse_model(EHospitalGetHospitalsResponse, data)
