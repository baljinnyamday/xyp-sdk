"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS300117_earlyDetectionService (health, Эрүүл мэндийн яам)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS300117_earlyDetectionService"


class EarlyDetectionServiceResponse(XypModel):
    """WS300117_earlyDetectionService: Эрт илрүүлэгийн сервис"""

    status: int | None = Field(default=None, alias="status", description="Төлөв")
    url: str | None = Field(default=None, alias="url", description="pdf татах эсвэл асуумжын линк")


def early_detection_service(
    self: SyncService,
    *,
    regnum: str | None = None,
    report: str | None = None,
    type: int | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> EarlyDetectionServiceResponse:
    """WS300117_earlyDetectionService: Эрт илрүүлэгийн сервис

    Provider: Эрүүл мэндийн яам

    Args:
        regnum: регистрийн дугаар
        report: Эрт илрүүлэгийн тайлангийн дугаар 2022 оных тогтмол 20220301-001
        type: төрөл 1 бол Хариуны PDF хаяг 2 бол Асуумж бөглөх хаяг авах
    """
    data = self._transport.call(
        _OPERATION,
        {
            "regnum": regnum,
            "report": report,
            "type": type,
        },
        auth,
        operator,
    )
    return parse_model(EarlyDetectionServiceResponse, data)


async def early_detection_service_async(
    self: AsyncService,
    *,
    regnum: str | None = None,
    report: str | None = None,
    type: int | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> EarlyDetectionServiceResponse:
    """WS300117_earlyDetectionService: Эрт илрүүлэгийн сервис

    Provider: Эрүүл мэндийн яам

    Args:
        regnum: регистрийн дугаар
        report: Эрт илрүүлэгийн тайлангийн дугаар 2022 оных тогтмол 20220301-001
        type: төрөл 1 бол Хариуны PDF хаяг 2 бол Асуумж бөглөх хаяг авах
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "regnum": regnum,
            "report": report,
            "type": type,
        },
        auth,
        operator,
    )
    return parse_model(EarlyDetectionServiceResponse, data)
