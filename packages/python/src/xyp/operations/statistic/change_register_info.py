"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS200115_changeRegisterInfo (statistic, Улсын бүртгэлийн ерөнхий газар)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from datetime import date, datetime

from pydantic import Field

from xyp._models import XypDate, XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS200115_changeRegisterInfo"


class ChangeRegisterInfoListData(XypModel):
    """мэдээллийн жагсаалт"""

    regnum_old: str | None = Field(default=None, alias="regnumOld", description="Шинэ регистр")
    regnum_new: str | None = Field(default=None, alias="regnumNew", description="Хуучин регистр")
    date: XypDate | None = Field(
        default=None, alias="date", description="Регистр сольсон он сар өдөр цаг минут секунд"
    )


class ChangeRegisterInfoResponse(XypModel):
    """WS200115_changeRegisterInfo: Регистр сольсон мэдээлэл"""

    list_data: list[ChangeRegisterInfoListData] | None = Field(
        default=None, alias="listData", description="мэдээллийн жагсаалт"
    )


def change_register_info(
    self: SyncService,
    *,
    date: datetime | date | str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> ChangeRegisterInfoResponse:
    """WS200115_changeRegisterInfo: Регистр сольсон мэдээлэл

    Provider: Улсын бүртгэлийн ерөнхий газар

    Args:
        date: огноо
    """
    data = self._transport.call(
        _OPERATION,
        {
            "date": date,
        },
        auth,
        operator,
    )
    return parse_model(ChangeRegisterInfoResponse, data)


async def change_register_info_async(
    self: AsyncService,
    *,
    date: datetime | date | str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> ChangeRegisterInfoResponse:
    """WS200115_changeRegisterInfo: Регистр сольсон мэдээлэл

    Provider: Улсын бүртгэлийн ерөнхий газар

    Args:
        date: огноо
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "date": date,
        },
        auth,
        operator,
    )
    return parse_model(ChangeRegisterInfoResponse, data)
