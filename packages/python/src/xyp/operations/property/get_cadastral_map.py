"""GENERATED by xyp-generator from spec/services.json. Do not edit.

WS100222_getCadastralMap (property, Газар зохион байгуулалт, геодези, зураг зүйн газар)
"""
# Operation modules are this package's own internals, so they may use the service's transport.
# pyright: reportPrivateUsage=false

from __future__ import annotations

from pydantic import Field

from xyp._models import XypModel, parse_model
from xyp._service import AsyncService, SyncService
from xyp.auth import CitizenAuth, OperatorAuth

_OPERATION = "WS100222_getCadastralMap"


class GetCadastralMapResponse(XypModel):
    """WS100222_getCadastralMap: Кадастрын зураг авах"""

    data: str | None = Field(default=None, alias="data", description="Кадастрын зурагны мэдээлэл")


def get_cadastral_map(
    self: SyncService,
    *,
    parcel_id: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> GetCadastralMapResponse:
    """WS100222_getCadastralMap: Кадастрын зураг авах

    Provider: Газар зохион байгуулалт, геодези, зураг зүйн газар

    Args:
        parcel_id: Нэгж талбарын дугаар
    """
    data = self._transport.call(
        _OPERATION,
        {
            "parcelId": parcel_id,
        },
        auth,
        operator,
    )
    return parse_model(GetCadastralMapResponse, data)


async def get_cadastral_map_async(
    self: AsyncService,
    *,
    parcel_id: str | None = None,
    auth: CitizenAuth | None = None,
    operator: OperatorAuth | None = None,
) -> GetCadastralMapResponse:
    """WS100222_getCadastralMap: Кадастрын зураг авах

    Provider: Газар зохион байгуулалт, геодези, зураг зүйн газар

    Args:
        parcel_id: Нэгж талбарын дугаар
    """
    data = await self._transport.call(
        _OPERATION,
        {
            "parcelId": parcel_id,
        },
        auth,
        operator,
    )
    return parse_model(GetCadastralMapResponse, data)
