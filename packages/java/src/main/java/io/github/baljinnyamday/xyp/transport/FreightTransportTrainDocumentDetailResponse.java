// Code generated by xyp-generator from spec/services.json. DO NOT EDIT.

package io.github.baljinnyamday.xyp.transport;

import io.github.baljinnyamday.xyp.Decoders;
import io.github.baljinnyamday.xyp.Extras;
import io.github.baljinnyamday.xyp.ResponseReader;
import java.util.List;

/**
 * The response of {@code WS100468_FreightTransportTrainDocumentDetail}: Галт тэрэгний ачаа тээврийн
 * бичиг баримтын дэлгэрэнгүй мэдээлэл авах сервис.
 *
 * <p>A field XYP left out, or sent in a shape this record does not expect, is {@code null} (an
 * empty list for a list); {@link #extras()} tells the two apart.
 *
 * @param bill Тээврийн бичиг баримтын ерөнхий мэдээлэл
 * @param freightForwarder Зуучийн мэдээлэл
 * @param sender Илгээгчийн мэдээлэл
 * @param receiver Хүлээн авагчийн мэдээлэл
 * @param stations Өртөөний мэдээлэл
 * @param payment Төлбөрийн мэдээлэл
 * @param freights Ачааны мэдээлэл
 * @param wagons Вагоны мэдээлэл
 * @param files Гэрээний файлууд
 * @param extras the response fields that did not fit this record (their component is {@code null}
 *     or empty) and the raw response, including the fields this record does not declare
 */
public record FreightTransportTrainDocumentDetailResponse(
    Object bill,
    Object freightForwarder,
    Object sender,
    Object receiver,
    Object stations,
    Object payment,
    List<Freights> freights,
    List<Wagons> wagons,
    List<String> files,
    Extras extras) {

  /**
   * Decodes this record from a response tree. The generated client uses it; it is public so you can
   * decode a tree you already hold, with {@link ResponseReader#decode}.
   *
   * @param reader the reader over this record's element
   * @return the decoded record
   */
  public static FreightTransportTrainDocumentDetailResponse decode(ResponseReader reader) {
    return new FreightTransportTrainDocumentDetailResponse(
        reader.get("bill", Decoders.ANY),
        reader.get("freightForwarder", Decoders.ANY),
        reader.get("sender", Decoders.ANY),
        reader.get("receiver", Decoders.ANY),
        reader.get("stations", Decoders.ANY),
        reader.get("payment", Decoders.ANY),
        reader.get("freights", Decoders.list(Decoders.object(Freights::decode))),
        reader.get("wagons", Decoders.list(Decoders.object(Wagons::decode))),
        reader.get("files", Decoders.list(Decoders.STRING)),
        reader.extras());
  }

  /**
   * Ачааны мэдээлэл.
   *
   * @param gng Ачааны ГНГ
   * @param name Ачааны мэдээлэл
   * @param weight Ачааны жин (кг)
   */
  public record Freights(String gng, String name, String weight) {

    /**
     * Decodes this record from a response tree. The generated client uses it; it is public so you
     * can decode a tree you already hold, with {@link ResponseReader#decode}.
     *
     * @param reader the reader over this record's element
     * @return the decoded record
     */
    public static Freights decode(ResponseReader reader) {
      return new Freights(
          reader.get("gng", Decoders.STRING),
          reader.get("name", Decoders.STRING),
          reader.get("weight", Decoders.STRING));
    }
  }

  /**
   * Вагоны мэдээлэл.
   *
   * @param number Вагон дугаар
   * @param type Вагон төрөл
   * @param owner Вагон эзэмшигч
   */
  public record Wagons(String number, String type, String owner) {

    /**
     * Decodes this record from a response tree. The generated client uses it; it is public so you
     * can decode a tree you already hold, with {@link ResponseReader#decode}.
     *
     * @param reader the reader over this record's element
     * @return the decoded record
     */
    public static Wagons decode(ResponseReader reader) {
      return new Wagons(
          reader.get("number", Decoders.STRING),
          reader.get("type", Decoders.STRING),
          reader.get("owner", Decoders.STRING));
    }
  }
}
