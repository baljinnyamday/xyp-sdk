// Code generated by xyp-generator from spec/services.json. DO NOT EDIT.

package io.github.baljinnyamday.xyp.health;

import io.github.baljinnyamday.xyp.Decoders;
import io.github.baljinnyamday.xyp.Extras;
import io.github.baljinnyamday.xyp.ResponseReader;
import java.util.List;

/**
 * The response of {@code WS300152_GetHealthInsurancePremiumList}: Эрүүл мэндийн даатгалын шимтгэл
 * төлөгдсөн болон төлөгдөөгүй саруудыг ялгаж буцаах сервис.
 *
 * <p>A field XYP left out, or sent in a shape this record does not expect, is {@code null} (an
 * empty list for a list); {@link #extras()} tells the two apart.
 *
 * @param payedList төлөгдсөн саруудын жагсаалт
 * @param notPayedList төлөгдөөгүй саруудын жагсаалт
 * @param extras the response fields that did not fit this record (their component is {@code null}
 *     or empty) and the raw response, including the fields this record does not declare
 */
public record GetHealthInsurancePremiumListResponse(
    List<PayedList> payedList, List<NotPayedList> notPayedList, Extras extras) {

  /**
   * Decodes this record from a response tree. The generated client uses it; it is public so you can
   * decode a tree you already hold, with {@link ResponseReader#decode}.
   *
   * @param reader the reader over this record's element
   * @return the decoded record
   */
  public static GetHealthInsurancePremiumListResponse decode(ResponseReader reader) {
    return new GetHealthInsurancePremiumListResponse(
        reader.get("payedList", Decoders.list(Decoders.object(PayedList::decode))),
        reader.get("notPayedList", Decoders.list(Decoders.object(NotPayedList::decode))),
        reader.extras());
  }

  /**
   * төлөгдсөн саруудын жагсаалт.
   *
   * @param year жил
   * @param month сар
   */
  public record PayedList(Long year, Long month) {

    /**
     * Decodes this record from a response tree. The generated client uses it; it is public so you
     * can decode a tree you already hold, with {@link ResponseReader#decode}.
     *
     * @param reader the reader over this record's element
     * @return the decoded record
     */
    public static PayedList decode(ResponseReader reader) {
      return new PayedList(reader.get("year", Decoders.INT), reader.get("month", Decoders.INT));
    }
  }

  /**
   * төлөгдөөгүй саруудын жагсаалт.
   *
   * @param year жил
   * @param month сар
   */
  public record NotPayedList(Long year, Long month) {

    /**
     * Decodes this record from a response tree. The generated client uses it; it is public so you
     * can decode a tree you already hold, with {@link ResponseReader#decode}.
     *
     * @param reader the reader over this record's element
     * @return the decoded record
     */
    public static NotPayedList decode(ResponseReader reader) {
      return new NotPayedList(reader.get("year", Decoders.INT), reader.get("month", Decoders.INT));
    }
  }
}
