// Code generated by xyp-generator from spec/services.json. DO NOT EDIT.

package io.github.baljinnyamday.xyp.property;

import io.github.baljinnyamday.xyp.Decoders;
import io.github.baljinnyamday.xyp.Extras;
import io.github.baljinnyamday.xyp.ResponseReader;
import java.util.List;

/**
 * The response of {@code WS100254_getBankPropertyInquiry}: Үл хөдлөх эд хөрөнгийн дэлгэрэнгүй
 * лавлагаа-Банк.
 *
 * <p>A field XYP left out, or sent in a shape this record does not expect, is {@code null} (an
 * empty list for a list); {@link #extras()} tells the two apart.
 *
 * @param propertySize the {@code propertySize} field
 * @param propertyNationRegnum the {@code propertyNationRegnum} field
 * @param propertyValue the {@code propertyValue} field
 * @param cadastNumber the {@code CadastNumber} field
 * @param propertyType the {@code propertyType} field
 * @param propertyUseKnd the {@code propertyUseKnd} field
 * @param propertyUseSub the {@code propertyUseSub} field
 * @param comptStatus the {@code ComptStatus} field
 * @param address the {@code address} field
 * @param isClosed the {@code isClosed} field
 * @param owner жагсаалт
 * @param history жагсаалт
 * @param extras the response fields that did not fit this record (their component is {@code null}
 *     or empty) and the raw response, including the fields this record does not declare
 */
public record GetBankPropertyInquiryResponse(
    String propertySize,
    String propertyNationRegnum,
    String propertyValue,
    String cadastNumber,
    String propertyType,
    String propertyUseKnd,
    String propertyUseSub,
    String comptStatus,
    String address,
    Long isClosed,
    List<Owner> owner,
    List<History> history,
    Extras extras) {

  /**
   * Decodes this record from a response tree. The generated client uses it; it is public so you can
   * decode a tree you already hold, with {@link ResponseReader#decode}.
   *
   * @param reader the reader over this record's element
   * @return the decoded record
   */
  public static GetBankPropertyInquiryResponse decode(ResponseReader reader) {
    return new GetBankPropertyInquiryResponse(
        reader.get("propertySize", Decoders.STRING),
        reader.get("propertyNationRegnum", Decoders.STRING),
        reader.get("propertyValue", Decoders.STRING),
        reader.get("CadastNumber", Decoders.STRING),
        reader.get("propertyType", Decoders.STRING),
        reader.get("propertyUseKnd", Decoders.STRING),
        reader.get("propertyUseSub", Decoders.STRING),
        reader.get("ComptStatus", Decoders.STRING),
        reader.get("address", Decoders.STRING),
        reader.get("isClosed", Decoders.INT),
        reader.get("Owner", Decoders.list(Decoders.object(Owner::decode))),
        reader.get("History", Decoders.list(Decoders.object(History::decode))),
        reader.extras());
  }

  /**
   * жагсаалт.
   *
   * @param nation the {@code Nation} field
   * @param surName the {@code surName} field
   * @param firstName the {@code firstName} field
   * @param lastName the {@code lastName} field
   * @param regnum the {@code regnum} field
   * @param citizenCompCode the {@code citizenCompCode} field
   */
  public record Owner(
      String nation,
      String surName,
      String firstName,
      String lastName,
      String regnum,
      String citizenCompCode) {

    /**
     * Decodes this record from a response tree. The generated client uses it; it is public so you
     * can decode a tree you already hold, with {@link ResponseReader#decode}.
     *
     * @param reader the reader over this record's element
     * @return the decoded record
     */
    public static Owner decode(ResponseReader reader) {
      return new Owner(
          reader.get("Nation", Decoders.STRING),
          reader.get("surName", Decoders.STRING),
          reader.get("firstName", Decoders.STRING),
          reader.get("lastName", Decoders.STRING),
          reader.get("regnum", Decoders.STRING),
          reader.get("citizenCompCode", Decoders.STRING));
    }
  }

  /**
   * жагсаалт.
   *
   * @param serviceName the {@code serviceName} field
   * @param registeredDate the {@code registeredDate} field
   * @param owner the {@code owner} field
   * @param ownerStatus the {@code ownerStatus} field
   * @param contract the {@code contract} field
   * @param contractPrice the {@code contractPrice} field
   * @param imageCount the {@code imageCount} field
   */
  public record History(
      String serviceName,
      String registeredDate,
      String owner,
      String ownerStatus,
      String contract,
      String contractPrice,
      Long imageCount) {

    /**
     * Decodes this record from a response tree. The generated client uses it; it is public so you
     * can decode a tree you already hold, with {@link ResponseReader#decode}.
     *
     * @param reader the reader over this record's element
     * @return the decoded record
     */
    public static History decode(ResponseReader reader) {
      return new History(
          reader.get("serviceName", Decoders.STRING),
          reader.get("registeredDate", Decoders.STRING),
          reader.get("owner", Decoders.STRING),
          reader.get("ownerStatus", Decoders.STRING),
          reader.get("contract", Decoders.STRING),
          reader.get("contractPrice", Decoders.STRING),
          reader.get("imageCount", Decoders.INT));
    }
  }
}
